# DWYP Operations Platform — Platform State
**Version: 7.10 | June 2026**

**Companion documents:** see `CLAUDE.md` for canonical doc inventory.

> Changelogs v6.1–v6.5 stripped. See git history for session-by-session detail.

---
## Current Position

- **Reckoning Phase 2 — 2-D / 2-E / 2-B (2026-06-15, pushed, /dev verification pending).** Three CIM-scoped spokes executed on branch `review-revise`. Spoke docs stay in `docs/` at `STATUS: ready for Code` until /dev verification, then flip to `COMPLETE — safe to delete`.
  - **2-D Audit_Trail positional-write fix.** `saveDerivativeAsset` (`dwyp_app.js`) wrote audit rows via bespoke positional `appendRow([now,'DWYP_App',event,assetId])` — transposed (actor→Event_Category, event→Actor, assetId→Episode_UID). Both branches (`save`→`DERIVATIVE_SAVE`, `save_copy`→`DERIVATIVE_COPY` — identical-defect sibling, surfaced + fixed in-scope) now route through canonical `logToAuditTrail(actor, eventCategory, episodeUid, contactId, detail)`: assetId → Detail, real Episode_UID (from AL `src` row) → Episode_UID column. Helper also bumps `audit_trail` version (bespoke writes didn't). Broader Event_Category/Actor column-semantics normalization remains for **D13/2b** — not touched.
  - **2-E Error-task idempotency.** `_pulse_spawnErrorTask` (`fairy_circle.js`) re-spawned an `Errors` task every pulse on a persistent failure. Added inline open-task guard (mirrors `_pulse_checkAssetIdIntegrity` / `spawnReviseAssetTask`) keyed on `(Episode_UID, Workflow_Step="Errors", Action_Title)` — title encodes guest+stage, so distinct stages each surface while the same persistent condition dedups. **Open decision (surfaced, recommend as-is):** guard stops per-pulse churn while a task stays open; it does NOT suppress re-spawn after a *completed* task whose root condition persists. No completed-window guard added — a genuinely recurring error should re-surface; revisit only if churn observed.
  - **2-B Bio_Summary sentinel eviction (D12).** Bio_Summary → content-or-blank; enrichment status lives in tasks. (1) Herald (`herald_fairy.js`) unconfirmed-identity path no longer writes `"Enrichment Pending"` — leaves cell untouched; pending status carried by the `Verify_Guest_Identity` task spawned in `runHerald` (**not** `Guest_Brief_Enrich` as the spoke stated — name corrected). (2) `writeBioSummary` strips a leading `**Bio_Summary:**` markdown-label artifact + trims (single Herald-only choke point; already bumps `contacts`). (3) Mending O1 (`housekeeping.js`): gate converted from interim module var to Governance_Config read `MEND_O1_GUESTBIO_ENABLED` (idiom `String(getGovernance(...)||'').toUpperCase()==='TRUE'`), fail-closed to disabled, **silent** return when off (no per-episode noise). Two sentinel-detecting readers inside O1 simplified (`!== 'Enrichment Pending'` clauses dropped → plain blank-check). **Audra hand-steps:** add Governance_Config key `MEND_O1_GUESTBIO_ENABLED=false`; clean existing sentinel cells in Bio_Summary (Carrie `"Enrichment Pending"`, failure prose, `**Bio_Summary:**` residue). **Phase 3 doc-sync owed:** Reference §Contacts — Bio_Summary = content-or-blank, enrichment status task-derived.
  - **Hanging — /dev verification (all three):** 2-D — derivative save + save_copy land Audit_Trail rows with each value in its correct column. 2-E — persistent error across two pulses yields exactly one open `Errors` task. 2-B — unconfirmed identity → blank Bio_Summary + open `Verify_Guest_Identity` task; confirmed → real bio, no markdown prefix. After pass, flip all three spoke `STATUS:` lines to `COMPLETE — safe to delete`.
- **Pipeline:** Tracks A/B/C all shipped. Bridge v2 (May 2026) shipped: compositional prompt assembly, v2.3 HOOK/QUOTE parsing, Slide_Index write retired. Verified on Derek Peterson (EP-260430-1427). **Caption sign-off — NOT live (gap, 2026-06-09):** `CAPTION_SIGNOFF` key is populated in Governance_Config, but no `_appendCaptionSignoff_` helper exists in code — the key has no consumer. Programmatic sign-off is unbuilt; queue as a build spoke if wanted.
- **Locked hub decision (2026-05-22): Transcript is the single source of truth for all copy passes.** Track A (Index) and Track B (Show Notes) both read the raw transcript independently via `gatherVertContext`. Neither is downstream of the other. No Vertex/RAG retrieval in either pass. Transcript injected directly.
- **Track A (CP1+CP2 — 2026-05-22):** Truncation cap removed from `buildEpisodeIndexPrompt` (was `.substring(0, 15000)`). Verified on Meenakshi — full arc now spans to Seeds of Wisdom close. AI Search Index block appended after REEL DESCRIPTIONS via `extractPrompt("# AI Search Index")` + `extractPrompt("# Pillars")` + `extractPrompt("# Voice Prohibitions")`, fenced by boundary line (literal extraction / curatorial indexing). **AI Search Index — redesigned and validated (2026-06-04):** section redesigned: Orientation + Location and Defining Arc sections added; Search Keywords cut; cross-episode references removed. Emission bug fixed in `buildEpisodeIndexPrompt` (block was silently absent). Freshness build-stamp added to index header. Anchor uniqueness instruction hardened (uniqueness-first anchor rule in template). Index now emits and has been validated on real episodes. **Open:** full-library rebuild verification (existing indexes pre-date redesign); anchor uniqueness at scale.
- **Track B (CP3 — 2026-05-22):** `runEditorialPass` repointed to transcript. No longer requires index v2 to exist first (dependency removed). `_buildEditorialPassPrompt_` now injects `FINISHED TRANSCRIPT:` instead of `EPISODE INDEX V2:`. Guest brief no longer in the prompt (removed 2026-06-09 — brief is CRM/JT-prep only). Verbatim-quote rule is now honest — Claude selects from real words on the page.
- **Reel Editorial pass — retired (2026-06-06).** `runReelEditorialPass` and `_parseReelEditorialOutput_` deleted from `vert_fairy.js`. Replaced by Gemini dual-output audio analysis writing directly to Asset_Library (see `syncReelAssets` bullet below). `_pulse_reelChain` calls `syncReelAssets` only — no editorial pass. `generateReelCaption` reads col 9 (`Reel_Summary`, the gloss). Dedup scoring uses either column.
- **`syncReelAssets` + Gemini audio analysis (2026-06-06).** `callGeminiVideoAnalysis_` retired; replaced by `callGeminiAudioAnalysis_` — server-side chunked resumable upload via Drive byte-range fetch (`REEL_UPLOAD_CHUNK_BYTES` governance key, default 40 MB; `Content-Length` header omitted — GAS-restricted). Dual-output prompt keyed by `REEL_ANALYSIS_PROMPT_KEY` (`# Reel Audio Analysis` section in Master Template, old `# Reel Editorial` section and key retired by Audra): Gemini returns `TRANSCRIPT:` + `GLOSS:` blocks; `_parseReelAudioResponse_` splits them; writes `Reel_Transcript` (col 8, verbatim) + `Reel_Summary` (col 9, scannable gloss) per reel. `hasSummary` gate checks col 8 — per-reel idempotency, resumes on re-run. Timeout guard bumped to 20 min (was 4.5 min). Rationale: tuned for an unusually large batch (9 clips, EP-260430-1505); normal episodes will process well inside 4.5 min, but the guard must accommodate the worst case. 20 min leaves a 10-min buffer under the Workspace 30-min hard ceiling — safe for unattended DP runs. **Client-deploy note:** consumer GAS accounts cap at 6 min; this guard must be lowered (e.g. 4 min) before deploying to a non-Workspace account. `_pulse_spawnUploadTask` added to `fairy_circle.js` — spawns `Upload_Produced_Episode` task when `Status === in_production` and no open/in_progress task exists; called from Stage 1 transcript-detect path and Stage 2 `in_production` block. Validated on EP-260430-1505 (Dr. Buck Blodgett): all 9 reels processed in a single pass.
- **Episode tab (Design) — live (2026-05-26).** Native `<video>` in `stEpView` fed by `getEpisodeStreamUrl` (V4-signed GCS GET URL, bucket `dwyp-review-playback`, path `episodes/{EUID}/proxy.mp4`, 8h expiry). Signing via `iamcredentials.googleapis.com :signBlob` using owner's `ScriptApp.getOAuthToken()`; explicit project-level signBlob grant required — `iam.automaticIamGrantsForDefaultServiceAccounts` org policy is active (AD #122). Governance keys `REVIEW_GCS_BUCKET`, `GCS_SIGNER_SA`, `GCS_EXPIRY_SECONDS` populated. Compose loop functional: focus-pause-freeze-timestamp, Send = optimistic rail append + `submitEpisodeCommentRow` + resume, Cancel = discard + resume. Request Revisions and Approve wired. **Upload affordance (2026-05-26, end-to-end test pending):** `Upload_Produced_Episode` task card renders "Upload Proxy" button → browser-side resumable chunked upload direct to GCS (`getEpisodeUploadUrl` mints a V4-signed POST URL; 5 MB chunks via `Content-Range`; 308 = continue, 200/201 = done). On success `completeUploadEpisode` fires automatically → `Video_Status → review` + `Review_Episode` spawned (loop A folder-watch retired). Self-heal path: if browser died after upload but before completion, re-clicking "Upload Proxy" detects existing proxy via `checkEpisodeProxyExists` and offers to complete without re-uploading. CORS: allow-list pinned to Apps Script `googleusercontent.com` sandbox origin — if origin drifts, preflight fails (AD #122). **Confirmed working 2026-05-26** on a 3-second and a 60-minute+ video. Known behavior (deferred): manually pausing the video before sending a comment produces no timestamp — only focus-triggered pauses stamp a time.
- **Reels sub-tab (Design) — live (2026-05-24, refined 2026-05-25).** Drive `/preview` playback in phone frame. Caption box reads `Caption_Host`; Generate writes caption from `Reel_Summary_Clean` via Claude (`generateReelCaption`). Two verbs: Export (moves reel → `Manual_Exports/`, filename = title slug; paired `.txt`), Request Revision (popup task form → `requestReelRevision` → `Revision_Notes`). Edit with Vids and day-picker removed. `closeReelRevision`: swaps `Drive_File_ID` on AL row by `Asset_ID`, moves old file → `Reels/Superseded/`, completes open `Revise_Reels` task, `bumpVersion` both domains, next Pulse re-spawns `Review_Reels`. **Drive chrome shown** — two-person internal app; crop hack retired. `getReelStreamUrl` returns `/preview` URL (was `uc?id=`). Reel cards: thumbnail left 1/3 (Drive thumbnail API `?sz=w200`), name + full summary right 2/3, no truncation. Modal z-index bug fixed (duplicate `z-index:200` was overriding `400`, hiding modal behind Studio Overlay at z-index 300). Reel revision modal centered with full corner radius.
- **Revision Flow spoke — live (2026-05-24).** Episode review compose loop: focus pauses video + freezes timestamp; Send = optimistic rail append + `submitEpisodeCommentRow` write + resume; Cancel = discard + resume; empty Send = no-op + resume. Rail receipts: round-grouped sealed cards (`Revision_Round` from Episode_Log). Request Revisions = hard-seal → `requestEpisodeRevisions` GAS call → composer locks → sealed notice appended to round card. `Revise_Episode` Complete button wired to `completeEpisodeRevisionTask` → validates Episode/ single-video invariant before completing. Reel revision: inline textarea replaced by popup task form; fix text → `Revision_Notes` via `requestReelRevision(epUid, assetId, notes)`. Episode_Log extended to 10 cols (`Revision_Round` col 10; header added manually). `spawnTask()` now reads `revisionNotes` from taskConfig (fixes blank-instruction bug on all revision tasks). Note: episode player source was Drive `uc?id=` (broken for large files) → repointed to GCS signed URL in the 2026-05-26 spoke.
- **Tasks schema — col 17 added: `Asset_ID`** (FK to Asset_Library for revision tasks). `TASKS_COLS.Asset_ID = 17` in `dwyp_app.js`; `spawnTask()` in `fairy_circle.js` writes it header-driven. **Manual step required:** add `Asset_ID` header in column 17 of Tasks tab in production Master Sheet.
- **Master Template — v3.0 live.** Paste + B6 #3 (ATTRIBUTION parser) + B6 #2 (extractPrompt consolidation) all shipped 2026-05-20. extractPrompt CR/LF fix applied (fairy_circle.js) — v3.0 paste introduced \r line endings; split now handles \r/\n/\r\n. Smoke test (test_extractPromptSmokeTest) confirmed all 5 new section keys return non-empty. `# AI Search Index` section added to template (2026-05-22).
- **v3.0 coupling — B6 #3: SHIPPED.** ATTRIBUTION-line parser live. QUOTE block format and _bridgeParseRankedItems_ / materializeQuoteGraphicAssets shipped together 2026-05-20.
- **v3.0 coupling — B6 #2: SHIPPED.** ${brandVoice} retired. extractPrompt consolidation live: Guest Brief pulls # Show Philosophy / # Pillars / # Peer Shows; Editorial pass pulls # Host Voice / # Voice Prohibitions / # Caption Mechanics / # Ranking Schema / # Show Notes.
- **Voice-injection keys retired.** BRAND_VOICE_ID, CAPTION_VOICE_SUPPLEMENT_ID, DELIVERABLES_VOICE_SPEC_ID blanked 2026-05-19; B6 #2 shipped 2026-05-20 completes the retirement. In-template section extraction via extractPrompt is the active path.
- **Spoke 0 — Caption Consolidation: complete.** `Caption_Host` (col 10) and `Caption_Guest` (col 11) schema live. 3-variant JSON format + `_parseCaptionDraft_` parser retired. Enrichment generators `enrichQuoteAssetsFromTranscript` and `enrichReelsForEpisode` retired, plus caption UI affordances (`pbRegenCaption`, `pbGenerateCaption`, Regenerate + Generate caption buttons). **`generateReelCaption` is NOT retired — it is live** (`dwyp_app.js`, called from the Reels surface; reads `Reel_Summary`, col 9). Earlier draft of this line wrongly listed it as retired.
- **v3 Wiring:** Phase 2 shipped (dual-JSON canvas, Export button, viewport fix). Phase 3 (reel card expand) queued. Scheduling = separate Schedule surface, not yet built.
- **Design surface overhaul (May 2026).** Text scaling: corner drag normalizes scaleY→fontSize + scaleX→width, scale resets to 1; A+/A− buttons (2pt steps). Color picker: popover with brand row + complementary row (#235789 / #CBCBD4 / #638475 / #8D918B); `stApplyColor` clears per-character fill overrides. Overlay reset: circular arrow SVG replaces ✕, pushes history. Export PNG fix: `multiplier:2` removed (was producing blank canvas at 4K); `discardActiveObject` before capture. Export txt: caption field passed as second argument. Delete key guard: `isContentEditable` check prevents caption-field edits from removing canvas objects.
- **Publish tab retired (May 2026).** ~7,000 lines of `pb*` code removed: all CSS, stPublishPanel HTML, var pb state, ~120 `pb*` functions. Design is the sole landing route: `stSwitchTab()` Publish branch gone; `stStudioMain` always visible. `openStudio()` default: calls `stSwitchTab('design')` then `stAccSelectTaskView('episodes')` — app opens on Tasks > Episodes. `showEpisodeReview(episodeUid)` navigates to the episode review surface (was a stub to `openStudio('design')`). `pbAutoSelectEpisode()` retired; data-load auto-select wired to `stSelectGuest()` directly. (Note: a `docs/DWYP_Publish_Feature_Patterns.md` was referenced here as preserving 6 reusable patterns, but the file was never committed and does not exist — reference removed. If those patterns are still wanted, reconstruct from git history of the `pb*` code.)
- **Asset surfacing — rankings retired; code cleanup complete (2026-06-05).** JT curates by hand. No `Quality_Score` sort, no auto-surfaced ranked candidates, no slot auto-pairing. `Quality_Score` (col 19) + `Slot_Tags` (col 20) dropped from Asset_Library sheet; `ASSET_LIBRARY_COLS` entries removed; all write paths (`materializeQuoteGraphicAssets`) + read paths (`getRankedAssetLibraryCandidates` deleted) cleaned. **Open Audra hand-step:** Master Template `SLOT_TAGS:` / `QUALITY_SCORE:` emit lines — remove when convenient (benign with cols absent; no code readers remain).
- **Design and Schedule are separate surfaces.**
  - **Design** — compose. Pick hook/quote (images) or reel; author/edit caption; place on canvas; pick background.
  - **Schedule (Spokes 1–3 + 2.1 + 3b complete, 2026-06-05).** Left-rail guest sub-item (peer to Images · Reels · Episode). Pool = AL rows for this episode where `Status='schedule'`; placed assets badged + sunk (`Availability='placed'`). Workspace toggles The Week ↔ Swipe Package. The Week is template-driven from `Posting_Schedule` (no hardcoded days/slots/sentences). Drag from pool → slot places asset; drag slot card → pool or another slot to move or remove. Swipe = per-card rows above always-present drop zone; deduplication enforced. `edit ↗` round-trips to Images (canvas restored via `getAssetDisplayState` + `loadFromJSON`) or Reels (auto-selects rail card). Optimistic UI with server-revert on failure. Send to Schedule on Images: AL upsert at `Status:'schedule'` — no Drive render, no `Drive_File_ID` write. Pool card QG previews: offscreen Fabric bake from `Canvas_State` + `bgCache` (no Drive fetch for QG). Export buttons removed from Images surface (Export + day-picker) and Reels surface. **Export is Schedule-surface only.** **Single Export (2026-06-05):** "export" button on each pool card; `schExportSingle(assetId)` renders full-res PNG client-side (3× multiplier, 1080×1350) and calls `exportSingleScheduleAsset` → `Manual_Exports/Singles/`; reel COPY (archive stays in `Reels/`). Untested. **Export All (2026-06-05, rewritten):** `schExportAll()` renders all placed QG offscreen client-side, passes `imageRenders` map to `exportAllSchedule(episodeUid, imageRenders)` → `Manual_Exports/[Day]/`+`/SWIPE/` (flat, episode-scoped by staging location; no guest wrapper). Gate: `Availability=placed` on AL row. Re-run: day/SWIPE folders cleared; `Singles/` preserved. Reels COPIED (not link-in-text). Untested — needs a full week of placed assets. **Edit return path:** resolved by derivative mode — all three exit triad buttons (Save / Save a Copy / Discard) auto-switch to Schedule on commit.
  - **`Availability` removal rule (confirmed, 2026-06-02):** on week-slot removal, always flip to `'available'`. Binary flag is week-only; double week-placement is discouraged by sink+badge; rule is minimal and accepted.
  - **Ratio column present in `Posting_Schedule` (col 7).** Slot glow currently uses type-only matching (both sides normalize to `'reel'` or `'image'`); `schTypeMatches` is the single edit point if ratio matching is wired.
  - **`Staging/Schedule_Renders/`** — folder may exist in Drive from prior runs; no code writers remain. Flag for manual cleanup (Audra).
  - **Companion scope:** with Schedule separate, the per-asset companion needs only the asset-in-focus — no slot-stack siblings to thread.
- **Performance Foundation:** Phase 1.1–1.5 complete and live in production.
- **Staging retired as workflow.** Code pushes directly to production. Routing helpers preserved in codebase.
- **Reality Reconcile spoke — done (2026-06-09).** Three live bugs fixed: (1) `findGuestBriefInContactLibrary` restored in `filing_fairy.js` (scans Contact Library for `GuestBrief*` docs, prefers `GuestBrief_*_${epUid}`, falls back to most-recent; owned by Herald/CRM, not pipeline). (2) Hook parser fixed — `_bridgeParseRankedItems_` now reads `N. text` (`/^\s*(\d+)\.\s*(.+)$/gm`) matching `saveShowNotes`; both `vert_fairy.js:693` and the `dwyp_app.js:3612` card-editor primary path corrected. (3) `bumpVersion('social_assets')` — domain name confirmed kept (placements are a distinct domain from `asset_library`); no code change. `housekeeping.js:516` Vertex comment corrected (us-south1 is the locked working region; do not route to us-central1). `DWYP_Codebase_Map.md` refreshed (version-helper attribution, Schedule fn names, Pipeline Flow narrative). **Closed (2026-06-09):** `social_assets` Versions row populated (Version=0) by Audra.
- **Track C per-type gate + brief removal spoke — done (2026-06-09).** (1) `materializeQuoteGraphicAssets` gate replaced: now counts existing hooks (`Display_Name` prefix `Hook `) and quotes separately, excluding empty `Quote_Text` rows (neutralizes placeholder rows). Skips only when both types are at/above expected (10 hooks, 6 quotes); writes missing type only in partial case; force path unchanged. Buck/Becky/Kyla pattern: 7 existing quotes → `needQuotes=false`, 0 existing hooks → `needHooks=true` → hooks added only. Known-inert: legacy `"Hook:"`-prefix rows (pre-5/21 format, Carrie only) miscount as quotes — archived episodes never re-enter the chain. (2) Guest brief removed from pipeline: `gatherVertContext` no longer calls `findGuestBriefInContactLibrary`; `GUEST BRIEF:` block removed from `buildEpisodeIndexPrompt` (Track A) and `_buildEditorialPassPrompt_` (Track B). `findGuestBriefInContactLibrary` itself untouched in `filing_fairy.js` — Herald/CRM still own it. (3) Transcript-fallback log demoted: `findTranscriptInFolder` "no finished/clean" path now writes `state_change`/`info` (was `error`/`warning`) — designed behavior (Staging fallback to Raw), not a failure. **Verify (next pulse):** Buck/Becky/Kyla/Elizabeth episodes get hook rows with no duplicate quotes; Elizabeth gets quotes; Audit_Trail shows no brief-lookup lines for A/B; transcript fallback logs as info.
- **Mending Fairy v1 — done (2026-06-09).** `runMendingFairy(epUid, status)` wired into `runHousekeeping()` after `repairStagingSubfolders`. Three v1 ops: I4 (`_mend_I4_txtRegen` — T1 auto-write missing `.txt` caption sidecars for exported PNGs; caption sourced from Asset_Library via `_mend_I4_buildCaptionMap`; 3-attempt backoff); I1 (`_mend_I1_reconcile` — fill-when-absent + conflict detection for `guest_name` / `contact_id` across manifest vs. Episodes row; fill-when-absent: 3-attempt backoff; conflict: immediate `task_spawned`, no re-spawn nightly); O1 (`_mend_O1_guestBioEnrich` — Herald bio enrichment when `Bio_Summary` is empty; T2 fill-only; 3-attempt backoff; no researchable signals → immediate `task_spawned`; **gated by `MEND_O1_GUESTBIO_ENABLED`, default off — see Reckoning 2-B**). Two-axis design: `_mendDecide` / `_mendUpdate` own the backoff axis; per-op logic owns the T1/T2/T3 tier axis. `mend_ledger` persisted via single `patchManifest({ mend_ledger: ledger })` per run. Pre-release guard: fires only on `in_production` | `ready_to_release`. `MEND_MAX_ATTEMPTS = 3`. `test_runMendingFairy()` in dev_tools. **Hanging — unvalidated:** I4/I1/O1 untested on a live episode; validate via `test_runMendingFairy` on a real episode or wait for next nightly run. Confirm Audit_Trail entries, manifest ledger writes, and task spawns land as expected. **Hanging — PROPOSAL_ retirement:** when Spoke 1 (Show Notes Provenance) and Spoke 3 complete, `PROPOSAL_DWYP_Mending_Fairy.md` should be retired and its tiered autonomy patterns (`_mendDecide`/`_mendUpdate` + T1/T2/T3 taxonomy) routed to a permanent canon home (Platform_Reference AD or dedicated fairy-patterns doc).
- **Podcast band in Buckets workspace — done (2026-06-09).** Virtual projection per AD #127: `stRenderBucketsView()` renders a "Podcast" band of the current user's open/in-progress/blocked episode-linked tasks, grouped by guest name, due-date sorted. No `Bucket` write, no schema or User Registry change. Workflow-step tasks (`BK_POD_SPECIAL_STEPS`: uploads, reel swaps, release) render "Open in Episodes" (jump + auto-expand via `bkPodOpenEpisode`) instead of a Complete button — generic completion would skip pipeline side effects (`completeUploadEpisode` etc.). Generic tasks complete in place via `writeTaskComplete`. Known minor gap: jump can't auto-expand TBD/upcoming episode cards (no task region) — lands in Episodes view, fails quiet. "Today" band (all origins, due-date sorted) parked as the follow-on projection. Same session: AI companion rail chat inputs enlarged (`.ep-chat-input` min-height 34→64px, max 90→180px) + missing auto-grow wired (`epChatAutoGrow`, all four rails). **Hanging:** untested in browser — needs clasp push + visual pass.
- **Review-task completion bypass — fixed (2026-06-09).** Audit found two pre-existing bugs in `stRenderEpCardTaskList` (Studio Episodes view): (1) `Review_Episode` fell to the Kind-3 generic Done button → `writeTaskComplete` could close the review gate without the Approve/Request Revisions flow, contradicting the in-app review gate (Reference §76) — dashboard `renderTaskButtons` deliberately renders navigation-only for this step. (2) `Review_Guest_Brief` generic Done skipped the `approveGuestBriefEnrich` chain → JT's brief-review task never spawned. Fixed: Kind 2b (`Review_Episode` → "Review Episode" → `showEpisodeReview`, no completion affordance) and Kind 2c (`Review_Guest_Brief` → Open brief link + Approve → reuses dashboard `approveGuestBriefTask`). Podcast band updated to match (`Review_Episode` → direct `showEpisodeReview`; `Review_Guest_Brief` added to `BK_POD_SPECIAL_STEPS`). **Structural fix shipped same session (hub decision: flip to allow-list).** New shared gate `taskAllowsGenericComplete(t)` + `GENERIC_COMPLETE_STEPS` allow-list (`Custom_Images`, `Post_Social`, `Filing`, `Revise_Images`, `quick`; human-assigned always allowed). Studio Kind-3 fallback split: 3a approve-class reviews → Review link + Approve (same generic write as dashboard `approveTask`); 3b allow-listed/human → Done; 3c unrecognized fairy-spawned → no completion affordance, muted "System task" label. Podcast band fallthrough consults the same gate — ungated steps route to Episodes instead of rendering Complete. Behavior matrix verified (8/8 cases): Review_Episode→nav, Review_Guest_Brief→jump, uploads→jump, graphics reviews→approve, Filing→generic, unknown fairy step→no button, human/quick→generic. Future fairy steps with side-effect completion are now safe-by-default on all surfaces; add to `GENERIC_COMPLETE_STEPS` only when generic completion is confirmed safe.
- **Loose-task invisibility — fixed (2026-06-09, pre-JT-onboarding sweep).** Found: tasks with no `Episode_UID`, no `Bucket`, and `Workflow_Step !== 'quick'` rendered NOWHERE — dashboard loose-task rendering was retired (SPOKE_Tasks_3; `renderDashboardLoose` is dead code, still present but uncalled), Buckets view only renders named bands, mobile `submitNewTask` sent no bucket at all. Every loose task created from mobile was a sheet row with zero surfaces. Fixes: (1) **Unsorted band** in Buckets workspace — projection catching my open loose tasks whose Bucket is blank or stale; nothing can be invisible now. (2) Mobile `submitNewTask` routes `bucket: getBucketDefaultForUser(assignee)`. (3) Desktop Buckets form self-assign falls back to own `Default_Bucket` when no bucket picked. (4) Mobile optimistic task push replaced with `getTasks()` refetch — pushed rows had `_rowIndex: null`, so Complete/Delete on a fresh task errored until reload. (5) `bkRenderTaskCard` now applies the generic-completion gate (button hidden for unrecognized fairy steps). **Heads-up:** first Buckets render after deploy may surface previously-invisible stale rows in Unsorted — that's recovery, not a bug; triage them once. **Pre-JT checklist (existing known issues, not fixed here):** headphones gold/red inverted on episode cards (AD #124 — piecemeal fix forbidden, needs the full icon pass; tell JT to read the action line, not the icon color); manual video pause stamps no timestamp in episode review (focus-pause does — known deferred); Reels + Schedule companions pending validation (JT's surfaces — validate before she leans on them); derivative edit surface (edit ↗ / Save / Save a Copy) untested; JT's User Registry row needs `Buckets` + `Default_Bucket` populated by Audra before cross-assignment works.
- **Secretary eager verify-identity task retired (2026-06-09).** `runSecretaryForNewEvent` no longer spawns "New guest detected — verify identity" on `isNew && !guestEmail` — pre-Fix-20 residue. Herald's identity check is now the sole gate: confirmed → brief written, no task; unconfirmed → `Verify_Guest_Identity` task; handoff throw → existing "Herald failed to run" recovery task. Secretary logs an INFO line ("Deferring identity verification to Herald") on the thin-stub path. Low-confidence duplicate-contact task unchanged. **Hand-step:** the open "New guest detected — verify identity: Dovie Gena" task is from the retired path — if Herald confirmed Dovie and wrote the brief, complete/delete it manually.
- **Help Desk "coming soon" moved to right rail (2026-06-09).** Tasks workspace (Episodes + Buckets) now enters icon-rail mode: new `tasks` entry in `railState`/`railPaneMeta`, `stRailIconsTasks` strip with one life-buoy icon, `stHelpDeskPane` placeholder ("Help Desk coming soon"). Starts closed (no default pane). `stTasksAssets` added to `stHideAllPanels`. Workspace empty-state notes reverted. Untested in browser — verify the assets column renders at 36px in tasks mode during the visual pass.
- **Icon-rail unification + Schedule workspace restructure (2026-06-09).** All five Studio surfaces now use the single collapsible icon-rail system (`enterIconRailMode`/`setRailPane`): Images (already live), Reels (old tab-bar removed; `stSetDesignTab` enters `reels` mode), Episode (push drawer retired — `episode` entry added to `railState`/`railPaneMeta`, container = pane = `stEpBrowser`, single AI Chat icon `stIconBtnChatEp`, in-pane Close routes to `setRailPane`; `epDrawer*` functions left dead-in-place, janitorial removal candidate), Schedule (chat-only rail), Tasks (Help Desk placeholder). `railDefaults`: images→hq, reels→browse; episode/schedule/tasks start closed. Drag-handle width now remembered per rail mode (previously epDrawer-only; images/reels/schedule used to reset to 290px). **Schedule workspace restructure (hub decision 2026-06-09):** candidate pool moved out of the rail into the workspace as a right 1/3 column (Arrange pattern: slots 2/3, cards 1/3) — JT can see assets while the AI Chat rail is open. `schPoolPane`/`schPoolList` IDs preserved so pool render + drag-to-slot wiring is untouched; old Candidates/AI Chat tab bar removed; `stSetReelRailTab`/`schSetRailTab` are thin legacy wrappers to `setRailPane`. **Hanging — visual pass checklist (all untested):** reels tab opens with browse pane; episode chat opens/closes from strip icon and in-pane ✕; schedule pool cards render at the wider column width; drag pool→slot and slot→pool still work from the new DOM location; week/swipe toggle and Export All footer unaffected; rail collapse/expand on all five surfaces; dragged rail width persists per surface.
- **Show-notes header colon drift — fixed (2026-06-09).** Claude's editorial pass (Track B) began intermittently omitting trailing colons on section header lines ("HOOKS" instead of "HOOKS:"). Blast radius: `getShowNotesForEdit` parsed zero sections (silent blank card editor — now also guarded with a visible notice on ok+empty), `_vertBuildSectionProvenance_` produced empty provenance, Track C exact-string slicers (`HEADER_HOOKS` etc., `_bridgeSliceSection_`) miss their boundaries, `extractSectionFromProse` end-lookahead over-captures. Fixes: (1) `_normalizeShowNotesHeaders_` in `vert_fairy.js` — normalizes claudeResult to canonical "HEADER:" form immediately after the Claude call, before doc write and provenance (code backstop, Fix 17 pattern); (2) `getShowNotesForEdit` reader now colon-tolerant + canonicalizes headers, so existing colon-less docs render and type correctly; (3) `_vertBuildSectionProvenance_` header test colon-tolerant. Verified against fixture: 5 headers normalized, no false positives on numbered hooks / QUOTE / ATTRIBUTION / quoted-caps content lines. **Hand-steps (Audra):** add explicit "every section header is ALL CAPS followed by a colon on its own line" instruction to the `# Show Notes` Master Template section (prompt is primary enforcement); for episodes whose docs were generated colon-less, either hand-add colons or force re-run Track B — and check whether Track C already ran against a colon-less doc for those episodes (hook/quote asset extraction would have missed or over-captured).
- **Schedule pool cards — QG label + sizing fix (2026-06-09).** Pool card label for quote graphics was `Display_Name` ("Guest Quote 3"); now `Quote_Text` (the actual on-image text, same field the edit ↗ / save writeback preserves), `Display_Name` fallback. Reels: title primary, `Reel_Summary` (fallback `Caption_Host`) secondary. Cards enlarged for the workspace pool column: 64px thumb (was 38), name 3-line clamp (was single-line nowrap), caption 2-line clamp. No GAS changes. Untested in browser.
- **White canvas default for text-only QGs (2026-06-09).** Text-only assets (JT's send-to-schedule-without-designing flow) rendered on transparent canvas — unreadable thumbs, transparent exports. White is now the default at every render point unless a background is present: `stInitCanvas` sets `backgroundColor:'#FFFFFF'`; `stEnsureWhiteBg()` restores it after every `loadFromJSON` (undo/redo/ratio-change/schedule-edit round-trip — serialized states from before the default have no backgroundColor); both offscreen bakes (`_schRenderOneQgThumb`, `_schRenderQgFullRes`) ensure white in finalize. A background image covers the full canvas, so white never competes with a placed background. New canvas saves serialize the white bg into `Canvas_State`. **Open root-cause question (untraced):** JT's text-only sends arrive with text tiny at top-left ("3-line menu") — the creation path serializes bad text coords/size. Workaround: Audra resizes in edit. Trace which send path builds that canvas state before JT's first real week of sends.
- **Asset Deletion, Demotion & Quote Replenishment spoke — done (2026-06-10).** Four sections; all five touched files parse-validated; clasp-pushed (confirmed by Audra 2026-06-11). **§1 Reels permanent delete** — `deleteReelPermanent(episodeUid, assetId)` (`dwyp_app.js`): shared slot-block guard → tombstone (`Status=rejected`) → move MP4 to `Reels/Delete` (get-or-create, non-fatal) → bump `asset_library` + Audit. Red **Delete** button in reel action bar (`stReelDeleteBtn` / `stDeleteReel`), both JT + Audra, hidden in derivative mode; confirm "will not go to Bank." **§2 Schedule remove-from-pool** — `removeFromPool(episodeUid, assetId)` (`dwyp_app.js`): slot-block → **reel** demote `schedule→candidate`+`available` (returns to Reels, no Drive change) / **quote graphic** reject `→rejected`; bumps `asset_library` only (slot-block means no Social_Assets row touched). `schRemoveFromPool` "remove" button on pool cards; confirm on quote-reject only (reel demote silent). **Shared guard** `_assetOccupiesSlot_(assetId)`: true iff a Social_Assets row references the Asset_Library_ID with a non-empty Slot — catches week **and** swipe; never consults `Availability` (swipe placements don't set it). `getAssetLibraryRow` gained additive `Asset_Type` field. **§3 Bridge floor replenishment** — `materializeQuoteGraphicAssets` (`vert_fairy.js`) rewritten: status gate `{in_production, review, ready_to_release}` (applies to force path too); floor counts **non-rejected** quote_graphic rows only (rejected no longer suppress top-up); placeholder rows (empty Quote_Text + empty Display_Name) ignored; **text-dedup** against all remaining rows (active + live-rejected) via `_bridgeNormText_` so rejected texts are never resurrected (do-not-regenerate) and items never duplicated; force flips system-authored + JT-untouched rows → rejected and recreates fresh, JT-touched rows protected; names by parsed doc index; returns `replenished`/`created`/`rebuilt`/`skipped` (`status_gate` / `at_floor` / `no_new_items`). **No LLM top-up** (Audra: JT has an in-app path to pull more hooks/quotes; floor need not self-heal beyond the doc). Helpers `_bridgeNormText_`, `_bridgeGetEpisodeStatus_` (header-driven). **§4 Purge** — `_pulse_purgeRejectedAssets(agentName)` (`fairy_circle.js`), new **Daily Pulse Stage 7** (system-level, once per run): deletes `rejected` AL rows where `Release_Date + 30d < today`; episodes with no Release_Date never purged; trashes **by `Drive_File_ID`** (non-fatal; folder location cosmetic since a §1 move may leave the file elsewhere); bumps `asset_library` once if ≥1 deleted; Audit per purged row + sweep summary. `test_purgeRejectedAssets()` in `dev_tools.js`. **Hanging — untested:** all four sections unvalidated in browser/live — verify §1 delete (slot-block + Reels/Delete move), §2 demote/reject (slot-block), §3 replenishment naturally tops up hooks on EP-260430-1505 / EP-260501-1953 / EP-260501-1955 while `in_production` (OUT-scope backfill, verify-only), §4 purge via `test_purgeRejectedAssets`. **Reference appends — DONE (Hub, 2026-06-10):** AD #128 (deletion lifecycle: tombstone semantics, AD #99 purge exception, slot-block guard, operation model, replenishment gate) and AD #129 (six-state Episodes Status enum incl. `review` + `live`) appended; AD #99 prose and schema-section enum corrected at source.
- **Upload_Raw_Assets task + transcript-expectation gate spoke — done (2026-06-10).** `fairy_circle.js` only; not yet pushed (stacked with Deletion spoke — see push state below). **§1 Spawn** — `_pulse_spawnUploadRawTask` called from Stage 1 upcoming block when `Recording_Date <= today` (window math per AD #126 — `<=` not `===`; missed runs and retroactive date entries self-heal; idempotency guard skips if any non-cancelled Upload_Raw_Assets task exists for the episode). Assigns to `ASSIGNEE_PRODUCER`, priority normal; `Payload_Link` = Raw folder Drive URL built from `Raw_Folder_ID`; `Bucket` empty (AD #127); bumps `tasks` version via `spawnTask`. **§2 Auto-complete (dual path)** — Auto: `_pulse_completeUploadRawTask` called immediately before `patchEpisodes(epUid, { Status: "in_production" })` in Stage 1 transcript-detect block; reads Tasks fresh (avoids stale-cache race); marks first matching open/in_progress Upload_Raw_Assets row complete via `updateTaskStatus`; Audit INFO line (actor = pulse stage); bumps `tasks` version. Manual: `writeTaskComplete` in `dwyp_app.js` — generic, no special-step side effects; `Upload_Raw_Assets` is not in `BK_POD_SPECIAL_STEPS`. **§3 Expectation gate** — top of `_pulse_contentChain`: if no transcript detected, checks `_pulse_hasOpenUploadRawTask(epUid)`; open task found → Audit INFO "waiting on raw asset upload, content chain skipped" + `return 0` (no error task, no ERROR audit); no open task → falls through to existing urgent-error path unchanged. Gate fires on both scheduled and force paths. Miss-transcript error sites suppressed when gate fires: `vert_fairy.js:84-85` (gatherVertContext ERROR audit), `vert_fairy.js:100-101` (outer catch ERROR audit), `fairy_circle.js` Track A catch (`_pulse_spawnErrorTask`), Track B catch (`_pulse_spawnErrorTask`). Three new helpers: `_pulse_spawnUploadRawTask`, `_pulse_completeUploadRawTask`, `_pulse_hasOpenUploadRawTask`. `Upload_Raw_Assets` is now a live `Workflow_Step` value. **Hanging — untested:** Dr. Kathy Zhang (EP-260610-0520) recorded today (2026-06-10) at 17:00, still upcoming, no transcript — first live end-to-end exercise; tomorrow's pulse should spawn her Upload_Raw_Assets task with Raw folder link; gate should suppress Track A error while she is in that state; auto-complete should fire and close the task before `in_production` flip when transcript lands. Adam/Eric: raw assets uploaded by hand today; next pulse should detect transcripts and flip status; their pre-model open urgent error tasks have no paired Upload_Raw_Assets task and are manually closeable (predate the model, no auto-complete path). **Push state:** both batches clasp-pushed 2026-06-10 (rode with Contacts Surface CP1). Repo and deployment in sync as of that push.
- **Contacts Surface spoke (Phase 1: CRM-lite + Quick Add + Enrich) — done (2026-06-10).** First people surface (Surface Principle BC #7, locked by Hub 2026-06-10 — already captured, no doc action from Code). One surface, two chromes, inside Studio. **Chrome:** left-rail root item `Contacts` (after Tasks, `stAccSelectContacts`); center card feed `stContactsView` (`ct-*` CSS/JS family — distinct from `.st-ep-*`); right rail reserved for Phase 2 Companion (`contacts` entries in `railState`/`railPaneMeta`, empty strip `stRailIconsContacts`, container `stContactsAssets` — Companion drops icons/panes into these); mobile chrome at <=700px while surface active (`body.ct-active`): both rails + drag handle hidden, workspace-only, header back button → Tasks > Episodes. Sort: `Created_At` desc, client-side (`getContacts` server sort untouched; sort veto offered, declined). Reads version-checked (`st._cache.contacts`, key `'global'`); avatar Sync flush reloads in place. **Editing:** expanded card edits in place — `CONTACTS_WRITABLE` expanded to the 16 spoke-editable fields (system fields protected); `updateContactField` stamps `Last_Activity` on every front-end edit; text fields debounce 800ms, optimistic with rollback + card re-render on failure; Influence_Tier (LF/HI/EH) + Relationship_Type (multi) as write-through pills. **Create:** `createContactFromApp(fields, source)` — whitelist + header-driven append; system-sets Contact_ID/Source/Created_At/Last_Activity; bumps `contacts`; Audit `human_action` line; returns row with `_rowIndex`. Desktop Add overlay (`ctAddModal`, Display_Name required, `Source: manual`); mobile Quick Add (`ctQuickAddModal`: Name/Email/Social URL, >=1 to submit; soft dupe warn — normalized name / exact email / normalized URL vs Website+Social_*; inline Add anyway / Cancel, one tap, never blocks; `Source: quick_add`; single URL auto-classified to platform `Social_*` column by domain, else `Social_Other`). Creates are server-confirmed, not optimistic (loose-task `_rowIndex: null` precedent). **Enrich:** `enrichContactFromApp(contactId)` — `runHeraldBio` + if contact has an episode with Status in {upcoming, in_production, review, ready_to_release} (multiple: latest `Recording_Date` wins) → `runHeraldBrief` regen via existing path (spawns its usual Review_Guest_Brief task). Per-card Enrich button both chromes; pending spinner survives re-renders; feed refetches on completion. Quick Add auto-enrich fires only with an Email/Social anchor; name-only waits for manual Enrich (wrong-person guard). **No Herald signature changes.** Content edit in `runHeraldBio` research context: anchors lead (email, website, ALL populated socials — was first-social-only `socialSignal`, local removed) + Organization/Referred_By/Tags breadcrumbs; applies to all callers (intake, Mending O1); Phone deliberately excluded. **Production Notes retired:** Secretary shell-doc creation removed from `runSecretaryForNewEvent` (manifest init writes `asset_ids: {}`); `appendRevisionComment` doc-append retired entirely (Audra decision 2026-06-10) — Episode_Log is the sole revision record, signature unchanged (`stagingFolderId` param retained, unused); `PRODUCTION_NOTES_PROMPT_KEY` and `# Production Notes` were never referenced in code; zero Production Notes references remain. Existing PN docs in Drive untouched. **Hand-steps (Audra):** delete `PRODUCTION_NOTES_PROMPT_KEY` row from Governance_Config (deletable now); add `quick_add` to Source column validation; verify Relationship_Type validation list = Guest/Sponsor/Donor/Roundtable/Prospect/Denied; clasp push CP3 batch (`dwyp_app.js`, `dwyp_ui.html`, `herald_fairy.js`, `secretary_fairy.js`, `fairy_circle.js`). **Hanging — unvalidated:** Enrich end-to-end on a contact with an active episode (bio rewrite + brief regen + task spawn + Audit lines) and on an episode-less contact; pending-state behavior; next Secretary intake creates no PN doc + clean manifest; revision request lands in Episode_Log only; Quick Add dupe-warn precision vs live Contacts data (surface back if normalized-name false-positives); CP1/CP2 visual pass items if not yet exercised (mobile chrome at <=700px, rail enter/exit Contacts <-> other surfaces, edit rollback on failed save).
- **Optimistic UI & Post-Test Polish spoke — done (2026-06-10, Hub-as-spoke session).** Scoped from the 2026-06-10 browser-verification findings. **§1 Optimistic conversions** (`dwyp_ui.html`): `schRemoveFromPool` and `stDeleteReel` converted from write-then-reload to optimistic — local state mutates immediately (card leaves pool / reel leaves rail), backend write async, **visible rollback + error toast on failure** (card/reel restored at original index), cache invalidation on success only (no full surface reload). `schRemoveFromSlot` was already optimistic — untouched, served as the in-codebase pattern. Inventory result: contact blur-saves and slot placements already optimistic; mobile loose-task create deliberately server-confirmed (`_rowIndex` precedent) — no change. **§3 Slot-block visibility** (`dwyp_ui.html`): client-side block in `schRemoveFromPool` checked BEFORE the confirm dialog — any placement with non-empty Slot (week AND swipe, from local `sch.placements`) → "unschedule first" toast, no server call; pool-card remove button dimmed (`opacity:0.35`) with "Occupies a slot - unschedule first" title when slotted (new `slottedAny` set in `stRenderSchedulePool`, covers swipe unlike week-only `weekPlaced`). Server `_assetOccupiesSlot_` guard unchanged as backstop. Test-report note: the A1 "silently disabled" finding was actually a fast-fading toast — server message existed; the fix makes the block visible structurally. **§4 Social glyphs** (`dwyp_ui.html`): root cause — page loads **unpinned `lucide@latest`**, and current Lucide removed brand icons (instagram/youtube/linkedin/twitter render as empty boxes; core mic/globe/link fine). Both render sites (`CT_SOCIALS` + expanded-card socials array) swapped to stable core glyphs: camera / video / briefcase / at-sign. **Flag:** pin the Lucide CDN version in a future pass (unpinned `@latest` is how this broke silently). **§5 Asset_ID integrity check** (`fairy_circle.js`, `dev_tools.js`): `_pulse_checkAssetIdIntegrity` as **Daily Pulse Stage 8** — scans Asset_Library for duplicate Asset_ID values (primary key; ~20 first-match lookup sites assume uniqueness; the 2026-06-10 collision misrouted a `rejectAsset` write to Meenakshi's row). Detection only, no auto-fix: WARNING audit line naming IDs + row numbers, one urgent `Data_Integrity` task (idempotent — skips while any Data_Integrity task open/in_progress; suppressBump, pulse bumps tasks). `test_checkAssetIdIntegrity()` in dev_tools. Out of scope by decision: rewriting the 20 lookup sites to match Episode_UID (schema invariant stands; defend by detection). **Validation:** new code snippet-validated under node (syntax + execution); authoritative server-file validation = next clasp push (sandbox mirror was stale this session — see push-state note). **Hanging — untested in browser:** optimistic remove/demote/delete update instantly + roll back visibly on failure; slotted-card dim + pre-confirm block; glyphs render on contact cards; Stage 8 no-ops on clean data (`test_checkAssetIdIntegrity` should produce no audit line post-cleanup). **Hanging — data restore (Audra, time-boxed):** Meenakshi's quote row ("Suffering is the part you do alone…", EP-260504-0736) is `Status=rejected` from the misrouted A3 write; episode released 26-May → purge destroys it ~25-Jun unless flipped back to `candidate`. **Pushed (confirmed by Audra 2026-06-11)** (batch: `dwyp_ui.html`, `fairy_circle.js`, `dev_tools.js`). **Session note:** git HEAD predates all 2026-06-10 spokes — commit from VS Code (never from a sandboxed session; a stale mirror this session made complete files read as truncated).
- **Guest-brief approve loop — fixed (2026-06-11, AD #131).** Root cause: Audra's enrich task and JT's review task both carried `Workflow_Step=Review_Guest_Brief`; the shared `approveGuestBriefTask` always chained `writeTaskComplete` + `approveGuestBriefEnrich` → `spawnGuestBriefReviewForJT`, so every Approve netted a new `Review_Guest_Brief` (unbounded loop; observed 2 struck + 1 live on Derek Peterson). **Fix — spawner split:** `runHeraldBrief` enrich task now spawns `Workflow_Step='Guest_Brief_Enrich'`, `Action_Title="Enrich guest brief: <name>"` (`herald_fairy.js`). `spawnGuestBriefReviewForJT` keeps `Review_Guest_Brief` and gains an idempotency gate — new helper `hasOpenReviewGuestBrief(episodeUid)` (header-driven Tasks read) skips the spawn + logs an INFO Audit line if an open/in_progress `Review_Guest_Brief` already exists for the episode. **Fix — handler branch:** `approveGuestBriefTask(taskId, rowIndex, episodeUid, workflowStep)` (`dwyp_ui.html`) now takes a 4th `ws` arg: `Guest_Brief_Enrich` → `writeTaskComplete` + `approveGuestBriefEnrich` (respawn, correct); `Review_Guest_Brief` → `writeTaskComplete` only, terminal, no respawn. **Gate/render sites updated for `Guest_Brief_Enrich` parity** (all in `dwyp_ui.html`): `REVIEW_STEPS_ALL`/`APPROVE_STEPS` allow-list; dashboard `renderTaskButtons` (Open Brief link + Approve special-case, passes `ws`); Studio Kind 2c (branch + passes `ws`); `BK_POD_SPECIAL_STEPS`; `stGetEpBlockingTask` BLOCKING_ORDER; `buildBands` hasBrief. `Guest_Brief_Enrich` renders Approve (never generic Done) on every surface; bk-pod routes it to Episodes. **Hand-steps (Audra):** (1) manually complete/delete the live "Review guest brief: Derek Peterson" task (third spawn from the retired loop; struck rows untouched). (2) Any open producer-assigned `Review_Guest_Brief` rows are old enrich tasks — hand-flip `Workflow_Step` to `Guest_Brief_Enrich` or complete them (Code has no sheet access; identify by Assignee=producer + Workflow_Step=Review_Guest_Brief). Pushed 2026-06-11. **Hanging — untested:** approve an enrich task → exactly one JT `Review_Guest_Brief` spawns; approve the JT task → completes, nothing spawns; re-run the approval chain → gate holds, nothing spawns.
- **Upload lifecycle fix spoke (AD #130) — done and pushed (2026-06-11).** Two files: `dwyp_app.js`, `dwyp_ui.html`; both syntax-validated (node `--check` + inline-script extraction). Covers backlog #1/#3/#4/#5. **§1 Composer lock (A3):** Studio episode composer lock no longer trusts client-cached `ep.Video_Status`. Composer/footer rendering extracted to `_stEpApplyComposerLock(uid, locked, approved)`; `_stEpApplyRevisions` is now the authoritative reconciler — derives lock from `getEpisodeRevisionHistory.videoStatus`, mirroring mobile `renderEpisodeReview`. Automatic (no-refresh) unlock depends on the `episodes` version bump at re-upload completion (§5). **§2 Durable marker (AD #130e):** new Episodes col 15 `Upload_Started_At` (ISO-8601); presence = in-progress, value = start time + stale calc (`UPLOAD_STALE_MINUTES`, default 30, computed server-side). Set by `_markEpisodeUploadStarted(episodeUid)` **inside `getEpisodeUploadUrl`** (Hub-approved set-point — single mint point for all three upload paths; also flips open `Upload_Produced_Episode`/`Revise_Episode` task → `in_progress` as UI echo). Cleared by `completeUploadEpisode`, `completeProxyReplace`, `cancelEpisodeUpload` via `_clearEpisodeUploadMarker`. Surfaced through `getEpisodes` (`Upload_Started_At` + `Upload_Stale`) and `getEpisodeRevisionHistory` (`uploadStartedAt` + `uploadStale`). **§3 Player awareness + cancel:** both players refuse stale playback while marker set (Studio `_stEpApplyStreamUrl`/`_stEpApplyUploadMarker`; mobile `_epReviewApplyUploadMarker`) — show "Upload in progress (started <time>)" or, past threshold, "Upload interrupted - verify or re-upload" wired to the `checkEpisodeProxyExists` self-heal (mobile: informational only, no button per Surface Principle). Task cards + Episode-surface proxy slot render the same notice. Cancel = client abort flag (`epUploadAbort`) stops the chunk loop + `cancelEpisodeUpload` (clear marker + revert task to open); Cancel button added to all upload panels. **§4 Single proxy backend (AD #130a):** `getProxyStreamUrl` thinned to `return getEpisodeStreamUrl(episodeUid)` — mobile/dashboard review now streams the same V4-signed GCS object; Drive Episode/-folder scan retired. `completeEpisodeRevision` validation repointed Drive-scan → `checkEpisodeProxyExists`. **§5 Completer unification + gates (AD #130b/c/d):** shared `_spawnReviewEpisodeIfNone(uid, guest, contact)` gates `Review_Episode` spawn on no-open-row; called by `completeUploadEpisode` and the rewritten `completeEpisodeRevision` (now `-> review` + gated respawn + clear marker + complete task; the `-> pending` end state retired). `completeProxyReplace` routes through `completeEpisodeRevision` when an open `Revise_Episode` exists (`_findOpenReviseEpisodeTaskId`), else clears marker only. `approveEpisodeForRelease` now completes open `Review_Episode` row(s) on Approve; REVIEW_STEPS sweep in `completeFinalEpisodeUpload` unchanged (backstop). **Hand-steps (Audra/Hub, must exist before Push B verify):** `Upload_Started_At` header at Episodes col 15; `UPLOAD_STALE_MINUTES` in Governance_Config. **Revision_Round ride-along — Hub confirmed KEEP CURRENT (2026-06-11).** Spoke asked `maxRound || 1` -> `maxRound + 1`; not applied. Trace: client writes comment rows at `round = revisionRound + 1` (pre-incremented) and the rail seals at that same number, so current code already yields distinct, incrementing rounds (1,2,3); `maxRound + 1` would orphan the seal note (`stEp-round-N+1` with no comments). No code change. **Dead-code deletion — DONE (Hub-approved 2026-06-11).** `getEpisodeReviewContext` and `getProxyFileId` deleted from `dwyp_app.js` (Publish-accordion helpers, zero callers since Publish retired May 2026); `getEpisodeReviewContext` removed from Codebase Map Episodes row. **Hanging — untested in browser:** Push B (marker visible mid-upload from a 2nd tab; navigate-away+refresh shows in-progress/interrupted; cancel reverts cleanly; players refuse stale playback) and Push C (mobile review plays GCS proxy; revise via each of the three paths -> exactly one Review_Episode respawns -> Approve completes it, no co-resident duplicates; Revision_Round increments across two cycles).
- **Upload & task lifecycle verification spoke — done (2026-06-11, read-only diagnostic).** Settled backlog 🔴 #1–#6 root causes; full findings encoded in the two fix-spoke bullets above. Doc verdict: code matched State L291 — `requestEpisodeRevisions` auto-completes `Review_Episode`; AD #76 was wrong on both clauses and is corrected at source (Reference, 2026-06-11). Decisions locked: AD #130 (review task lifecycle, single GCS proxy backend, upload-in-progress marker — mechanism addendum: Episodes col 15 `Upload_Started_At`) and AD #131 (guest-brief vocabulary split). Backlog #6 closed as answered — auto-complete is intended behavior; observed co-residence was the ungated `Review_Episode` re-spawn, fixed in the upload-lifecycle spoke. The report's `Revision_Round` tangential ("round never increments") was a misread — trace confirmed rounds already increment via pre-incremented comment writes; do not re-add to backlog. Surviving tangential, promoted to backlog 🟠: tasks completed by client-cached `_rowIndex` (not `Task_ID`) can strike the wrong row after append/delete; pairs with generic-bucket Complete rendering for `APPROVE_STEPS` members (task-completion hygiene cluster, unscoped).
- **Interim fix — reel centering (pushed 2026-06-11).** Reels playback centered within the phone-frame iframe (`dwyp_ui.html`). Cosmetic, no behavior change. **Reels stay on Drive — deliberate (Audra, 2026-06-11), not deferred-by-default.** AD #130a's single-GCS-backend decision covers the episode review proxy only; `getReelStreamUrl` (Drive `/preview`) is the intended reels path. Revisit GCS reels only after the #7 jitter diagnostic delivers a verdict on GCS streaming quality — do not migrate before then.
- **Bug-fix thread (2026-06-11/12) — Enrich cluster + row-write hygiene. Done, pushed (confirmed by Audra 2026-06-12).** Files: `fairy_circle.js`, `herald_fairy.js`, `dwyp_app.js`, `dwyp_ui.html`; all syntax-validated (node --check + inline-script extraction; helpers fixture-tested under node). **§1 `extractJson` rewrite (#8, `fairy_circle.js`)** — old two-pass did blanket `\n`→`\\n` escaping that corrupted structural whitespace: ALL pretty-printed Gemini JSON failed both passes → `PARSE_ERROR`. Blast radius was all three consumers: Herald social fields (silently dropped), Organization extraction, and `checkGuestIdentity` (failed open to confirmed — Fix-20 identity gate effectively disabled). New three-pass: fence-strip + bracket-isolate, then (1) parse as-is, (2) `_escapeCtrlCharsInJsonStrings_` — state-machine escape of raw ctrl chars inside string literals only, (3) `normalizeSmartChars` repair last (curly-quote delimiters; no longer mutates typographic chars inside values that parse clean). Sentinel contract `{error:"PARSE_ERROR", raw}` unchanged. **§2 Enrich partial update (#10)** — new `getContactRow(contactId)` (`dwyp_app.js`, getContacts row shape + `_rowIndex`); `ctFireEnrich` success now single-row refetch → `ctAbsorbUpdated` in-place patch + `ctRerenderCard`; cache `versionsAtFetch` left stale deliberately (quiet refetch next nav-in); full reload survives only as fetch-failure fallback. **§3 Herald transcript anchor + honest results (#9 mechanical layer)** — `_heraldFindTranscriptForEpisode_` / `_heraldFindTranscriptForContact_` (`herald_fairy.js`; reuses vert_fairy `findTranscriptInFolder` cross-file; `HERALD_TRANSCRIPT_CHAR_CAP = 50000`; soft-fail, never blocks). Injected as context blocks (Fix-5 pattern — template untouched): bio research gets most-recent contact transcript; `runHeraldBrief` gets episode-specific transcript (post-recording re-briefs; pre-recording = no-op). `runHeraldBio` now returns `{bioUpdated, socialUpdated, orgUpdated, socialParseFailed, transcriptUsed}` (additive; existing callers ignore); `enrichContactFromApp` passes it through as `res.bio`; `ctEnrichToastText` renders what actually landed (partial-failure warning when social extraction failed). **#9 companion layer + #13 → `docs/PROPOSAL_DWYP_Contacts_Companion.md`** (STATUS: exploring; Herald/Gemini-powered Contacts rail companion per BC #7 reservation; "is this them?" chips + headshot picking live there; 6 open questions incl. CC-3 headshot-fetch spike). **§4 Row-write guards (backlog 🟠 task-completion hygiene)** — `_resolveTaskRow_(sheet, rowIndex, taskId)` (`dwyp_app.js`): verify Task_ID at claimed row → relocate by column scan on mismatch (INFO audit) → throw if gone (no write lands wrong). Wired into `writeTaskComplete`/`deleteTaskRow`/`uncompleteTask` (additive optional param; no-taskId = legacy passthrough); `taskId` threaded through all 10 client call sites. Same pattern for Contacts: `_resolveContactRow_` wired into `updateContactField` (4th optional `contactId` param), threaded through all 5 call sites incl. legacy #app-shell pair — **prerequisite for #16 contact deletes; manual row deletes are now safe for in-flight writes.** Note: manual sheet deletes don't bump `contacts` — stale client caches self-heal on next write via the guard; avatar Sync after hand-deleting is the clean habit. **§5 `bkRenderTaskCard` standalone-safe (APPROVE_STEPS rendering)** — bucket-band cards now mirror the Episodes-view kind ladder: `Review_Episode` → nav-only; `Review_Guest_Brief`/`Guest_Brief_Enrich` → Open brief + Approve via `approveGuestBriefTask`; approve-class → Approve + Review link (Kind-3a parity, was a bare generic Complete); allow-list gate; system-task label. All three render paths (named buckets, Unsorted, Podcast delegation) flow through it. **Hand-steps (Audra):** clasp push (4 files); optional — strengthen social-crawl prose in Master Template `HERALD_RESEARCH_PROMPT_KEY` section (crawl each anchor URL, pull linked profiles). **Hanging — untested in browser:** enrich partial card update + toast variants; bucket-card Approve/Review rendering; stale-index relocate (hand-delete a Contacts row, then edit a contact below it). **Janitorial flagged:** `findTranscriptInFolder` now has a second consumer (Herald) — AD #35 promotion to `fairy_circle` candidate.
- **Bug-fix thread (2026-06-12, second batch) — Moderate tier: #11/#12/#15/#16. Done, pushed (confirmed by Audra 2026-06-12).** Files: `dwyp_app.js`, `dwyp_ui.html`, `secretary_fairy.js`; syntax-validated (node --check; dwyp_app via reconstructed tail — sandbox mount byte-cap truncates large files, real file intact; recurring, see 2026-06-10 session note); `deleteContactRow` harness 5/5. **§1 Generic-completion gate amendment (#12, Audra-approved 2026-06-12)** — flowless steps locked out by the 2026-06-09 allow-list are now clearable everywhere: `GENERIC_COMPLETE_STEPS` += `Intake`, `Scheduling`, `Errors`, `Data_Integrity`, `Verify_Guest_Identity`, `Upload_Raw_Assets`, `Produce_Episode`; `taskAllowsGenericComplete` also passes blank-`Workflow_Step` fairy tasks (flowless by construction — no step key, no dedicated flow to bypass; covers error spawns + legacy rows). Dashboard `renderTaskButtons` adopts the shared gate (inline `completeSteps` list retired) and gains a dedicated `Revise_Reels` branch (`stCompleteReelRevision` → `completeReelRevision`) — generic Complete there used to bypass the Drive swap. **§2 New-interview notification (#15 resolved as new spawn)** — `runSecretaryForNewEvent` spawns completable `New Interview Scheduled: [guest] - [EEEE, MMM d at h:mm a]` after episode-record creation (assignee producer, `dueDate` = recording datetime, `Workflow_Step=Scheduling`, non-fatal, once per creation). Reschedule now spawns one task each to HOST and PRODUCER (Audra decision 2026-06-12 — both attend recordings; independently completable), copy carries time + `dueDate`. Old "New guest detected" copy confirmed dead (comment-only residue). **§3 Undo (#11)** — `showUndoToast(msg, onUndo)` (6s window; `.toast.show` now `pointer-events:auto`) + shared `undoTaskComplete(task, rerender)` (uncompleteTask → restore into `state.tasks` → re-render). Wired into all five generic-complete sites (`completeTask`, `stCompleteEpCardTask`, `bkCompleteTask`, `bkCompleteSticky`, `dbCompleteTask`); never on approve chains (side effects). `showToast` gained a shared timer (overlapping-toast fix). **Buckets Completed band:** always-rendered collapsed band at bottom of left column; expand lazy-fetches `getRecentCompletedTasks()` (new, `dwyp_app.js`: complete + `Completed_At` within 7d, SKIP_STEPS excluded, newest first; assignee filter client-side); per-row Undo restores in place. **§4 Contact delete (#16)** — `deleteContactRow(rowIndex, contactId)` (`dwyp_app.js`): blocked when any episode references the Contact_ID (AD #31 — flip Relationship_Type instead) or any open/in_progress task does; `_resolveContactRow_`-guarded; Drive folder untouched; bump + human_action audit. UI: Delete button in `ct-detail-foot` (confirm dialog, server-confirmed, never optimistic); sibling `_rowIndex` staleness after the row shift self-heals via the resolve guards. **Hand-steps (Audra):** clasp push (3 files; stacks with the 2026-06-11/12 first batch). **Hanging — untested in browser:** undo toast on all five surfaces; Completed band expand/undo; contact Delete (blocked + clean paths); first real calendar event spawns the New Interview task with correct local time. **Open (Audra decision pending):** #17 Contact optimization still undefined (Audra checking original thread). "Serious warnings → tasks" closed 2026-06-12: no warning-only paths promoted; enrich toast covers social-extraction failure.
- **#17 version-bump audit (2026-06-12) — done, pushed (confirmed by Audra 2026-06-12).** Defined: #17 "Contact optimization" = Performance Principle compliance (missing `bumpVersion` / non-optimistic writes). Static audit of all GAS mutators ran; findings fixed in `dwyp_app.js` + `fairy_circle.js`. **Root pattern: cross-domain mutations bumped only one domain — `social_assets` was systematically under-bumped** (only the two 2026-06-05 Schedule fns bumped it). Fixed: `deleteSocialAssetByAssetLibraryId` (+social_assets, in-helper so callers can't forget), `patchAssetLibraryRow` (+asset_library, in-helper; some callers double-bump — harmless, monotonic), `unscheduleAsset` (+social_assets), `unscheduleReel` (+social_assets when SA rows flipped), `placeAssetSchedule`/`removeAssetFromSchedule` (+asset_library on week-slot Availability flips), `clearReminderRows` (+tasks when cleared>0). `writeSocialAssetStatus` audited clean (misnamed — AL-only). Client optimistic half: checked against the 2026-06-10 inventory — known full-reload paths already converted (that spoke + this thread's Enrich partial update); `stUncompleteTask` full refetch accepted (rare path). **Dead-code deletion — DONE (Audra-approved 2026-06-12, Hub-as-spoke):** `submitRevisionRequest`, `addPostingSlot`, `runMarcomForEpisode` deleted from `dwyp_app.js` (incl. orphaned Marcom JSDoc above `getOwnerEmail`); `addPostingSlot` removed from Codebase Map Social/Assets row; syntax-validated (node --check via reconstructed tail — sandbox byte-cap again). Landed after the 2026-06-12 push — rides next push, stacked with #18 Contacts reflow. **Hand-steps (Audra):** `Revision_Requests` tab confirmed absent (2026-06-12) — function never ran. Only residue: delete `REVISION_REQUESTS_TAB_NAME` from Governance_Config if the key exists (likely absent for the same reason). **Open decision (Audra):** (2) `Episode_Log` has no version domain — `appendEpisodeLog`/`appendEpisodeLogEntry` can't bump; today's fetch-on-open reads make this benign; add an `episode_log` domain row only if a cached Episode_Log reader ever appears.
- **#18 Contacts desktop reflow — done (2026-06-12, Hub-as-spoke). Not pushed.** CSS-only (`dwyp_ui.html`): `.ct-feed` 560px column → responsive grid (`auto-fill minmax(320px,1fr)`, gap replaces card margins, max-width 1320px, `align-content:start`); `.ct-card.expanded` spans full row (`grid-column: 1/-1`, neighbors reflow below); `.ct-feed-note` spans all columns. Mobile <=700px untouched — grid collapses to one column = prior stack. No JS changes (`ctToggleExpand` already re-renders the feed). **Hanging — visual pass:** grid density at common widths; expanded-card detail fields stretch wide (~1300px) — if long value lines read poorly, fix is a one-line detail max-width or two-column field grid, Audra's call after seeing it. **Tier context (2026-06-12):** #19 logo stamp parked (Audra converting elements to SVG); #20 IntelliScript scoped in discussion (delete-only verbatim transcript edit for Resolve; needs prompt section + programmatic subtractive-verify guard; design decisions open: output location, trigger point, cut criteria); #21 Write surface parked; #23/#24 need a hub session (overlap with parked AL hygiene / Mending bucket); #22 definition not yet pasted.
- **Revision Conversation model — designed and LOCKED (2026-06-12, Hub thread). Build pending.** Full design at `docs/PROPOSAL_DWYP_Revision_Conversation.md` (STATUS: locked, awaiting scope). Core: seal ownership inverted — JT comments freely across sessions (one row per comment, existing Episode_Log), withdraw-X pre-finalize; **Finalize** = her commitment gesture, locks new comments (timestamp-anchoring rationale); task relay (first comment spawns awareness task → finalize consumes it + spawns revise task with upload link); per-comment resolution (Audra-only: resolved/declined + one-cell note); T-minus finalize deadline (`Release_Date` − `REVISION_FINALIZE_LEAD_DAYS`, 0 at launch, 7 preferred pending JT); replies/threading deferred to enhancement. `Video_Status` inventory ran (read-only): **keep column, retire lock semantics** — ~8 lock-mechanic client sites retired by build, ~10 display sites untouched, no pulse readers. Episode_Log review finding: col 8 `Resolved` exists dormant (written `false`, never flipped) — repurposed as status col, zero migration. **Hand-steps (Audra, before Spoke A):** Episode_Log headers `Resolved_At` (col 11) + `Resolution_Note` (col 12); `REVISION_FINALIZE_LEAD_DAYS` governance key; optional col-8 header rename to `Comment_Status`. Spokes A (schema+GAS) and B (surfaces) sequenced in the proposal doc.
- **Revision Conversation v1 — Spoke A (schema + GAS) — done (2026-06-12). NOT pushed (clasp reauth needed — see below).** `dwyp_app.js` only; `node --check` clean (full 260KB file, no truncation this run). All six scope items landed; cycle state lives in the manifest (zero new Episodes columns). **§1 Status writes** — `setEpisodeCommentStatus(episodeUid, rowIndex, expectedBody, status, note, authorEmail)`: one function for `resolved | declined | withdrawn`; row addressing via `_resolveEpisodeCommentRow_` (no Comment_ID exists — stored Body is the verify key, mirrors `_resolveTaskRow_`; verify at rowIndex → relocate by unique Body match → throw on missing/ambiguous, never writes a guessed row). Writes col 8 status + col 11 `Resolved_At` + col 12 `Resolution_Note` (declined only). Withdraw blocked when cycle finalized. Bumps `episodes`. **§2 Cycle state** — `_readRevisionCycleState(stagingFolderId)` reads `revision_cycle` + `revision_finalized` from manifest (lazy defaults `{cycle:1, finalized:false}`, fail-safe on any manifest error). Finalize writes `revision_finalized:true`; re-upload (`completeEpisodeRevision`) bumps `revision_cycle`+1 and clears the flag (fail-safe, never blocks the proxy returning to review). **§3 Task relay** — first comment of a cycle spawns `Revision_Awareness` producer task ("JT has begun revisions") via `_spawnRevisionAwarenessIfNone` (self-guarded, one open per cycle); `requestEpisodeRevisions` (re-semanticized seal→finalize) consumes it (auto-complete) + spawns/appends `Revise_Episode` "Revise episode: N items" (N = non-withdrawn comments in cycle) with upload deep-link; keeps closing `Review_Episode` + writing `Video_Status='revision_requested'`. **§4 T-minus** — `_computeRevisionTMinus(releaseDate)`: deadline = `Release_Date` − `REVISION_FINALIZE_LEAD_DAYS`; returns `{deadline, days}` (whole-day delta, can go negative past T-0) or `null` when no `Release_Date`. **§5 `getEpisodeRevisionHistory`** — now returns per-row `status`/`resolutionNote`/`resolvedAt`/`author`, filters out `withdrawn` tombstones, and adds `cycle`/`finalized`/`tMinus` to the payload; Entry_Type/Asset_Type filtering intact. **§6 Build check (Visible_To, col 9):** **dormant — no reader honors it.** Every reference is a writer (`appendEpisodeLogEntry` + the `fairy_circle` log wrapper, almost all hardcode `"both"`); no code path filters comments by `Visible_To`. Report-only per scope; left untouched. **Janitorial (Hub-accepted 2026-06-12):** writer-only column, all hardcoded `"both"` — retire or wire in a future cleanup; no action now. **`submitEpisodeCommentRow`** signature unchanged (mobile call site at `dwyp_ui.html:11954` passes a malformed 4-arg shape — pre-existing, Spoke B territory); round is now server-authoritative from the manifest cycle (client `revisionRound` ignored), and a finalized cycle returns `{ok:false, error:'finalized'}`. **Note — slight scope interpretation:** the re-upload cycle bump sits inside `completeEpisodeRevision` (a Video_Status writer, nominally out-of-scope); the Video_Status write itself is untouched — only a manifest patch was added, required for the loop to continue (finalized must reset or comments stay permanently blocked). **Hand-steps (Audra):** `clasp login` (token expired — `invalid_grant`), then `clasp push -f`. **Hanging — untested (needs push + live):** awareness spawn on first comment; finalize consumes it + spawns N-item revise task; post-finalize comment/withdraw blocks; re-upload opens next cycle (comments allowed again, Revision_Round increments); status writes land in cols 8/11/12 and the rail reflects them; T-minus surfaces (and is null with no Release_Date). Spoke B (surfaces — Revisions rail pane both chromes, lock-reader retirement, seal-render → cycle checklist, countdown) is next.
- **Revision Conversation v1 — Spoke B (surfaces) — done (2026-06-12). NOT pushed (hand to Audra).** `dwyp_ui.html` only; single inline `<script>` block, node `new Function` clean (GAS scriptlets neutralized for the check). Precondition (Spoke A pushed + relay exercised live) confirmed by Audra before build. **Shared renderer** — new `epRev` module: one `epRevBuildHtml`/`epRevRender` drives both chromes (Studio rail pane `#stEpRevisionsList`, mobile inline `#epRevisionRail`). Comments group by cycle (`revisionRound`); current cycle expanded + actionable, historical cycles collapse (`epRevToggleCycle`) and are read-only. The old round-card rail functions (`stEp`/`ep` `RailAppendRow` + `RenderRailFromHistory`) deleted. **Role-gated affordances** — resolve (gray + check) / decline (`window.prompt` reason → one-cell note) = `isOwner()`; withdraw X = own comment (`author === userEmail`) pre-finalize; Finalize ("Done — submit revisions", confirm) + T-minus countdown = `isJT()`. All status writes optimistic with visible rollback + toast; addressing = sentinel `rowIndex=0` + `row.body` verify key (server `_resolveEpisodeCommentRow_` relocates by unique Body match — no GAS change; history payload carries no sheet rowIndex). **Studio rail** — `list-checks` icon peer to Companion in `stRailIconsEpisode`; new `stEpRevisionsPane` sibling of `stEpBrowser`; `railPaneMeta.episode` gains a `revisions` pane; `railDefaults.episode='revisions'` (checklist auto-opens — it is the surface's primary work); added to `stHideAllPanels`. **Submit flow (item 6 + 4)** — malformed 4-arg Studio `submitEpisodeCommentRow` fixed to 5-arg `(uid, tsRaw, body, epRev.cycle, email)` (raw timecode; server brackets it; round server-authoritative). Both composers: comment optimistically appends to the rail set (`epRevAddOptimistic`), compose box clears, workspace never accumulates; failure removes the temp row; `res.finalized` blocks gracefully. **Lock-reader retirement (item 5)** — composer never locks pre-finalize; post-finalize the compose box blocks new notes from the manifest `finalized` flag (not `Video_Status`). `_stEpApplyComposerLock` re-signatured to `(uid)`, reads `epRev.finalized`/`epRev.videoStatus`; mobile `_epReviewFillComposer` mirrors it. Studio/mobile footers reduced to Approve only (Finalize moved into the checklist finalize bar). Seal rendering ("Sealed — Revisions requested") gone. **Video_Status read/write split (interpretation, per the locked inventory verdict):** retired the client *writes* (optimistic flips at finalize/approve/re-upload — `stEpApprove`, `approveEpisodeVideo`, `_onEpUploadComplete`); kept the ~10 display *reads* untouched. Freshness now comes from `_cacheInvalidate` + the next data load, not optimistic mutation — display readers (card labels, status dot, Buckets) may read briefly stale until refetch; accepted as the design's server-authority model. **Layout** — `#stEpVideoWrap` bumped `0 0 33%` → `1 1 auto`, `.st-ep-revisions` → `flex:0 0 auto` so the composer sits compact and the video takes the space freed by moving comments to the rail. **Deviation:** single push at spoke end (not the after-item-3 checkpoint) — coherent interdependent unit; Audra batch-tests. **Hand-steps (Audra):** `clasp push -f` (stacks with the unpushed Spoke A + prior 2026-06-12 batches). **Hanging — untested in browser (needs push + live):** full JT loop (first comment → awareness task; withdraw; Finalize → revise task + countdown), Audra resolve/decline + note round-trip lands in cols 8/11/12 and rail reflects it, post-finalize compose block, historical-cycle collapse toggle, T-minus rendering (and hidden when null), optimistic-rollback paths on server failure, `window.prompt` modal in the GAS iframe (confirm is used app-wide; prompt unverified here). Codebase Map: same-file (no cross-file responsibility move) — light refresh optional (episode review now = cycle checklist + shared `epRev` renderer; lock semantics retired). **Hub-accepted (2026-06-12) — all four interpretation calls ratified.** Two live watch-items: (a) **identical-text comments** — two open comments with the exact same Body throw on resolve/decline (server `_resolveEpisodeCommentRow_` refuses to guess — correct behavior); if it ever bites, fix is a timestamp tiebreaker in the addressing, not now. (b) **decline `prompt`** — same sandbox gate as `confirm` (works app-wide), so expected fine; glance in the browser pass. Independent of the sandbox answer, `prompt` is single-line — if decline reasons run longer than a phrase, swap to an inline reason field on UX grounds.
- **Transcript sniff-rename fallback — done (2026-06-12, Hub-as-spoke). Not pushed.** Root cause of the recurring Adam Meyer errors: Resolve exports transcripts as `.txt` into Raw without "transcript" in the filename; name-based detection misses them; `gatherVertContext` strikes out everywhere → urgent error task every pulse. Fix (`vert_fairy.js`): `_sniffRenameRawTranscript_` — fires only when `findTranscriptInFolder` misses on Raw; content-sniffs unlabeled `.txt`s by timecode density (`\d{1,2}:\d{2}:\d{2}`, threshold 10); exactly one qualifier → rename to `<name> Transcript.txt` + Audit INFO + same-run detection retry; multiple qualifiers → `_spawnTranscriptAmbiguityTaskIfNone_` (idempotent urgent task, `Workflow_Step=Errors`, never guesses); none → existing no-transcript error path unchanged (Claude does not write). Deliberately NOT inside `findTranscriptInFolder` (Herald consumes it — a read fn must not rename as a side effect); wired at the `gatherVertContext` Raw-fallback site only. Syntax-validated (reconstructed file — stale-mirror truncation again, now on vert_fairy.js at 55KB: not a byte cap, partial mirror; workaround standing). **Surfaced, decision parked (Audra):** Stage 1 `_pulse_detectTranscript` scans `Staging/Episode/` only — never Raw. An `upcoming` episode whose only transcript sits in Raw never auto-flips to `in_production`, labeled or not. Should detection also scan Raw? Changes pipeline trigger semantics — hub decision before wiring. **Hanging — live validation:** Adam Meyer is the test case — next pulse after push should sniff-rename his Raw `.txt`, run the chain clean, and stop the recurring error tasks (close the stale ones by hand after).
- **Episode playback jitter — CLOSED (2026-06-12): encode profile confirmed as root cause.** 720p two-pass 2–5 Mbps Premiere re-export plays clean on the same GCS pipe that served the jittery ~17 Mbps master-grade export. Streaming layer was never the problem. **Open follow-up:** a Resolve Deliver recipe for web-friendly proxies (720p H.264 8-bit 4:2:0 + AAC, network-optimized) so episodes don't need a Premiere round-trip every cycle — Audra testing; original diagnosis bullet follows. Derek Peterson proxy (~10GB / 1:16:34 ≈ 17 Mbps): browser-pass findings — GCS streaming layer healthy (arbitrary 40-min seek resumed near-instantly → moov placement fine, range serving fine; CORS/permissions acquitted — both fail binary, not jittery), audio clean + video judder = **decode-side**: encode profile outside Chrome's hardware-decode comfort (master-grade export suspected: high bitrate, possibly 4:2:2/10-bit). Audra re-exporting from Premiere: 720p two-pass VBR 2–5 Mbps (8-bit 4:2:0 progressive ≈ Chrome-native). Test logic: new proxy clean → closed as encode root cause; still jitters → investigate viewing-machine GPU/driver. Same cut → JT's timestamp-anchored comments stay valid. ffprobe available in sandbox — old file can be probed from `C:\Projects\DWYP` to document root cause. Tooling note: Chrome-extension network reader does not capture `<video>` media requests; seek-behavior observation is the working substitute.
- **Studio "Review Episode" button — broken in Studio context (found 2026-06-12 browser pass, confirmed by source).** Kind 2b (`stRenderEpCardTaskList`) calls `showEpisodeReview`, which flips the dashboard/mobile app-shell views (`hideAllViews` + `episodeReviewView`) — underneath the Studio overlay. In Studio: click → server calls fire → no visible change. Mobile/dashboard contexts unaffected (their path is correct). From the 2026-06-09 task-button spoke — behavior matrix was verified statically, never in browser. Workaround: left rail guest → Episode. Fix options (small spoke): Studio Kind 2b routes to the Studio episode surface (guest select + `stSetDesignTab('episode')`), or `showEpisodeReview` closes the Studio overlay first. Bucket-band and dashboard call sites must keep current behavior. Could not click-retest on /dev (no open `Review_Episode` task renders the button anywhere right now).
- **Review/Revise rebuild — SPOKE_A/B/C — built + pushed to `/dev` (2026-06-13/14), branch `review-revise`. VERIFICATION IN PROGRESS — closeout BLOCKED.** Reconciliation rebuild on top of Revision Conversation v1: a single derived-phase source of truth, the final-upload deep-link, the composer-lock live re-fetch, mobile-review retirement, and `Video_Status` logical retirement. Scope + checkpoints in `docs/SPOKES_Review_Revise_A_B_C.md`; mid-verification detail in `docs/HANDOFF_Verification_InProgress.md` (both live until closeout). Locked model (code-reconciled): turn = whichever episode task is open (Review_Episode → Revise_Episode → Upload_Final_Episode → ready_to_release); weld = manifest `revision_cycle` + per-comment `Revision_Round`; `Video_Status` retired LOGICALLY only (column physically kept); composer-lock bug = stale-read/invalidation, fixed by a live re-fetch trigger, not the rewrite. **`main` holds a clean restore point. A/B/C is clasp-pushed to `/dev` but still UNCOMMITTED in git — it lives in the working tree on `review-revise` (HEAD = the restore point); the completing commit lands at closeout.**
  - **SPOKE_A (GAS, `dwyp_app.js`) — built.** (1) Derived-phase SSOT: `_deriveEpisodePhase(episodeUid)` (~L2937) + pure resolver `_phaseFrom(status, openSteps)` (~L2975) — phase ∈ {review, revise, approved, released, production}, derived from open tasks + Episodes `Status`, never `Video_Status`. Exposed additively: `phase` on each `getEpisodes` row (batch `_phaseFrom`, ~L423) and on `getEpisodeRevisionHistory` (~L3067). (2) Final-upload deep-link: `approveEpisodeForRelease` (~L670) sets the spawned `Upload_Final_Episode` `payloadLink` to the Staging `Episode/` child-folder URL (resolve by name; WARN + fallback to Staging-root URL on miss, ~L712–742); spawn is idempotent (skips if an open `Upload_Final_Episode` row exists). (3) Approve → Upload_Final → `completeFinalEpisodeUpload` lifecycle present; not yet walked end-to-end on `/dev` (blocked by bug #1).
  - **SPOKE_B (surfaces, `dwyp_ui.html`) — built.** (1) Composer-lock live re-fetch: `_stEpRevPoll`/`_stEpStartRevPoll`/`_stEpStopRevPoll` (~L10908) re-run the entry-time `episodeRev` staleness check (gated on `episodes`+`tasks`) + `getEpisodeRevisionHistory` refetch while the Studio review surface is active — `visibilitychange`→visible plus a 25s interval; started at the tail of `stLoadDesignEpisode`, torn down in `stSetDesignTab` + `closeStudio` + a self-guard, with mid-flight uid checks. Reuses the existing cache/version path — no new fetch. (2) Mobile review retired (Surface Principle): `showEpisodeReview` (~L13482) now branches on `ctIsMobileVp()` — **desktop** opens the Studio episode review via `openStudio('design')` + `stSelectGuestView(uid, name, 'episode')` (the rail's own path); **mobile** renders a hard "Open on desktop to review" wall. The mobile renderer + composer cluster was deleted (`renderEpisodeReview`, `_epReviewFillComposer`, `_epReviewApplyUploadMarker`, `epComposerFocus`, `epComposerCancel`, `epComposerSend`, `approveEpisodeVideo`); shared `epRevInit`/`submitEpisodeCommentRow`/`approveEpisodeForRelease`/`epRev*` renderers untouched. **This resolves the Studio "Review Episode" button bug above** (Kind 2b dead-ended under the overlay). *(First cut over-walled ALL callers — desktop affordances dead-ended at the wall; fixed same-thread with the `ctIsMobileVp()` branch.)*
  - **SPOKE_C (`Video_Status` logical retirement) — built; NOT verified (keystone bug #1 below).** All readers repointed to A's `phase`; `videoStatus` removed from `getEpisodeRevisionHistory`/`getEpisodes` payloads; `writeVideoStatus` + every call site deleted (grep-clean); client reads zero (one explanatory comment remains, `dwyp_ui.html:11295`). **Physical column kept inert** — `EPISODES_COLS.Video_Status = 11` (`dwyp_app.js:194`) frozen at last value, no constant touched; physical delete is a future SPOKE_D (positional-read landmine, per CLAUDE.md). Invalidation guard (composer-lock bug class): `episodeRev` staleness now gates on `episodes` AND `tasks` (`dwyp_ui.html:10858`/`10877`) so task transitions flip the turn without the incidental `writeVideoStatus` bump that previously carried it; B's poll is the backstop. `_deriveEpisodePhase` regression WARN repointed off the frozen column → compares derived `phase==='production'` against `Status==='review'` (`dwyp_app.js:2961`).
  - **OPEN BUGS (block closeout):** **#1 (critical) — composer stays locked after re-upload.** After Audra re-uploads a revised cut from the Revise task, JT's turn flips (Review_Episode respawns) but his composer stays LOCKED on the new cut — the composer-lock bug class recurring. Lock comes from `_stEpApplyComposerLock` when `epRev.phase==='approved'` OR `epRev.finalized`; phase is `review` post-re-upload, so the suspect is `revision_finalized` not cleared on the Revise-task upload path (`completeEpisodeRevision` clears it inside a fail-safe try/catch; `getEpisodeRevisionHistory` reads `finalized` straight from the manifest). Diagnose root cause first (is manifest `revision_finalized` actually false? which upload path was used? did the `episodes`+`tasks` bumps fire?) before fixing — **C is not verified until JT unlocks on the new cut.** **#2 — role-gating leak:** JT (host) sees "Replace proxy" (a producer action); role-gate it via the `isOwner()` pattern. **#3 — desktop wall over-walling: FIXED this session** (the `ctIsMobileVp()` branch in B above); re-verify the desktop task→episode path on `/dev`.
  - **Hanging — closeout (do NOT run until the loop verifies on `/dev`):** walk the Keanu Reeves throwaway test episode review → comment → submit → revise → re-upload → **JT unlocks** → repeat → Approve → `Upload_Final_Episode` spawns with the `Episode/` deep-link → ready_to_release. Only then: the completing commit deletes `docs/SPOKES_Review_Revise_A_B_C.md`, `docs/PROPOSAL_DWYP_Revision_Conversation.md`, and `docs/HANDOFF_Verification_InProgress.md`; **keep** `docs/HANDOFF_PostBuild_ReturnToReckoning.md` (next session's warm-start → reckoning D2–D15). *(The `HANDOFF_Review_Revise_Model_and_Verification.md` and `PROPOSAL_Reels_GCS_Playback.md` named in the spoke/handoff closeout lists are already absent from `docs/` — no action.)* Testing-found extras to fix/track: lock contention, stuck upload spinner (completes server-side, UI hangs on post-success refresh).
- **Episode surface column swap — done (2026-06-12). Not pushed.** Show notes now left, video+compose right (`dwyp_ui.html`): with comments living in the Revisions rail pane (Spoke B), the review loop (watch/compose/checklist) clusters on the right edge; show notes get their own zone. DOM order swap + `.st-ep-right` border flipped + vdiv drag math mirrored (notes width now measured from left edge). IDs deliberately keep historical names (`stEpLeft` = video panel, now rendered right) — rename deferred to the parked redesign pass; comment at the markup explains. **Hanging:** visual check on /dev after push — divider drag direction, notes min-width clamp, video panel at right edge beside rail.
- **isOwner()/isJT() gate — ROOT CAUSE FOUND AND FIXED, verified live (2026-06-12).** Symptom: Revisions pane showed withdraw × only — no resolve ✓ / decline ✗ for Audra. Three-layer diagnosis: (1) `isOwner()` compared registry email vs `Session.getEffectiveUser()` — repointed to governance `ASSIGNEE_PRODUCER` (`producerEmail` in `getAppConfig` + `APP_CONFIG`), symmetric with `isJT()`. Still failed. (2) Sheet values matched, logout/login no change. (3) Gate instrumentation (temp pane footer line) read `u23 p25` — **the `<?= JSON.stringify(v) ?>` template idiom double-encodes**: GAS contextual escaping already produces a JS string, so stringify's quotes land INSIDE the value (every APP_CONFIG string built this way carries literal leading/trailing `"`). **Latent consequence: `isJT()` has been false for JT always** — her task security filter never engaged and the Finalize button would never have rendered; Gems/NotebookLM quick-link URLs likewise quote-wrapped. Fix: startup normalizer strips wrapping quotes from all 8 template string values (`dwyp_ui.html`, after `APP_CONFIG`). Verified live: owner=true, ✓/✗/note all functional; `window.prompt` works in the GAS iframe (Spoke B flag closed). Diagnostics removed after verification. **Behavior changes (corrections): JT's task filter engages for the first time; her Finalize button now renders.** `ownerEmail` config value: zero consumers — janitorial. **Session hazard (canon candidate): scriptlet delimiters evaluate ANYWHERE in `dwyp_ui.html` including JS comments** — writing the idiom inside a comment served a blank page (`ReferenceError: v is not defined`); never write `<?` sequences in template-file comments. **Hanging:** JT-session verification (filter now only shows her tasks; Finalize renders + works); resolve/decline round-trip on a real comment (status write + gray flip + rollback path untested beyond render). (audit run 2026-06-09; 3 voice keys + 7 Scribe keys deleted). No blocker, but flag before next feature that adds a governance key.
- **Revision pane — live-test fix round (2026-06-12 evening, found during Audra's /dev exercise). Not pushed.** (1) **Duplicate-body ambiguity fixed:** Spoke B sent sentinel `rowIndex=0`, making body text the sole address — two comments with identical text made resolve/decline throw ("Comment text is ambiguous"). `getEpisodeRevisionHistory` rows now carry `rowIndex` (sheet row); all four client write paths pass it; body demoted to verify key (the `_resolveTaskRow_` pattern as Spoke A designed). (2) **Owner finalize:** `epRevFinalizeBarHtml` gates on `isJT() || isOwner()` — Audra can close the set on JT's behalf (unblocks re-upload when JT unavailable); T-minus countdown now visible to both. (3) **Comment edit (pencil):** new `editEpisodeCommentBody` (`dwyp_app.js`) — author-only, open-status-only, finalize-blocked, row-addressed; client `epRevEdit` via prompt prefilled with current body, optimistic + rollback, ✎ beside withdraw × on own comments. Known quirk: prompt exposes the `[MM:SS]` prefix — editable, harmless. **Hanging:** push + retest resolve/decline on the duplicate-text comments; JT's Finalize still unverified (check Gov `HOST_EMAIL` vs her registry `User_ID` — same vocabulary-mismatch class as the producer gate).
- **Comment status writes — THIRD root cause found and fixed (2026-06-12 evening): Episode_Log col H had strict boolean data validation.** Symptom: resolve/decline reported ok:true + audit lines, UI gray flip held, but col 8 stayed FALSE — statuses reverted on next fetch. Mechanism: GAS batches writes and validation fires at FLUSH; `setEpisodeCommentStatus` never flushes, so setValue('resolved') passed, audit logged, ok:true returned, and the write was discarded post-return ("Enter TRUE or FALSE" surfaced only under explicit `SpreadsheetApp.flush()` in a dev_tools probe). **Fixed by hand (Audra):** validation rule removed from col H; `Resolved_At` (11) + `Resolution_Note` (12) headers added (had been missing). Full checkmark saga = three stacked causes: Session-vs-registry gate → template double-encoding → boolean validation. **Lesson (canon-adjacent): repurposing a boolean column requires checking its data validation, and ok:true from GAS does not prove a write survived flush — flush-then-readback in tests.** HOST_EMAIL/registry confirmed matching — JT's Finalize needs only a fresh page load. **VALIDATED same night: full Revision Conversation loop ran end-to-end live** (awareness spawn 19:04 → owner finalize 19:41, 3 items → revise task with upload link → re-upload 19:43 → cycle reset → fresh Review_Episode 19:44). Remaining retest: resolve/decline persistence post-validation-removal; JT session pass.
- **PIN-login-inert recurring bug** — symptom: PIN boxes don't auto-advance, Continue never enables (whole front-end dead). Cause is never the PIN code — it's a JS parse error in the main script block, usually smart/curly quotes in a hand-authored `innerHTML` string from a companion/chat build. Fix at the bad quote, not the PIN handler. Diagnostic: `node -e "new Function(scriptBlockContent)"` per `<script>` block surfaces the dead block fast.
- **Index Audit step — designed, parked.** Inserts between Track A and Track B. Claude builds index from injected transcript → Gemini audits index against Vertex-retrieved corpus chunks (filtered to epUid) → appends `## Audit Findings` section to index doc with severity + location anchor + citation + suggested correction → spawns `Review_Index_Audit` task → HITL gate → next pulse: Claude produces clean revised index, re-reads transcript only if any finding marked CRITICAL → Tracks B/C run against revised index. Architecture role split: Claude generates (synthesis, nuance), Gemini audits (literal, source-checking), Vertex retrieves (corpus-grounded independence from Claude's injected source). Design doc: `DWYP_Index_Audit_Design.md`. **Parked:** corpus confirmed name-keyed, not epUid-keyed (verified 2026-05-19); retrieval workaround: "[guest name]'s episode" queries yield good results; episode-scoped indexing requires corpus re-tag/re-import before audit spoke opens. **Lift shift (May 2026):** `_vertexMarkerQuery_` (the Vertex retrieval helper the audit design planned to reuse) was deleted in the pipeline rewire spoke. Audit spoke must write a new epUid-filtered Vertex retrieval function — not a reuse. Function recoverable from git.
- **Derivative Edit Surface — Spokes A/B/C (2026-06-05, pushed, untested).** `edit ↗` from Schedule enters derivative mode (`st.derivativeMode = true`). Action bar: creation verbs (Schedule / Clear) replaced by exit triad (Save / Save a Copy / Discard). Images: H&Q rail tab hidden; Backgrounds + AI Chat remain; canvas non-attribution text extracted via `imgChatGetCanvasText()` → `Quote_Text`; Save commits same AL row (`Quote_Text` + `Canvas_State` + `Caption_Host`), bumpVersion; Save a Copy appends new unplaced `Status='schedule'` AL row sharing `Episode_UID`/`Asset_Type`/`Display_Name`/`Canvas_State`; Discard client-only. Reels: rail locked to one reel under edit (`stRenderRailReelList` filters to `st.selectedReelId` in derivative mode); Save writes `Quote_Text` (title card) + `Caption_Host` (no `Canvas_State`); Reel Save a Copy shares `Drive_File_ID` (same clip, new AL row with edited title/caption — TikTok vs IG use case); Discard client-only. GAS: `saveDerivativeAsset(assetId, action, canvasJson, captionText, quoteText)` — single endpoint, `action='save'|'save_copy'`, AL-only (no Social_Assets touch), normalizes `Quote_Text` via `normalizeQuoteText`, bumpVersion + Audit_Trail on both paths. Export: `_uniqueFilename(folder, baseName, ext)` helper prevents collision when two AL rows share the same title slug; suffix scheme `-2`, `-3`; applied to `_writeAssetToExportFolder` + `exportSingleScheduleAsset` (paired `.txt` sidecar tracks same suffix). `stSyncDerivativeUI()` reads `st.derivativeMode` + `st.designTab` and shows/hides all action buttons; called at end of `stSelectDesignTab`. **Untested — needs placed week (Images Save/Copy) + reel Save-a-Copy validation across both export paths.**
- **Companion spine + Episode companion — Pass 1 of 4 (2026-06-03).** Reusable companion architecture built and wired to Episode surface. **GAS (`dwyp_app.js`):** `companionChat(surface, episodeUid, history, userMessage, workspaceState)` — single entry point for all four surfaces; `_companionBuildSystem` assembles system instruction from `extractPrompt("# Companion Voice")` + `# Show Philosophy` + `# Pillars` + full episode transcript (via `stLoadEpisodeIndex`; transcript injected into system instruction each call, history stays clean); `_companionBuildUserPrompt` prepends show-notes text as per-turn workspace context with anti-self-eating guard. Reels/Schedule are explicit stubs (`// Pass 3/4`). Voice comes from template — `CLAUDE_STUDIO_SYSTEM` / `STUDIO_MODE_INSTRUCTIONS` untouched (zero-caller, janitorial pass after all four companions built). **Client (`dwyp_ui.html`):** `epChat` state (history, loading, uid); `#stEpChatPane` wired with scrollable history, textarea (Enter=send, Shift+Enter=newline), Send button; `epChatReset` called on every episode switch; `epChatMarkdown` inline renderer; workspace state = `#stEpShowNotesBox.innerText`. **Master Template:** `# Companion Voice` section added (Audra, 2026-06-03). **Workspace-state contract locked** (fits all four surfaces without spine changes): `{ showNotesText }` → Episode; `{ activeAssetText }` → Images; `{ titleCardText, captionText }` → Reels; `{ placedAssets }` → Schedule. **Episode companion validated.**
- **Images companion — Pass 2 of 4 (2026-06-04).** `images` case filled in `_companionBuildSystem` and `_companionBuildUserPrompt` — no spine changes. System instruction: full transcript (verbatim-quote discipline), do-not-repeat pool from `getEpisodeHooksAndQuotes` (exclusion list, server-side pull), canvas-awareness + iteration-on-variation instruction. Per-turn workspace: `activeAssetText` from canvas `_isText` objects (sent on every send). Client: `#stImgChatPane` pane wired; `stSetRailTab` extended to handle `'chat'`; `imgChat` state (history, loading, uid); `imgChatReset` called on episode switch; reuses `ep-chat-*` CSS. History is per-episode (not per-asset). **Mostly validated. Attribution wrinkle:** companion may include attribution line in quote output — requires human judgment on whether to use. No code fix; (resolved 2026-06-05, see Companion Voice sweep).
- **Reels companion — Pass 3 of 4 (2026-06-05).** `reels` case filled in `_companionBuildSystem` and `_companionBuildUserPrompt` — no spine changes. System instruction: full transcript (verbatim-quote discipline), refine-first posture (default to sharpening the title card and caption JT already has; produce net-new options only on explicit request), stay-on-surface guard. Per-turn workspace: `{ titleCardText, captionText }` from `stTitleCardField` + `stCaptionField` DOM nodes (sent on every send; anti-self-eating guard). Client: `stReelBrowser` restructured to tab-bar model (Reels | AI Chat) via `stSetReelRailTab`; `#stReelChatPane` created; `reelChat` state (history, loading, uid); `reelChatReset` called on episode switch; reuses `ep-chat-*` CSS. History is per-episode (not per-reel). **Pending validation.**
- **Schedule companion — Pass 4 of 4 (2026-06-05).** `schedule` case filled in `_companionBuildSystem` and `_companionBuildUserPrompt` — no spine changes. **THE FIREWALL:** `stLoadEpisodeIndex` is deliberately NOT called in the schedule case — companion has no transcript, forcing audience-shaped judgment. System instruction: social-media strategist role, firewall stated in prose (belt-and-suspenders), advise-first posture (assess the week JT has, no unprompted strategy), stay-on-surface guard (arrangement only; copy work redirected to Images/Reels), slot-intent literacy (template `why` as slot goal; ad-hoc slots respected from craft), source-awareness (cite trends cache if present, else flag as general). Per-turn workspace: `{ placedAssets, unplacedPool }` assembled client-side by `schedChatBuildWorkspace()` from live `sch.placements` × `sch.candidates` × `sch.days` — no new GAS round-trip. Each placed item carries: slot identity (day/platform/slotId), slot intent (`why`) or `adHoc:true`, asset type, surface text (captionHost / quoteText / reelSummary per type), placement status (week vs. swipe). Unplaced pool items also included. Anti-self-eating: workspace is live week state only. Client: `stSchedulePool` restructured to tab-bar model (Candidates | AI Chat) via `schSetRailTab`; `#schedChatPane` created; `schedChat` state (history, loading, uid); `schedChatReset` called on episode switch; reuses `ep-chat-*` CSS. History is per-episode. **Pending validation (needs placed week).**
- **Companion Recon + Teardown (2026-06-03).** All Publish-era and orphaned companion/chat plumbing removed from `dwyp_app.js` and `dwyp_ui.html`. **GAS deleted:** `generateWithClaude`, `isImageRequest`, `isExplicitTextRequest`, `callPublishLLM`, `generateReelTitleCard`, `getOrGenerateReelSummary`, `ensureReelSummaries`, `assembleSlotForegroundContext`. **Kept for build:** `stLoadEpisodeIndex` (full-transcript loader, now wired into companion spine); `CLAUDE_STUDIO_SYSTEM` + `STUDIO_MODE_INSTRUCTIONS` (zero-caller — janitorial pass after all four companions built). **HTML shells:** Images AI Chat button gutted (`onclick` removed), ID preserved as build target (`#stRailTabChatImg`). Reels AI Chat fully wired (Pass 3). Schedule AI Chat fully wired (Pass 4). **CSS removed:** `.sv-*` Social Vert block, `.st-chat-col`, `.st-chat-brand-*`, `.st-chat-hist`, `.st-chat-header*`, `.st-chat-input*`, `.st-reels-fields`. **Asset_Library reel columns (live, verified against production sheet 2026-06-09): col 8 = `Reel_Transcript` (verbatim), col 9 = `Reel_Summary` (scannable gloss).** `ASSET_LIBRARY_COLS.Reel_Summary = 9`. No `Reel_Summary_Clean` column exists — earlier rename note was never the live state; do not reintroduce that name.
- **Pulse content chain wiring — verified (2026-06-03).** `buildEpisodeIndexV2` confirmed reachable via `in_production` branch of `dailyPulse()` → `_pulse_contentChain` (`fairy_circle.js:2011 → 2131`). Idempotency gate: two-layer — manifest field check in chain (`!manifest.episode_index_v2`, `fairy_circle.js:2124`) + Drive file-presence verification inside `buildEpisodeIndexV2` (`vert_fairy.js:327-351`; self-repairs dangling pointer). `PULSE_CONTENT_ENABLED` now `TRUE` — chain live (daily runs since 2026-06-06). Throttle: `PULSE_HEAVY_PASS_BUDGET` = 3 (set 2026-06-09; temporary — Audra intends to raise once the pipeline runs smooth). **Template precondition before bringing Track A online:** redesigned `# AI Search Index` section must be pasted into Master Template before `PULSE_CONTENT_ENABLED → TRUE`.
- **Track A "extract-not-interpret" posture — code-level implementation shipped (May 2026).** `buildEpisodeIndexPrompt` enforces: speakers' interpretations belong in the index; auditor interpretations do not; no attribution of intent, motivation, or causation beyond speaker's own words. Synthesis happens downstream in B/C. Sections: EPISODE SUMMARY / GUEST PROFILE / KEY THEMES / CAPTION SEEDS / TRANSCRIPT MAP / REEL DESCRIPTIONS.
- **Track A/B/C Pipeline Rewire — complete (May 2026).** `buildEpisodeIndexV2` now Claude-based: reads injected raw transcript directly, no Vertex RAG. `runVertFairy` retired. Loop D rewired to two-condition logic: `episode_index_v2` absent → Track A (`buildEpisodeIndexV2`); `episode_index_v2` set + `show_notes` absent → Track B (`runEditorialPass`). At most one pipeline step per pulse run. `runVertFairyForEpisode` in `dwyp_app.gs` repointed to `buildEpisodeIndexV2` (Fairy Remote Control button remains live).
- **Design Surface fixes (May 2026):** Right rail — `#stBgPane` CSS override (`flex-direction:column; padding:0; overflow:hidden`) so Upload/Generate stack at top and bg pool scrolls below. Attribution canvas — `stDropText` rewritten to place quote and attribution as two independent Fabric.js `Textbox` objects (`_textType:'quote'` at `ch/2 - 40`, `originY:'bottom'`; `_textType:'attribution'` at `ch/2 + 40`, `originY:'top'`). Attribution box removed if `isHook` or no `st.guestName`.
- **dev_tools.js cleanup (May 2026):** `ACTIVE_EP_UID` constant at top — single paste point for all test wrappers. Vestigial functions removed: Phase 1.2–1.3 full test suite (8 functions), all `setup_*` / `migrate_*` one-time functions, diagnostic + smoke tests. Track A comment corrected to reflect Claude intent. (`test_runReelEditorialPass` and its `_force` variant are both gone — Reel Editorial pass retired 2026-06-06.)
- **Companion Voice template sweep — complete (2026-06-05).** Three `# Companion Voice` additions authored (voice thread) and pasted into Master Template, replacing the prior section wholesale. (1) Surface-ownership clause in the firewall: companion produces copy its own surface owns; redirects copy belonging to another surface by packaging a clickable instruction that round-trips to the owning surface (not a draft — diagnosis crosses, copy doesn't). (2) Misattribution guard: a line that might be someone else's words renders as an unattributed hook, not an attributed quote; phrased as posture-toward-doubt so it holds for both transcript-grounded (Episode/Images/Reels) and transcript-firewalled (Schedule) companions. (3) Schedule Strategist register added (fourth register): transcript-firewalled by design (judges standalone audience resonance), authority order performance-data > trends-cache > general-craft, companion names its source. Authority order reads correctly with performance-data absent today (degrades to trends/craft) and absorbs analytics when wired.
- **Revision loop — operating expectations LOCKED (Audra's 17 points, 2026-06-12 night).** Coherence-checked against build: 14/17 already live and validated end-to-end same night. Policy points (no machinery): JT's critical post-finalize need → generic user task (existing); approval is explicitly JT's; at countdown danger zone Audra may override using her owner Finalize/Approve buttons (both exist). Deltas: red danger zone now `days <= 1` (shipped, not pushed); `REVISION_FINALIZE_LEAD_DAYS` → 5 (Audra hand-edit pending); **Replace-Proxy verb is the sole structural gap and next spoke — uploads must distinguish "revision cut" (closes cycle, reopens comments) from "replacement" (review state untouched); the 19:43 encode-fix upload wrongly consumed cycle 1, third bite of this gap in one day.** Plus pane state-line clarity ("Cycle N — X items, Y addressed. Waiting for new cut.") folds into the Replace-Proxy spoke; cold-load limbo (4-5s fictional state) is the pending caching/skeleton spoke.
- **Parked for hub (added 2026-06-12):** (a) **Replace Proxy shortcut** — PROMOTED to next spoke, see operating-expectations bullet above. (b) **Stage 1 transcript detection never scans Raw** (see sniff-rename bullet) — should it? Changes pipeline trigger semantics. ((c) T-minus visibility — resolved 2026-06-12: Finalize button + countdown now render for owner as well as JT; producer may close the set on JT's behalf, e.g. when uploading a new version. `epRevFinalizeBarHtml` gates on `isJT() || isOwner()`.)
- **Parked for hub:** Studio visual redesign pass (Audra finds current look dated; direction: less novel, more modern — off-the-shelf patterns over bespoke; known friction: episode surface vdiv split, rail chrome, card styling; warm-start prompt written 2026-06-09, run as its own thread, no code until direction locked); Reference AD numbering hygiene (#124 appears twice with different content — per-asset icon model vs. mend_ledger schema — #125 twice, #126 out of order; retrieval-by-AD-number ambiguous for those; renumber or alias in one pass); JT text-only send path serializes bad text coords (see white-canvas bullet); Tasks Help Desk (Gemini-based read-only chat on the Tasks workspace: how-to + system + research questions; design settled 2026-06-09 — purpose-written `DWYP_Help_KB` Drive doc as knowledge source (ID via Governance_Config), live tasks/episodes state + Audit_Trail fetcher for system questions ("Phase 4" note in `dwyp_ui.html` loader), Gemini web grounding for research, `callGeminiChat_` wrapper + `helpDeskChat()` endpoint, rail pane reusing companion UI pattern; one spoke, three checkpoints; hand-step: Audra writes KB doc; "coming soon" note shipped in Buckets + Episodes empty states); Stub → real card swap on first paint (skeleton-first, closes orphaned-Card-1 race); AL row as single source of truth across surfaces (audit-first spoke before patch); Gemini-as-fallback for all Claude writing paths (generalize existing `STUDIO_LLM_MODE = claude` fallback pattern into `callClaudeAPI()` so every writer — Tracks A/B/C, audit revision, reel editorial, captions, chat — falls back to Gemini on Claude API failure, logged to Audit_Trail; single point of fallback logic; open design question); consolidate/retire stale Brand Identity Drive files after v3.0 lands — fix `DWYP_BrandVoice_v1.md` cross-ref in Operating Model §12 (and retire Caption Supplement reference) once v3.0 supplies in-template `# Forbidden Phrases` / `# Substitutions`; Arrange hold-an-open-slot (`saveArrangeOrder` writes consecutive Tuesdays by position — no way to express a gap week or Roundtable placeholder); AL scheduled hygiene + Banked-clip migration to its own home (hygiene half is a natural future Mending Fairy op; Banked home needs design first); episode archive mechanism (released episodes sit at `ready_to_release` indefinitely — Mai Vo released 6/1, status never advances).

**Retired surfaces:** Frame.io, Safety Fairy, Marcom Fairy, Scribe Fairy, Social Vert, Image Workshop, Quick Caption (standalone), Librarian Vert/Social Vert personas, Publish tab.

**Scribe template keys deleted (2026-06-09).** All seven `SCRIBE_*` template keys removed from Governance_Config (they were dead/blank). The earlier "migrate to Writer Email quick-starts" plan is **superseded** — pipeline comms will be handled differently (new approach TBD). Docs still carrying the migrate-to-Writer narrative (App_Structure, Build_Playbook, Reference AD #111/#30, User_Flows) are stale on this point pending the new approach.

### App
- **Web app URL (production):** https://script.google.com/macros/s/AKfycbzCed5Fmv9TNDf6ivQUcmhgUWWOyEVK4P3sxS8_KMQx7YOY6JeY7r-dh8jEw5DpecrI/exec
- **JT's URL:** Same URL + `?who=jt`
- **Staging URL (`/dev`):** https://script.google.com/a/macros/wiseonewithin.com/s/AKfycbwHRxyQ22Zi0TFwT3av5jf30MiPhxBtV9tjb4hMxm0/dev
- **Deployment model:** Execute as Me (Audra). USER_ACCESSING rejected — locked, do not revisit.
- **Identity:** PIN-based. PIN screen appears once on first load; identity persists until sign-out.

### Master Sheet
- **Production URL:** https://docs.google.com/spreadsheets/d/1p5ahHe4hgG6sHN4u13UyvEJWg5IwCkAfADjeqxwlTnw/edit
- **Staging Sheet ID:** `13bXMjxEf_L-BFH69OtUGOU6ywxt6BTat1kO9ik46Swk`
- **Tabs (code-read):** Contacts, Tasks, Episodes, Episode_Log, Asset_Library, Social_Assets, Audit_Trail, Versions, Governance_Config, Posting_Schedule, User_Registry.
- **Tabs (manual-only, NOT read by code):** `Reference` (old episode-UID mapping, JT's), `Social_Posts` (JT's manual content calendar). Present in the sheet; no GAS reader. Don't assume the pipeline consumes them.

---

## Staging Environment — Locked Architecture

The platform runs on a two-deployment model. One script project, two master sheets, deployment-aware routing.

**Operational change (May 2026 hub):** Staging-first cadence retired. Code pushes directly to production going forward. Staging sheet (`13bXMjxEf…`) remains available but is no longer maintained in sync. Routing helpers and architecture remain in place under the Code Integrity Mandate (see `CLAUDE.md`).

**Deployment workflow (June 2026):** GAS (clasp push) and GitHub are deployed simultaneously. There is no longer a separate "clasp re-auth pending" step — they go together.

### Deployments

| | URL | Sheet |
|---|---|---|
| **Staging** (`/dev`) | `/a/macros/wiseonewithin.com/s/.../dev` | `13bXMjxEf_L-BFH69OtUGOU6ywxt6BTat1kO9ik46Swk` |
| **Production** (`/exec`) | `/macros/s/.../exec` | `1p5ahHe4hgG6sHN4u13UyvEJWg5IwCkAfADjeqxwlTnw` |

Staging always serves the latest pushed code. Production serves only the version explicitly deployed via Manage Deployments → pencil → New version.

### Routing Helpers (in `fairy_circle.js`)

- `isStaging()` — compares `ScriptApp.getService().getUrl()` to `STAGING_DEPLOYMENT_URL` in Governance_Config. Exact-string match. Fails closed to production on any error.
- `getMasterSheetId()` — returns `STAGING_SHEET_ID` from Governance_Config when `isStaging()` is true; returns `MASTER_SHEET_ID` from Script Properties otherwise. Fails closed to production if `STAGING_SHEET_ID` is missing.

**Bootstrap exception:** `getGovernance()` reads `MASTER_SHEET_ID` directly from Script Properties. Routing it through `getMasterSheetId()` would create infinite recursion. The routing table lives in production Governance_Config; staging reads bootstrap from production.

### Rules for All New Code

1. All sheet access goes through `getMasterSheetId()`. Audit complete — all existing call sites use the wrapper.
2. No hardcoded URLs or sheet IDs. Staging values in `STAGING_DEPLOYMENT_URL` and `STAGING_SHEET_ID`.
3. Code Integrity Mandate applies — never thin or rename `isStaging()`, `getMasterSheetId()`, or any function calling them.

### Finished-final invariant. 

A full-res final episode file is removable by exactly one path: a deliberate, transparent, isolated human action that does nothing but remove it. Removal is never a side effect of any other operation — not a drag, reschedule, save, state change, or task reset. Deletion lives in exactly one isolated function. Arrange enforces this from the scheduling surface; Final_Remove (live, 2026-06-07) enforces it from the upload surface. Both point here.

### Caveats — What Routing Does NOT Cover

- **Drive folders are shared.** `IMAGE_BACKGROUND_LIBRARY_ID`, `CORPUS_DRIVE_FOLDER_ID`, episode folders — all production unless explicitly remapped on staging.
- **External APIs are shared.** Claude API key, Vertex RAG corpus, Gemini — same endpoints, real money on test runs.
- **Triggers always run as production.** `ScriptApp.getService().getUrl()` is null in trigger context, so `isStaging()` returns false. Trigger code paths cannot be tested via staging URL — use `dev_tools.gs` manual invocation.

### Pre-Flight Verification

`DWYP_PreFlight_Staging_Verification.md` provides a Claude Code prompt (verification mode) that checks discriminator function, wrapper presence, orphan call sites, governance key population, and schema parity. Run before any spoke that writes to sheets or modifies schema.

---

## Foundation Documents

See `CLAUDE.md` for canonical doc inventory and reading order.
---

## GAS File Status

| File | Status | Notes |
|---|---|---|
| `fairy_circle.gs` | ✅ Pushed | **SPOKE_Task_Projection (2026-06-07):** `_pulse_recordingReminders` + `_pulse_releaseReminders` replaced with empty no-ops. `clearReminderRows()` added — idempotent sweep: marks Recording_Reminder / Release_Reminder / Runway / Release_Day rows as `complete`, skips already-complete; returns `{ cleared: N }`; logs to Audit_Trail. | **Janitorial (2026-06-05):** THE SCRIBE section deleted — `draftPhaseEmail` + `scribeWriteAndDraft` removed (zero callers; Scribe Fairy retired AD #111). `BRAND_VOICE_ID` key untouched — still live in Herald. `getBodyTextSkippingHeadings` also removed (janitorial Item 2 — sole caller `buildPlaceholderMap` in artist_fairy already gone). | **SPOKE_Orchestrator_Pulse (2026-06-01):** `dailyPulse()` rewritten as single state-driven orchestrator. Old loop stack (Loops 1/2/3/B/C/D) replaced. Stage 0: `checkCalendarForInterviews()`. Stage 1 (`upcoming`): `_pulse_recordingReminders` + transcript watch → `patchEpisodes({Status:"in_production"})` + chain. Stage 2 (`in_production`): `_pulse_contentChain` (A→B→C, chained-within-pulse, per-stage try/catch + error task); `_pulse_reelChain` (syncReelAssets only — editorial pass retired 2026-06-06). Stage 3 (`review`): `_pulse_releaseReminders` + `_pulse_finalVideoDetect` backup (calls `completeFinalEpisodeUpload(epUid)` — no rowIndex). Stages 4/5/6: no-op / stub. Loops 3 + B deleted outright (Safety Fairy retired; images untracked). Helper functions: `_pulse_detectTranscript`, `_pulse_contentChain`, `_pulse_reelChain`, `_pulse_spawnErrorTask`, `_pulse_recordingReminders`, `_pulse_releaseReminders`. **SPOKE_Tasks_1:** All 6 active dailyPulse loop skips: `complete`→`archived`. Loop D: `patchEpisodes(epUid, { Status: "in_production" })` fires on transcript detect (idempotent). **Loop 1 rewritten:** D-1 detection, date-aware task titles, spawns two tasks (HOST + PRODUCER). All other loops unchanged. **Null guard:** `callGeminiAPINoSearch` only sets `payload.systemInstruction` when non-null. **Spoke 3:** `callClaudeAPI(prompt, systemInstruction, callerName, history, options)` added — Anthropic Messages API. `callGeminiImageConversational(prompt, imageHistory, sourceImageBase64, sourceMimeType)` added. `callGeminiImageAPI()` removed. **Staging routing:** `isStaging()` and `getMasterSheetId()` helpers locked architectural pattern. **Phase 1.2–1.3:** `bumpVersion()` with LockService + audit_trail recursion guard. 8 write paths retrofitted. `spawnTask()`/`updateTaskStatus()` get `suppressBump` param. `dailyPulse()` bumps tasks + episodes once per run at end. Staged + verified. **Reels Surface spoke (2026-05-24):** `spawnTask()` — `Asset_ID` field added to `fields` object; written header-driven to Tasks col 17. **Revision Flow spoke (2026-05-24):** `appendEpisodeLog` writes 10-element row (`Revision_Round` as element 10). `spawnTask()` reads `revisionNotes` from taskConfig — fixes blank-instruction bug on all revision tasks. **Team assignee spoke (2026-06-01):** `_pulse_recordingReminders` and `_pulse_releaseReminders` — paired HOST + PRODUCER spawns collapsed to single spawn with `assignee: "team"`. One task row per reminder instead of two. |
| `secretary_fairy.gs` | ✅ Pushed | **SPOKE_Tasks_1:** `createEpisodeRecord()` init: `Status:"upcoming"` (was `"active"`). Calendar handler: `!== "archived"` (was `!== "complete"`). Recording Reminder spawn removed. Calendar scan switched to `Calendar.Events.list()` Advanced Service. `wrapCalendarApiEvent()` adapter. `Utilities.sleep(3000)` between events. **Phase 1.3:** `updateLastActivity()` + `createContactStub()` bump contacts. Staged + verified. |
| `vert_fairy.gs` | ✅ Pushed | **SPOKE_TrackC_Gate_Brief (2026-06-09):** `materializeQuoteGraphicAssets` idempotency gate rewritten per-type — hooks (`Display_Name` prefix `Hook `) and quotes counted separately, empty-`Quote_Text` rows excluded; skip only when both ≥ expected (10/6); partial case writes missing type only; manifest `quote_graphic_asset_count` = actual writes. `gatherVertContext` brief lookup removed; `GUEST BRIEF:` block removed from Track A/B prompts (`findGuestBriefInContactLibrary` untouched — Herald/CRM owner). Transcript-fallback log demoted to `state_change`/info. | **Janitorial (2026-06-05):** Quality_Score + Slot_Tags write lines removed from `materializeQuoteGraphicAssets` (4 writes: hookRow + quoteRow). `runReelEditorialPass` stale skip-guard comment cleaned. | **Spoke 4 + 5:** Major rewrite. Two-pass pipeline. `MODEL_NAME` removed; `CLAUDE_MODEL`, `CLAUDE_API_KEY`, `EPISODE_SEARCH_INDEX_KEY` added. Pure Vertex RAG retrieval; Claude generation via `callClaudeAPI()`. Hook cleaning moved from Gemini to Claude. Pass 2 episode index doc creation. **Track B (May 2026):** `runEditorialPass` + `_buildEditorialPassSystemInstruction_` + `_buildEditorialPassPrompt_`. **Track C (May 2026):** `_bridgeSliceSection_` + `_bridgeParseLabeledCaptions_` + `materializeQuoteGraphicAssets`. Bridge agent writes 16 Asset_Library rows (10 hooks + 6 guest quotes). **Bridge v2 (May 2026):** `runEditorialPass` prompt now compositional — three `extractPrompt` calls (`# Voice Prohibitions`, `# Ranking Schema`, `# Show Notes`). `_bridgeSliceSection_` — em/en/horizontal bar dash normalization (never again fails on Unicode dash variants). `_bridgeParseLabeledCaptions_` — inline caption text fix (captures text on label line, not only body). `_bridgeParseRankedItems_` — new helper, parses v2.3 `HOOK N:` / `QUOTE N:` blocks with inline `SLOT_TAGS:` + `QUALITY_SCORE:` lines; defensive: unrecognized tags → Any, missing score → 3, out-of-range clamped, all anomalies logged. `_appendCaptionSignoff_` — new helper, reads `CAPTION_SIGNOFF` governance key, idempotent (no double-append), applied to all `Caption_Draft` writes in `materializeQuoteGraphicAssets`. `materializeQuoteGraphicAssets` write path: `Slot_Tags` + `Quality_Score` populated from parsed items; `Slide_Index` write retired (commented with dated note). **Reel Editorial (May 2026):** `runReelEditorialPass(epUid, {force=false})` — composes same three template sections, batches all reels in one Claude call, writes cleaned `Reel_Summary` + `Slot_Tags` + `Quality_Score` back per row; idempotent (skips already-scored unless `force:true`). `_parseReelEditorialOutput_` — same defensive parsing as bridge. Test pending (needs episode with Reel-type AL rows with `Reel_Summary` populated). **Pipeline Rewire (May 2026):** `runVertFairy` + all show-notes pipeline functions retired (deleted): `queryVertexShowNotes`, `generateShowNotesWithClaude`, `cleanHooksWithClaude`, `buildShowNotesSystemInstruction`, `buildShowNotesPrompt`, `generateEpisodeIndex`, `createEpisodeIndexDoc`, `createShowNotesDoc`, `writeShowNotesDoc`. Vertex helpers deleted: `_vertexMarkerQuery_`, `_parseTimestamp_`, `_parseTimestampSecs_`, `_parseSpeaker_`, `_extractFirstSentence_`, `_estimateTokens_`, `retrieveVertexRAGContext`. `EPISODE_INDEX_V2_MARKERS` constant deleted. `testRunVertFairy` test wrapper deleted. `buildEpisodeIndexV2` rewritten: Claude reads injected raw transcript via `gatherVertContext`, calls `callClaudeAPI`, writes `.md` file to `EPISODE_SEARCH_INDEX_KEY` folder, patches `manifest.episode_index_v2`. `buildEpisodeIndexPrompt` rewritten: knowledge-only (no HOOKS/QUOTES/IMAGE PROMPTS), extract-not-interpret posture, six sections. `gatherVertContext` and `findTranscriptInFolder` kept (used by new `buildEpisodeIndexV2`). **Transcript-as-source rewire (2026-05-22):** `buildEpisodeIndexPrompt` — truncation cap (`.substring(0, 15000)`) removed; full transcript now reaches Claude. AI Search Index block appended after REEL DESCRIPTIONS: `extractPrompt("# AI Search Index")` + `extractPrompt("# Pillars")` + `extractPrompt("# Voice Prohibitions")` composed + fenced by boundary line (literal extraction / curatorial indexing); silently omitted if sections missing. `gatherVertContext` — third fallback added: Raw Production folder via `manifest.raw_folder_id` (lookup order: Episode/ → Staging root → Raw Production). `runEditorialPass` — index v2 dependency removed (Track B now independent of Track A); transcript + guest brief loaded via `gatherVertContext`; duplicate Contact Library guest brief lookup removed. `_buildEditorialPassPrompt_` — `episodeIndexV2Text` → `transcriptText`; label `EPISODE INDEX V2:` → `FINISHED TRANSCRIPT:`; "reading a curated index" framing → "quotes must be verbatim from the page." |
| `herald_fairy.gs` | ✅ Pushed | **SPOKE_Tasks_1:** Prior-episode filter: `ready_to_release` OR `archived` counts as a prior appearance (was `complete` only). Fix 20: thin-data hard-stop. checkGuestIdentity() helper. Pending path live. Corrupt guard + task spawn on catches. Guest Brief two-step. spawnGuestBriefReviewForJT() exported. **Phase 1.3:** 4 write paths bump contacts. Staged + verified. |
| `artist_fairy.gs` | ✅ Pushed | `exportSlidesToPng()` — sole survivor; called from Track C / pulse (dev_tools.js Stage 5). **Janitorial (2026-06-05):** All dead helpers removed: `buildPlaceholderMap`, `resolveGuestHeadshotBlob`, `resolveHostHeadshotBlob`, `populateSlideDeck`, `processSlide`, `processGroup`, `replaceTextPlaceholders`. File header and stale section comments cleaned up. |
| `filing_fairy.gs` | ✅ Saved | spawnTask() normalization. Dead writes removed. Post-filing tasks: Studio Assets Ready (JT, rename pending Spoke 1) + Produce Episode (Audra). Corpus deposit to `CORPUS_DRIVE_FOLDER_ID`. Direct `ragFiles:import` commented out — us-south1 unsupported. |
| `housekeeping.gs` | ✅ Pushed | **SPOKE_Mending_Fairy_v1 (2026-06-09):** `runMendingFairy(epUid, status)` added — wired in `runHousekeeping()` after `repairStagingSubfolders`. Pre-release guard (`in_production`|`ready_to_release`). Three ops: `_mend_I4_txtRegen` (T1 — auto-write missing `.txt` caption pairs; caption from `_mend_I4_buildCaptionMap`; 3-attempt backoff), `_mend_I1_reconcile` (T2 — `guest_name`/`contact_id` manifest-to-Episodes reconcile; fill-when-absent backoff; conflict → `task_spawned` immediate, no re-spawn), `_mend_O1_guestBioEnrich` (T2 — Herald bio on empty/sentinel Bio_Summary; fill-only; 3-attempt backoff; no signals → `task_spawned` immediate). `_mendDecide`/`_mendUpdate` helpers own backoff axis. `MEND_MAX_ATTEMPTS = 3` constant. Single `patchManifest({ mend_ledger: ledger })` at run-end. | **SPOKE_Tasks_1:** Loop filter: processes `in_production` + `ready_to_release` only (was `active`). `upcoming` excluded — no transcript to parse. parsePipelineBlock(epUid): per-section idempotency. Corpus sync section commented out — us-south1 regional API unavailability. `test_syncCorpusFolder()` stubbed. |
| `clerk_fairy.gs` | 🔴 Rebuild queued | Owns doPost(). **Janitorial (2026-06-05):** Dead `invite → scribeLetSchedule()` route removed — `scribeLetSchedule` never existed; any `invite` POST was throwing ReferenceError. doPost() now returns a logged "no active routes" error for all payload types. Clerk rebuild still queued (AD #24). |
| `dev_tools.gs` | ✅ Pushed | **SPOKE_Mending_Fairy_v1 (2026-06-09):** `test_runMendingFairy()` added — reads `ACTIVE_EP_UID` episode row, passes `(epUid, status)` to `runMendingFairy`. MENDING FAIRY section inserted between MAINTENANCE and TASK PROJECTION MIGRATION. | **SPOKE_Task_Projection (2026-06-07):** `test_clearReminderRows()` added — one-time migration wrapper for `clearReminderRows()`; logs cleared count. | `ACTIVE_EP_UID` = `EP-260430-1505` (Dr. Buck Blodgett). **SPOKE_Orchestrator_Pulse (2026-06-01):** Rewritten in lifecycle order: DIAGNOSTICS (`test_checkTokenScopes`, `test_checkSignerSa`); STAGE 0 (`test_checkCalendarForInterviews`); STAGE 1 (`test_buildEpisodeIndexV2`); STAGE 2 (`test_runEditorialPass`); STAGE 3 (`test_materializeQuoteGraphicAssets`); STAGE 4 (`test_syncReelAssets`); STAGE 5 (`test_exportSlidesToPng`); STAGE 6 (`test_spawnUploadEpisodeTask`); REELS REVISION (`test_closeReelRevision`); SYSTEM-LEVEL (`test_dailyPulse`); MAINTENANCE (`test_repairStagingSubfolders`, `test_runFilingFairy`, `test_archiveLiveEpisodes`). `test_artistFairy` deleted (runArtistFairy retired). |
| `dwyp_app.gs` | ✅ Pushed | **SPOKE_Final_Remove_Proxy_Replace (2026-06-07):** `removeFinalEpisodeUpload(episodeUid)` — isolated deliberate scrub of the full-res final: trashes the single file in `Episode/`, clears `Final_Episode_ID`, reverts `Status → review`, spawns (or keeps) open `Upload_Final_Episode` task; `Video_Status` never touched; bumps episodes + tasks; logs human_action audit trail. `reconcileFinalEpisodeSlot(episodeUid)` — slot truth check: if `Final_Episode_ID` set but `Episode/` empty → backward-reconcile (same patch/spawn, no trash); if `Episode/` has file but ID blank → forward-complete via `completeFinalEpisodeUpload`; consistent → no-op. Pulse safety verified: Stage 3 calls `completeFinalEpisodeUpload` which returns "No file found" on empty slot; pulse swallows that error — no thrash condition. | **SPOKE_Episode_Arrange (2026-06-07):** `saveArrangeOrder(orderedEuids)` — reads Episodes tab, computes next-Tuesday base, writes `Release_Date` by position, clears `Release_Date` for non-live/non-archived episodes not in list (drag-back), `bumpVersion('episodes')`. `_nextTuesdayBase_()` — returns next Tuesday (if today IS Tuesday, base = today). `updateEpisodeName(episodeUid, newName)` — writes `Guest_Name`. `getEpisodeCompletedTasks(episodeUid)` — returns complete-status tasks, skipping demoted cue workflow steps. `uncompleteTask(rowIndex)` — flips Status → open, clears Completed_At, `bumpVersion('tasks')`. | **Janitorial (2026-06-05):** `Display_Text: 21` removed from `ASSET_LIBRARY_COLS` (phantom col — sheet is 20 cols); col-21 write removed from `saveAssetDraft`; `display_text` field removed from `getAssetDisplayState` return; `_displayText` reference in `getRankedAssetLibraryCandidates` nulled. Dead Publish-era functions deleted: `placeAssetInSlot`, `rescheduleAsset`, `scheduleReel`, plus their sole-consumer helpers `getOrCreateApprovedFolder`, `buildApprovedFilename`, `writeApprovedAsset`. **Janitorial Item 2 complete (2026-06-05):** `Quality_Score`/`Slot_Tags` entries removed from `ASSET_LIBRARY_COLS`; 4 empty-string writes removed from `materializeQuoteGraphicAssets`; `getRankedAssetLibraryCandidates` deleted (zero callers). Open Audra hand-step: remove `SLOT_TAGS:`/`QUALITY_SCORE:` emit lines from Master Template. | **SPOKE_4 (2026-06-05):** `saveDerivativeAsset(assetId, action, canvasJson, captionText, quoteText)` added — `action='save'` overwrites same AL row (`Quote_Text`, `Canvas_State`, `Caption_Host`); `action='save_copy'` appends new AL row (`Status='schedule'`, `Availability='available'`, shares `Drive_File_ID`); AL-only, no Social_Assets touch; bumpVersion + Audit_Trail. `_uniqueFilename(folder, baseName, ext)` helper added — collision-safe export filenames; suffix `-2`/`-3`; applied to `_writeAssetToExportFolder` + `exportSingleScheduleAsset` (reel and image paths; paired `.txt` sidecar follows same suffix). | **SPOKE_2.1+3b (2026-06-05):** `addToWeekAsImage` deleted (legacy Publish canvas path; was the only `"scheduled"` enum write source). `exportAssetToDrive` + `exportReelToDrive` deleted (internals relocated to Schedule export path). `exportSingleScheduleAsset(episodeUid, assetId, base64Png)` added — QG: writes client-rendered PNG to `Manual_Exports/Singles/` + `.txt` (Caption_Host); reel: COPY Drive file to `Singles/` + `.txt`; no Status/Availability changes. `exportAllSchedule(episodeUid, imageRenders)` rewritten — accepts `imageRenders` map from client; gate `Availability=placed` on AL row; flat `Manual_Exports/[Day]/`+`/SWIPE/` structure (no guest wrapper); day/SWIPE cleared on re-run, `Singles/` preserved; reels COPIED. `_writeAssetToExportFolder` updated (new `base64Png` param; reel COPY instead of link-only). `closeReelRevision` stale Loop C docstring corrected (Loop C confirmed deleted AD #93). Vestigial `"scheduled"` writes (`placeAssetInSlot`, `scheduleReel`, `rescheduleAsset`) deleted in janitorial spoke 2026-06-05. | **SPOKE_Schedule_1 (2026-06-02):** Three new GAS endpoints. `getScheduleData(episodeUid)` — single round-trip: AL candidates (`Status='schedule'`), Posting_Schedule week structure (days/slots/why), Social_Assets placements for the episode. `placeAssetSchedule(episodeUid, assetId, slotId, caption)` — appends SA row; week slot sets `AL.Availability='placed'`; swipe slot skips Availability. `removeAssetFromSchedule(episodeUid, assetId, slotId)` — deletes first matching SA row (assetId+slotId+epUid); week slot flips `AL.Availability='available'`. All three call `bumpVersion('social_assets', ...)`. **SPOKE_Schedule_3 (2026-06-02):** `exportAllSchedule(episodeUid)` — reads SA placements for episode, partitions day-slots vs. SWIPE, resolves AL rows, builds `Staging/[GuestName]/` folder tree (only non-empty day folders + SWIPE if needed), copies image PNGs via `makeCopy` + writes matching `.txt` (Caption_Host), writes reel `.txt` (Display_Name + caption + Drive link). Re-run: clear-and-rebuild (trash + recreate guest folder). Caption selection: image always Caption_Host; reel Caption_Host for week, Caption_Guest for SWIPE. Helper: `_writeAssetToExportFolder(al, folder, isSwipe)`. Sanitizer: `_safeFilename(name)`. **Untested — push pending clasp re-auth.** | **SPOKE_Tasks_1:** `getEpisodes()` + `getActiveEpisodes()`: filter `archived` (was `complete`). `triggerReadyForRelease()`: `patchEpisodes(episodeUid, { Status: "ready_to_release" })` added before Filing task spawn. `EPISODES_COLS`: `Frameio_Project_ID` (col 15) + `Guest_Package_URL` (col 16) retired. **Episode Upload spoke (2026-05-26):** `_signV4(method, objectPath, expirySec, bucket, signerSa, extraHeaders)` — shared V4 signing helper; signs via `iamcredentials.googleapis.com :signBlob` (not `iam.googleapis.com`); `payload` field in request body (iamcredentials schema). `getEpisodeStreamUrl` refactored to use `_signV4`. `getEpisodeUploadUrl(episodeUid)` — mints V4 POST signed URL for resumable upload initiation; `x-goog-resumable:start` in signed headers. `checkEpisodeProxyExists(episodeUid)` — GCS JSON API HEAD/GET metadata check using owner OAuth token; returns `{exists:true|false}`. Also in this session: removed stale `Logger.log(JSON.stringify(tasks))` + misindented `return tasks` from `getTasks()`. | **Pipeline Rewire (May 2026):** `runVertFairyForEpisode` repointed to `buildEpisodeIndexV2` — function name preserved (Fairy Remote Control UI button calls it by name). `SOCIAL_ASSETS_COLS` 20-column map. Availability filter. Drive-fallback. Slide pairing. Reel Display_Name. v3 Publish image canvas + Hooks/Quotes. Spoke 6 `generateWithClaude()` 5-param signature. `isImageRequest()`/`isExplicitTextRequest()` heuristics. `saveBackgroundToLibrary()`. `stLoadEpisodeIndex()`. Asset enrichment functions. Reels Surface caption/title card Generate buttons. **Phase 1.2–1.3:** `getAllVersions()`, `getDomainVersion()` endpoints. 22 write paths retrofitted. `_resolveImageLibraryVersion()` corrected to scan file timestamps. Staged + verified. **Item 92 Phase 1 (May 2026):** `_parseCaptionDraft_(raw)` — defensive parse for Caption_Draft (handles JSON-stringified array from Gemini pre-pass output; picks parsed[0] if array). `getRankedAssetLibraryCandidates(episodeUid, assetType, slotId)` — **[vestigial — rankings retired; see Current Position]** server-side ranked read: Reel delegates to `getReelsForEpisode()`; image types filter Episode_UID + Asset_Type + Availability='available', rank by tag-match (slotId in Slot_Tags) then Quality_Score DESC then Created_At ASC (null QS = 0), return top 6. `assembleSlotForegroundContext(activeAssetId, activeAssetType, episodeUid)` — written, not yet wired (Phase 4); returns {active_card, same_date_siblings (cap 4, SIBLING_CAP hardcoded — OQ-D), episode}. `getPrecompBgImages()` — reads PRECOMP_BACKGROUND_LIBRARY_ID folder; returns {fileId, name, textColor, thumbnailUrl}; textColor from `_darktext`/`_lighttext` filename suffix (#1a1714 / #ffffff); sorted by filename; limit 60. `ASSET_LIBRARY_COLS` updated: col 19 = Quality_Score (vestigial-ranking), col 20 = Slot_Tags (vestigial-ranking); written by pipeline execution (`materializeQuoteGraphicAssets`, `runReelEditorialPass`) — no active midnight ranking pass under rankings-retired model. See Current Position. **Item 92 Phase 2 (May 2026):** `exportAssetToDrive(episodeUid, slotId, assetId, b64, canvasJson)` — resolves episode working folder, creates `Manual_Exports/` subfolder, writes PNG blob as `{slotId}_{assetId}_{YYYYMMDD-HHMM}.png` (JT_TIMEZONE), writes Canvas_State to AL row, logs `MANUAL_EXPORT` to Audit_Trail, returns `{url, filename, folderUrl}`. **Spoke 0 — Caption Consolidation (May 2026):** `ASSET_LIBRARY_COLS` keys renamed: `Caption_Draft` → `Caption_Host` (col 10), `Caption_Final` → `Caption_Guest` (col 11). All read/write paths updated. Enrichment generators (`generatePublishCaption`, `generateReelCaption`, `enrichQuoteAssetsFromTranscript`, `enrichReelsForEpisode`, `generateCaptionVariants_`, `callGeminiVideoAnalysis_`, `callGeminiTextAnalysis_`, `generateCaptionVariantsBatch_`) and `_parseCaptionDraft_` retired. Vert Fairy guard: `Canvas_State === ''` alone (Caption_Host removed — always set by system on row creation). **Design Sprint (May 2026):** `exportAssetToDrive` extended: `day` param adds day prefix (e.g. `MON_`) to filename; `canvasText` + `caption` params write paired `.txt` companion blob to `Manual_Exports/`. `exportReelToDrive(episodeUid, day, reelAssetId, titleText, caption)` added — resolves Drive file ID from Asset_Library (ASSET_LIBRARY_COLS.Drive_File_ID col 4), copies reel with day prefix to `Manual_Exports/`, writes paired `.txt` (titleText + caption), logs `REEL_EXPORT` to Audit_Trail. **2026-05-22 session:** `callGeminiVideoAnalysis_(driveFileId, prompt, apiKey)` restored — resumable upload to Gemini Files API, polls until ACTIVE, generateContent, DELETE temp file; 45MB size limit guard. `syncReelAssets(epUid, opts)` added — scans Staging/Reels/ (Approved/ first, then root) for MP4s, creates AL rows for unregistered files (Asset_ID = UUID, Status = candidate, Availability = available), runs `callGeminiVideoAnalysis_` per row; 4.5-min timeout guard (re-run picks up where it left off); `bumpVersion` + `logToAuditTrail` on completion. `stLoadEpisodeIndex()` rewritten — primary path: `gatherVertContext(episodeUid, "Studio")` → returns `transcriptText` (full transcript, same three-tier lookup as Track A/B); fallback: `manifest.episode_index_v2` blob read. Companion now grounded in full transcript. `STUDIO_MODE_INSTRUCTIONS.images` — `[[PROMPT: ...]]` instruction and `PROMPT —` definition removed. **Reels Surface spoke (2026-05-24):** `TASKS_COLS.Asset_ID: 17` added. `getTasks()` returns `Asset_ID`. `createTask()` writes 17-col row. `exportReelToDrive` changed copy→move. New: `generateReelCaption(assetId)` (reads `Reel_Summary`, calls Claude with `# Caption Mechanics` + `# Voice Prohibitions` system prompt, writes `Caption_Host`); `spawnReelEditTask(epUid, assetId, type)` (spawns `Revise_Reels`/`edit_vids` task with `Asset_ID` FK); `requestReelRevision(epUid, assetId)` (completes `Review_Reels`, spawns `Revise_Reels`); `closeReelRevision(epUid, assetId, newDriveFileId)` (swaps `Drive_File_ID` on AL row by `Asset_ID`, moves old → `Reels/Superseded/`, completes `Revise_Reels`, `bumpVersion` both domains). **2026-05-25 session:** `exportReelToDrive` — filename now uses title slug (sanitized `titleText`, max 120 chars) instead of day prefix + timestamp; day param removed. `getReelStreamUrl` — returns `/preview` URL (`https://drive.google.com/file/d/{fileId}/preview`); was `uc?id=`. | **SPOKE_Tasks_2 + SPOKE_Tasks_3:** No dwyp_app.gs changes (UI-only). **SPOKE_Episode_Tasks_1 (2026-05-31):** `EPISODES_COLS.Images_Status` renamed to `Final_Episode_ID` (col 12). `getEpisodes()` return field renamed to match. `spawnReelEditTask` adds `Drive_File_ID` lookup from Asset_Library; writes `payloadLink` (Drive file URL) to Revise_Reels spawn. New `approveEpisodeForRelease(episodeUid)` — calls `writeVideoStatus(approved)` then idempotently spawns `Upload_Final_Episode` task; replaces `writeVideoStatus` as the UI endpoint. New `completeFinalEpisodeUpload(episodeUid, rowIndex)` — scans `Staging/Episode/` for exactly one file, writes Drive ID to `Final_Episode_ID`, calls `patchEpisodes({Status:"ready_to_release",...})`, closes open Review_ tasks defensively, spawns Filing + Release tasks (Release idempotent), bumps versions, logs audit trail. New `completeReelRevision(episodeUid, assetId)` — scans `Reels/` root for any file whose Drive ID differs from the AL row current `Drive_File_ID`; expects exactly one new file; delegates to `closeReelRevision`. RFR cleanup complete (2026-05-31): `triggerReadyForRelease`, `checkReadyForRelease`, RFR modal, `.btn-ready-for-release` CSS deleted. **SPOKE_Orchestrator_Pulse (2026-06-01):** `completeUploadEpisode` — adds `patchEpisodes(episodeUid, { Status: 'review' })` after `Video_Status → review` write (same trigger, one added write: proxy-receipt now flips both Video_Status and Episode.Status). `completeFinalEpisodeUpload(episodeUid, rowIndex)` — `rowIndex` now optional; when absent (pulse backup path), scans Tasks for open `Upload_Final_Episode` task and completes it by row-scan; UI path (rowIndex present) unchanged. |
| `dwyp_ui.html` | ✅ Pushed (items 59–84 + item 91 + 2026-05-22 session; Reels spoke 2026-05-24; reel player revert 2026-05-25; Episode Upload spoke 2026-05-26; SPOKE_2.1+3b 2026-06-05; SPOKE_4 2026-06-05; Studio Cache 2026-06-07; SPOKE_Task_Projection + SPOKE_Episode_Arrange 2026-06-07; SPOKE_Final_Remove_Proxy_Replace 2026-06-07; SPOKE_Show_Notes_Card_Editor + postfix fixes 2026-06-08; SPOKE_Icon_Rail_Half_Height_Fix 2026-06-08; SPOKE_Freeform_Description_Editable 2026-06-09; SPOKE_IconRail_Migration + SPOKE_IconRail_Correction + SPOKE_IconRail_State_Behavior 2026-06-09) | **SPOKE_Final_Remove_Proxy_Replace (2026-06-07):** `stRenderEpCardTaskList` — two new slot affordances appended after the task list (rendered from `ep` data, not task rows). (1) Final-file slot: shown when `ep.Final_Episode_ID` is truthy; "Remove final" button (`.btn-delete`) → `stRemoveFinalEpisode` — confirm dialog + `removeFinalEpisodeUpload` GAS call; "Sync" button → `stSyncFinalSlot` — `reconcileFinalEpisodeSlot` GAS call; both use ID-keyed element refs (`sync-btn-{uid}`, `remove-btn-{uid}`). Optimistic local state update on Remove (clears `ep.Final_Episode_ID`, sets `ep.Status = 'review'`) before `refreshCurrentView`. (2) Proxy slot: shown when `ep.Video_Status` is non-empty and not `pending`; "Replace proxy" button (`.btn-complete`) → `startProxyReplace` — clicks hidden file input `proxy-file-{uid}`. Proxy replace flow: `startProxyReplace` → `onProxyFileSelected` → `getEpisodeUploadUrl` (reuses existing signed URL endpoint) → `_initiateResumableSession` (reused) → `_doProxyResumableUpload` (dedicated function; same 5 MB chunk/308-opaqueredirect pattern; on success shows "Proxy replaced." toast, no GAS completion call, no state changes). Progress UI: `proxy-upload-panel-{uid}` / `proxy-upload-bar-{uid}` / `proxy-upload-status-{uid}` + `_updateProxyUploadUI` / `_resetProxyUploadUI`. Visually distinct: Remove final = `.btn-delete` (transparent, red hover); Replace proxy = `.btn-complete` (neutral). | **SPOKE_Task_Projection + SPOKE_Episode_Arrange (2026-06-07):** CSS: `.st-ep-cue` (projected cue row — italic, small, flex with icon gap), `.arr-mode-bar` (toolbar strip), `.arr-dirty-dot` (7px gold, `.visible` shows it), `.arr-toggle-btn` / `.arr-save-btn` / `.arr-discard-btn`, `.arr-drop-zone` (expands to 24px gold on `.arr-over`), `.arr-tbd-drop` (dashed unscheduled drop area), `.st-ep-completed-task` / `.st-ep-completed-title` / `.st-ep-uncomplete-btn`. State: `st.arrangeMode`, `st.arrangeDirty`, `st.arrangeOrder`, `st.arrangeTbdIds`, `st._arrangeDragSrc`. `stGetEpCues(ep)` — window-math projection function (two axes). `stRenderEpisodesView()` dispatches to normal or arrange renderer. `stArrToolbarHtml()` — mode bar (both modes). Arrange state: `stArrangeEnter` / `stArrangeExit` / `stArrangeDiscard` (silent — no confirm) / `stToggleArrangeMode` / `stArrangeLeaveCheck` (dirty-nav guard). Drop handlers: `arrDzDrop` (insert-and-shift), `arrTbdDrop` (drag-back → Unscheduled). Arrange renderers: `stRenderEpisodesViewNormal` / `stRenderEpisodesViewArrange`. Card renderers: `stRenderEpArrBandCard` (scheduled; locked if live/archived), `stRenderEpArrTbdCard` (unscheduled; drag-enabled). Leave-guard wired to `switchTab`, `stSelectGuest`, `stSelectGuestView`, `stAccSelectTaskView`. `stToggleEpCard` lazy-loads completed tasks via `getEpisodeCompletedTasks` on first expand. `stLoadEpCompletedTasks` + `stUncompleteTask` + `stEditEpName` card edit handlers. `#stEpisodesView` container `flex-direction:column`. | **Studio Version-Aware Cache (2026-06-07):** Session-scoped in-memory read-path cache for all four Studio surfaces. `st._cache` object added to studio state (six surfaces: `reels`, `imagesHq`, `imagesBg`, `episodeRev`, `episodeUrl`, `schedule`). Four helpers: `_cacheGet`, `_cacheSet`, `_cacheIsStale`, `_cacheInvalidate`. Pattern: cache hit → render immediately → background `getAllVersions()` → quiet refetch if stale; miss → `getAllVersions()` first → fetch with `versionsAtFetch` recorded. CP0 finding: Images H&Q reads `manifests` (manifest Show Notes doc), NOT `asset_library` — corrected from original spoke draft. Pre-existing `st.hqContent` episode-key bug fixed by episode-keyed cache. Five surface gates: `stLoadDesignEpisode` (stream URL UID-keyed, no version gate; revisions gated on `episodes`), `stLoadRailReels` (`asset_library`), `stLoadHqContent` (`manifests`), `stLoadBackgrounds` (`image_library`), `stShowSchedule` (`asset_library` + `episodes`). 10 write-path `_cacheInvalidate` calls: `schDoPlace` + `schRemoveFromSlot` + `schRefreshFromServer` (must-haves — Social_Assets writes carry no version bump); `derivativeSave`, `derivativeSaveACopy`, `derivativeReelSave`, `derivativeReelSaveACopy`, `stSendToScheduleReel`, `stEpApprove`, `stEpRequestRevisions`. `stSyncFlush()`: captures `{ tab, dtab, uid }`, global-flushes all `st._cache`, re-triggers captured surface loader in place; off-Studio safe. Sync button: `avatar-menu-item` in avatar menu above Sign Out. | **SPOKE_4 (2026-06-05):** Derivative edit mode built end-to-end. `st.derivativeMode` flag added to studio state. `schEditCard` sets flag (removes old `publishOrigin` assignment). `stSyncDerivativeUI()` — reads `st.derivativeMode` + `st.designTab`, shows/hides action buttons; called at end of `stSelectDesignTab`. Images toolbar: derivative triad buttons (`stDerivSaveBtn`, `stDerivCopyBtn`, `stDerivDiscardBtn`) added (hidden by default); Schedule + Clear hidden in derivative mode. Reels action bar: reel derivative triad (`stReelDerivSaveBtn`, `stReelDerivCopyBtn`, `stReelDerivDiscardBtn`) added (hidden by default); Send to Schedule + Request Revision hidden in derivative mode. `stSetRailTab` — redirects `'hq'` → `'bg'` in derivative mode. `stRenderRailReelList` — filters to `st.selectedReelId` when `st.derivativeMode` (locked rail); `st.selectedReelId` pre-set in the edit round-trip block so filter activates before GAS async returns. Image derivative functions: `_derivImgExtract`, `derivativeSave`, `derivativeSaveACopy`, `derivativeDiscard`. Reel derivative functions: `derivativeReelSave`, `derivativeReelSaveACopy`, `derivativeReelDiscard`. All exit paths clear `st.derivativeMode`, call `stSyncDerivativeUI`, navigate to Schedule. `stCloseStudio` clears `derivativeMode`. | **SPOKE_2.1+3b (2026-06-05):** Export button + day-picker removed from Images canvas toolbar; `#stReelExportBtn` removed from Reels action bar. JS removed: `stToggleDayPicker`, `stPickDay`, `stExportDesign`, `stExportQg`, `stExportReel`. Pool card: "export" button added per asset → `schExportSingle(assetId)`. New `schExportSingle(assetId)` — QG renders full-res offscreen via `_schRenderQgFullRes` then calls `exportSingleScheduleAsset`; reel calls GAS directly. New `_schRenderQgFullRes(canvasStateJson, backgroundId, callback)` — offscreen Fabric StaticCanvas, multiplier:3 (1080×1350), same `bgCache`/`_schLoadBgForThumb` dedup path as thumbnail renderer. `schExportAll()` rewritten — collects placed QG candidates, renders each sequentially via `_schRenderQgFullRes`, passes `imageRenders` map to `exportAllSchedule(episodeUid, imageRenders)`. | **Episode Upload spoke (2026-05-26):** CSS — `.ep-upload-panel`, `.ep-upload-bar-wrap`, `.ep-upload-bar`, `.ep-upload-status`. `renderTaskButtons` — `Upload_Produced_Episode` task type renders "Upload Proxy" button calling `startEpisodeUpload`. `renderTaskCard` — injects hidden file picker + upload panel div for `Upload_Produced_Episode` tasks. Upload flow: `startEpisodeUpload` (existence check → self-heal dialog or file picker), `onEpisodeFileSelected` (hides button, shows panel, calls `getEpisodeUploadUrl`, initiates session), `_initiateResumableSession` (XHR POST to signed URL — `X-Goog-Resumable: start` + `Content-Type`; handles 200 and 201; reads `Location` header), `_doResumableUpload` (5 MB chunks via `fetch()` with `redirect:'manual'` — 308 Resume Incomplete resolves as `response.type === 'opaqueredirect'`; 200/201 = finalized; XHR rejected because browsers fire `onerror` on 308 with no Location header), `_onEpUploadComplete` (calls `completeUploadEpisode`), `_updateEpUploadUI`, `_resetEpUploadUI`, `_fmtBytes`. **`stRenderAccordionTasks()` rewritten** — previously filtered to `!t.Episode_UID` (loose only) and showed "No loose tasks"; now renders all tasks via `renderTaskCard()`. `loadData()` success handler — added `stRenderAccordionTasks()` call. `refreshCurrentView()` — added `stRenderAccordionTasks()` at top so task completions update accordion. | Studio left nav (Publish/Design/Write/Outreach/Ideas). Episode accordion, proxy player, F-4 comment submit. Reel workflow card layout (inline player). Image workflow v3 three-panel layout. Fabric.js canvas 360×450, 4:5 export 1080×1350. Spoke 7 session state, episode index loading, mode-aware tab switching. Reel card grid `minmax(0,1fr)` overflow fix. Drop shadow blur reduced. Attribution chip dark + gold + Nunito. **UI polish (May 2026):** Reel trim overlay (Ouroboros SVG + `pbLogoPulse` CSS keyframe, replaces amber bar); trim state persists across day-stack nav (`pb._trimPending` + `_pbApplyPendingTrimOverlays()`). Image card stack — 4:5 thumbnail with padding, red serif title, Drive CDN stubs (`_PB_FEED_STUBS`, 6 URLs). Image editor — toolbar + caption + actions in right column; canvas-left / controls-right layout; slot header platform/why rows hidden via CSS; Back-to-cards inline in header row. Scroll padding on `.pb-ws-active`. Right-rail drag resize handle between Claude and Backgrounds panels. **v3 Center Canvas cosmetic pass (May 2026):** Left rail font/color/size fixes; urgent=red rule removed; active=gold CSS fix; rail never collapses. Reel card collapsed = 9:16 placeholder + title card + summary. Reel expanded = animated side-by-side, header click to collapse, height-capped. Image card whole-card-clickable + hint text. `pbCardNameInput` stub. `title_card` in reel stubs. SVG gradient IDs made unique per card. Urgency past-date bug fixed. **Deferred (wiring phase):** Trim deep-link to specific GCS/Vids file; Processing overlay real async trigger. **Item 92 Phases 1–2 (May 2026):** `_parseCaptionDraft(raw)` (client-side mirror). `_pbNormType(assetType)` — normalizes all bank/bankclip variants to 'reel'. `_pbPrefetchAssets(uid, schedule)` — pre-fetches image-type candidates async per assetType into `pb._alCandidateCache`; pre-fetches reels into `pb.reelCards`. `_pbFindCandidateById(assetId, assetType)` — searches candidate cache. `getRankedCandidates()` **[vestigial — rankings retired; see Current Position]** updated: reads from `pb._alCandidateCache` for image types; falls back to stub pool during async load; shows precomp bg thumbnail (by canvas index) for cards with no Drive export. `_pbHydrateCardCanvas()` — three-tier hydration: (1) restore from `pb.cardCanvases[assetId]` (in-session state); (2) restore from `c.canvas_state` (AL row JSON, undo floor locked); (3) fresh build — resolves precomp bg by `_candidateIndex % bgPool.length`, sets `pb._defaultTextColor` from bg textColor signal, calls `pbAddTextToCanvas(c.quote_text)` then `pbApplyBackground()`, locks undo floor. `pbToolAddText()` — selects existing text object for editing or creates new via `pbAddTextToCanvas('Type your text here…')`. `loadPrecompBgImages()` — async fetch; retroactively applies background + corrects text fill if canvas open during load (race-condition handling). `pbCardClick` non-reel path: snapshots outgoing card to `pb.cardCanvases[outgoingAssetId]` before dispose; calls `_pbHydrateCardCanvas()`. `pbSaveAndExit` critical identity fix: saves to `pb.cardCanvases[assetId]` (not `pb.slotCanvases[slotId]`) — each image card has independent per-assetId storage; `pbSelectSlot` always calls `pbInitCanvas()` fresh for image cards. `pb` state additions: `_alCandidateCache`, `_activeCandidateData`, `_reelCardsLoaded`, `cardCanvases`, `_defaultTextColor`, `_precompBgImages`. Caption prefill from Caption_Draft only if no localStorage caption for this slot. Phase 2 ✅ shipped (May 2026): Fix 1 — `pbSaveAndExit` strips `obj.src` for data URIs + nulls filter matrices before server call (Canvas_State now writes to AL row). Fix A — Save button retired, Export button added (calls `exportAssetToDrive`); all exit paths route through save core before teardown; three exit semantics locked. Fix B — `_pbHydrateCardCanvas` Tier 2 resets `viewportTransform` + clears `backgroundImage` before bg re-apply (eliminates coordinate drift). Fix C — dual-JSON in save-core: `fullCanvasJson` (full base64) → `pb.cardCanvases[assetId]` (synchronous Tier 1 reopen, no async race); `serverCanvasJson` (stripped src, null filters) → server. **Spoke 0 — Caption Consolidation (May 2026):** `caption_draft` → `caption_host` throughout. `_parseCaptionDraft()` client-side retired; all call sites replaced with `String(...)`. `pbSetCaptionFromDraft()` simplified (no JSON.parse, `captionVariants` cleared). Regenerate caption button retired from image canvas + reel workspace. Generate caption button retired from reel card list. `pbRegenCaption()` and `pbGenerateCaption()` retired. **Design Surface Sprint — Round 1 (May 2026):** Design tab shipped as persistent standalone surface. H&Q chips panel in chat col — tap drops text onto `st.fabricCanvas`. QG/Reels sub-tabs (`stSetDesignTab`), Mon–Sat day picker + Export (`stExportQg`, `stExportReel`). Reel list view in content col. "Export Image" button removed from canvas toolbar. `stLoadHqContent`, `stRenderHqPanel`, `stDropText`, `stLoadReels`, `stRenderReelList`, `stSelectReel`, `stSetExportDay` added. **Design Restructure CP1+2 — needs revision (May 2026):** Left rail replaced with accordion (Design/Write/Schedule/Tasks headers). Guest picker removed from chat col — rail owns guest selection via `stRenderRailGuests`, `stSelectGuest`. Images|Reels segmented toggle (`stSegRow`) moved to chat col top. H&Q panel wrapped in collapsible tray (`stHqTray`): open by default, collapses on chip tap, tap header to reopen. Reel browser stub added to right rail (`stReelBrowser`, shown in Reels mode). `stAccToggle`, `stAccOpen`, `stAccSelectMode`, `stToggleHqTray`, `stCollapseHqTray`, `stLoadRailReels`, `stRenderRailReelList`, `stSelectRailReel` added. **Status: needs revision — bugs not yet diagnosed. CP3+4 (caption pinned field in chat col, Reels companion, reel player in center col) deferred. Tasks accordion data source (actual to-do items) needs Hub session to define.** **Design Surface Overhaul (May 2026):** Text scaling (normalize-on-scale corner drag + A+/A− buttons), color picker popover (brand + complementary rows), `stApplyColor` per-char fill clear, overlay reset (circular arrow SVG, pushes history), export PNG fix (`multiplier:2` removed), export txt passes caption field, delete key guard (`isContentEditable`). **Publish Retirement (May 2026):** All `pb*` CSS (~2,150 lines), stPublishPanel HTML, var pb state, ~120 `pb*` functions removed. Left nav: Publish entry gone, Design is default. `openStudio()` defaults to `'design'`. `showEpisodeReview()` → `openStudio('design')`. `pbAutoSelectEpisode()` retired; auto-select wired to `stSelectGuest()`. **2026-05-22 session:** Reels center panel background changed from `#111` to `#f0f0f0`. Phone frame made height-driven (`min(72vh, 560px)` aspect-ratio:9/16) replacing fixed 200px width; iframe absolutely positioned with top:-52px / height:calc(100%+100px) to clip Drive preview toolbar chrome. `stRenderRailReelList` — `Reel_Summary` now rendered in reel cards (`.st-reel-card-summary`, 3-line clamp). Reels selector disabled: `stSegReels` button set `disabled`; `stSetDesignTab` guards `tab==='reels'` → redirects to `'qg'` (blocks button, programmatic calls, and persisted state on load). Companion image prompts removed: `[[PROMPT:]]` instruction + `PROMPT —` definition stripped from `STUDIO_MODE_INSTRUCTIONS.images`; regex in `stFormatResponse` changed from `HOOK\|QUOTE\|PROMPT` to `HOOK\|QUOTE`; `stTapChip` prompt branch removed. **Reels Surface spoke (2026-05-24):** `stSegReels` re-enabled (disabled attr removed, `stSetDesignTab` guard removed). Caption box wired to `Caption_Host`. Generate caption button + `stGenerateReelCaption()`. Action bar (`stReelActions`): Export day-picker → `stPickReelDay` → `stExportReel`; Edit with Vids → `stEditWithVids()`; Request Revision → `stRequestRevision()`. `stSelectRailReel` shows action bar on reel select. `stExportReel` ref fixed: `stExportBtn` → `stReelExportBtn`. New: `stToggleReelDayPicker`, `stPickReelDay`, `stEditWithVids`, `stRequestRevision`. **Revision Flow spoke (2026-05-24):** Episode review rewritten — native `<video>` via `getProxyStreamUrl` (was Drive iframe), compose loop (`epComposerFocus/Send/Cancel`), rail receipts (`epRailAppendRow`, `epRenderRailFromHistory`), hard-seal Request Revisions (`epRequestRevisions`). `epReviewState` replaces `reviewSession`. `Revise_Episode` Complete → `completeEpisodeRevisionTask`. Reel revision popup (`reelRevisionModal`) replaces inline path; `stRequestRevision` opens popup; `stSubmitReelRevisionPopup` passes notes to `requestReelRevision`. **2026-05-25 session:** Drive chrome accepted — iframe crop hack retired; `#stReelPlayerFrame` now `position:absolute; inset:0; width:100%; height:100%`. Modal z-index bug fixed (duplicate `z-index:200` overrode `z-index:400`; modal was rendering behind Studio Overlay at z-index 300). `#reelRevisionModal` centered + full corner radius. Reel card layout redesigned: `st-reel-card-inner` row layout; `.st-reel-thumb-col` (flex 0 0 33%) + `.st-reel-body-col` (flex 1); thumbnail via Drive thumbnail API (`?sz=w200`); `Reel_Summary` shown in full (3-line clamp removed). Action bar simplified: day-picker and Edit with Vids removed; Export calls `stExportReel(st.episodeUid)` directly. `stToggleReelDayPicker`, `stPickReelDay`, `stEditWithVids` removed. **Nav Panel Teardown Fix (2026-05-30):** `stHideAllPanels()` added — hides all 11 content/assets panels; called first in `stSetDesignTab`, `stAccSelectTaskView`, `stShowScheduleStub`, `stAccSelectMode`. `stSyncSubItemActive` extended to clear Tasks `active` state when Guest sub-item activates. `stAccSelectTaskView` extended to clear Guest sub-item `active` state when Tasks item activates. | **SPOKE_Tasks_2:** `stRenderEpBandCard` (action label + state line + icon placeholders), `stRenderEpUpcomingCard`, `stRenderEpTbdCard`, `stRenderEpisodesView`. `stEpisodesView` added to `stHideAllPanels`. **SPOKE_Tasks_3:** `stBucketsView` panel — bucket bands, Quick Tasks, Capture form. `stRenderBucketsView` + helpers. `renderDashboardLoose` retired. **SPOKE_Episode_Tasks_1 (2026-05-31):** `approveEpisodeVideo` wired to `approveEpisodeForRelease`. `stRenderEpBandCard` rewritten — two-column layout (left: name/dateline/blocking task; right: state+icon/action dots); card-body tap = expand/collapse task list; lit headphones → Studio Episode; lit film → Studio Reels; calendar always inert stub. `stRenderEpUpcomingCard` — mic replaced by feather (quill) inert stub. New helpers: `stGetEpStateIcon(ep)` (Status→label+icon: in_production→activity, review→eye, ready_to_release→check, live→radio); `stGetEpBlockingTask` (state-advancing task or "Approve episode" when Video_Status=review); `stToggleEpCard`; `stRenderEpCardTaskList` (four kinds: GCS upload / Revise_Reels link+Complete / Upload_Final_Episode Complete / release checkbox+link / generic Done). Action functions: `stCompleteEpCardTask`, `stCompleteReelRevision`, `stCompleteFinalEpisodeUpload`, `stConfirmReleaseTask`. CSS: two-column card layout + task list/row styles. Dead ref: `ep.Images_Status` in `getEpisodeBadges` — field gone; cleanup with icon spoke. **Team assignee spoke (2026-06-01):** `securityFilter` — `assignee === "team"` passes JT's filter. `applyFilters` "me" branch — includes team tasks alongside personal. **Review signal cleanup (2026-06-02):** `stGetEpFilm` repointed from Review_Reels task-existence to `Episode.Status >= in_production`. `getEpisodeBadges` — `hasReel` repointed to Status; `hasImage` removed (images pipeline retired). `getDashboardIconStates` — reels: Status-based; images: always gray. `dbTapIcon` — reels route directly from Status, no task gate. `renderEpisodeToolsCard` — Reels active from Status; Images always muted. `renderTaskButtons` — Review_Images and Review_Reels branches removed (dead code; tasks never spawn). `REVIEW_STEPS_ALL`/`REVIEW_STEPS_WITH_LINK` — Review_Reels and Review_Images removed. `stGetEpBlockingTask` BLOCKING_ORDER — Review_Reels removed. |

---

## Fairy / File Architecture — Locked

| Role | File | Notes |
|---|---|---|
| Front desk | `fairy_circle.gs` | Shared utilities + all time-triggered entry points + staging routing helpers |
| Maintenance runner | `housekeeping.gs` | Nightly utility jobs + Mending Fairy v1 (I4/I1/O1 ops live). Called by `fairy_circle.gs`. |
| Dev / test scaffolding | `dev_tools.gs` | Manual test wrappers only. Never called by production. |
| Manual / on-demand triggers | `fairy_remote.gs` | Deferred. Opens when `dwyp_app.gs` gets heavy. |

---

## AI Layer Architecture — Locked

| Layer | Name | Technology | Role |
|---|---|---|---|
| **Retrieval** | Vert | Vertex AI RAG Engine (us-south1) | Queries corpus, delivers chunks to Drive docs or Claude. Never generates. |
| **Generation** | Claude | Claude API (`callClaudeAPI()`) | Receives Vert chunks, writes all human-facing copy — show notes, captions, hooks, chat responses. |
| **Orchestration** | GAS | Apps Script | Assembles packets, routes calls, writes outputs. Never generates. |
| **Image generation** | GenGem | Gemini image API (`callGeminiImageConversational()`) | Background image generation. History-based; preserves thoughtSignature. Always Gemini — no exceptions. |
| **Guest research** | Herald | Gemini API (`callGeminiAPI()`) | Guest research, bio, Guest Brief. Always Gemini — web search is a hard requirement. |

**Single governance key:** `STUDIO_LLM_MODE = claude` — governs all Claude text generation across Studio and Publish. Code-level Gemini fallback on Claude API failure — automatic, logged to Audit_Trail, no manual intervention.

**Vert's role:** Retrieval only. Queries corpus → delivers chunks. Track A no longer routes through Vertex — `buildEpisodeIndexV2` reads injected raw transcript directly via `gatherVertContext`. Vertex remains active for: (1) live retrieval — on-demand for Write tab and index-fallback scenarios; (2) future Index Audit step (Gemini audits Claude index against Vertex corpus chunks).

**`generateWithClaude()`** — retired (deleted 2026-06-03; see Companion Recon + Teardown above).

---

## Vertex AI RAG Engine — Setup (Complete)

**GCP project:** DWYP RAG (`309883149140`)
- All OAuth, IAM, scope configuration confirmed ✅

**Corpus:** `dwyp-studio-corpus`, **us-south1 (Dallas)**, `text-embedding-005`, **Spanner** vector backend
- Resource: `projects/dwyp-rag/locations/us-south1/ragCorpora/4611686018427387904`
- `STUDIO_CORPUS_ID` confirmed ✅
- Drive connector: watches `CORPUS_DRIVE_FOLDER_ID` — configured in GCP Console ✅
- Corpus populated manually — Vert has content to query ✅

**Corpus sync — current operational state:**
- Programmatic sync via GAS: **BLOCKED.** `importRagFiles` API returns 404 in us-south1. Both v1 and v1beta1 endpoints tested. Code preserved, commented out.
- Drive connector via GCP Console: **WORKS.** Vertex Drive connector watches `CORPUS_DRIVE_FOLDER_ID` and syncs on schedule.
- **Operational path:** Filing Fairy deposits assets to `CORPUS_DRIVE_FOLDER_ID`. Audra triggers sync in GCP Console after each episode files. One manual step per episode.
- **Future reactivation:** Check us-south1 regional expansion for `importRagFiles`. Code is preserved and commented — uncomment when available.

**GCS bucket (`dwyp_corpus_episodes`):** Retired from corpus pipeline. Bucket dormant.

---

## Transcript Source — Locked

| Source | Status |
|---|---|
| DaVinci Resolve (exported) | ✅ Preferred |
| Whisper (local) | ✅ Fallback |
| Gemini auto-transcription | 🔴 Future spoke |
| Riverside transcripts | ⛔ Never — hallucination risk |

---

## Asset Folder Structure — Locked

```
02_RAW_PRODUCTION/
  EP-YYMMDD-HHmm_GuestName/             ← Raw_Folder_ID on Episodes tab
    Production_Notes.gdoc
    [raw transcript]
    [headshot files]

03_STAGING_DRAFTS/
  EP-YYMMDD-HHmm_GuestName/             ← Production_Folder_ID on Episodes tab
    manifest.json
    Episode/
      [finished transcript]
      [finished episode video]
      [finished transcript]
    Images/                              ← Images pipeline retired; folder exists, untracked
      Approved/
      Save/
      Delete/
    Thumbnails/
    Reels/                               ← syncReelAssets scans here → AL rows; reel card routes via Episode.Status (AD #93)
      Approved/
      Save/
      Delete/

04_FINISHED_EPISODES/
  EP-YYMMDD-HHmm_GuestName/             ← Filing Fairy moves here

CONTACT_LIBRARY/
  [Contact_ID]/
    Guest_Brief.gdoc
    [headshot files: *_headshot.png/jpg]

IMAGE_BACKGROUND_LIBRARY_ID/
    [Gemini-generated backgrounds]
    [Audra-curated backgrounds]

CORPUS_DRIVE_FOLDER_ID/                  ← Watched by Vertex Drive connector
    [finished transcripts]
    [Episode Cards]
    [Guest Briefs]
    [Brand Voice doc]
    [Content Sensitivity doc]
    [Ops Prompts doc — remove Image Creator section first]
    [Brand Brain doc]
```

---

## Asset Review Flow — Locked

**Images:** Arrow nav (← →), one at a time, Approve/Save with auto-advance, Submit All, untouched → Save.

**Reels:** Scroll-based, tap-to-commit via moveReviewFile(), state badges persist, Submit All. Comment button wired — tap opens inline textarea, Submit spawns Revise_Reels task for Audra.

**Runway Reminder:** Projected cue (SPOKE_Task_Projection). Resolves client-side from `Release_Date` D-7 window — no spawned task row. Ready for Release button on task card only.

---

## Task Inventory — Confirmed

| # | Task | Assignee | Trigger | Notes |
|---|---|---|---|---|
| 1 | Recording Date Reminder | — | Projected cue (demoted from spawned row) | D-1 and day-of cues from `Recording_Date`. Window math (0 ≤ diff ≤ 1). Rendered client-side by `stGetEpCues()`. `_pulse_recordingReminders` is a no-op. No task row written. |
| 2 | Guest Brief Enrich | Audra | Herald on brief completion | ✅ Wired. |
| 3 | Guest Brief Review | JT | Audra approval of #2 | Auto-closes; JT pulls from Contact Library. ✅ Wired. |
| 4 | Image Workshop Ready | JT | Filing Fairy | ✅ Built. ⚠️ Name stale — IW retired. Rename to "Studio Assets Ready" in Spoke 1. |
| 5 | Produce Episode | Audra | Filing Fairy | ✅ Built. |
| 5b | Upload Produced Episode | Audra | Spawned after Produce Episode (manual step for now) | ✅ Built (2026-05-26). Task card renders "Upload Proxy" button → browser-side resumable chunked upload to GCS. Upload success auto-calls `completeUploadEpisode` → flips `Video_Status → review` + spawns `Review_Episode`. Self-heal: re-click detects existing proxy and offers to complete without re-uploading. End-to-end test pending (2026-05-26). |
| 6 | Review Episode | JT | Upload_Produced_Episode task completion (upload success) | ✅ Built. Lifecycle-gate task: completion is the system of record for review → approved transition. Auto-completed by `requestEpisodeRevisions` when JT seals revisions (closed before `Revise_Episode` is spawned). |
| 7 | ~~Review Images~~ | — | Retired (AD #92) | Images pipeline retired. Spawn removed (Loop B deleted). UI signals repointed to Episode.Status or removed. |
| 8 | ~~Review Reels~~ | — | Retired (AD #93) | Reel card elements route directly via Episode.Status. Spawn removed (Loop C deleted). UI signals repointed to Episode.Status. |
| 9 | Revise Reels / Revise Episode | Audra | `Revise_Reels`: JT Request Revision in Reels surface. `Revise_Episode`: JT Request Revisions in Episode review. | ✅ Live. `Revise_Reels`: spawned by `requestReelRevision`; carries `Asset_ID` FK + `Payload_Link` (Drive URL of old reel). Card: link + Complete button → `completeReelRevision()` scans `Reels/` root for non-original file and delegates to `closeReelRevision`. `Revise_Episode`: card renders two buttons — "View Comments" → `showEpisodeReview(episodeUid)` (navigates to episode review surface; revision history visible); "Upload Proxy" → `startEpisodeReplace(taskId)` → `fileInput.click()` (synchronous gesture chain, bypasses confirm dialog) → `onEpisodeFileSelected` → `completeUploadEpisode`. `renderEpisodeReview` always loads unlocked; `getEpisodeRevisionHistory` response is authoritative for lock state (prevents stale client Video_Status from re-locking composer after Audra re-uploads). |
| 10 | Upload Final Episode | Audra | JT approves episode proxy → `approveEpisodeForRelease()` | ✅ Built. Card: instruction to place final file in `Staging/Episode/` + Complete button → `completeFinalEpisodeUpload()` → `Final_Episode_ID` written, `Status: ready_to_release`, Filing + Release tasks spawned. |
| 11 | Release | Audra | `completeFinalEpisodeUpload()` | ✅ Built. Card: checkbox + link to `SPOTIFY_EPISODE_BASE`. Check → `writeTaskComplete`. |
| 12 | Runway Reminder | — | Projected cue (demoted from spawned row) | D-7 through D-1 window from `Release_Date`. Window math (0 ≤ diff ≤ 7). Rendered client-side by `stGetEpCues()`. `_pulse_releaseReminders` is a no-op. No task row written. |
| 13 | Release Day Tomorrow / Release Day | — | Projected cue (demoted from spawned row) | D-1 and D-0 cues from `Release_Date`. Window math (0 ≤ diff ≤ 1). Rendered client-side by `stGetEpCues()`. No task row written. |
| 14 | Errors / Admin | Audra | Various | Urgent. Never visible to JT (filtered by assignee via `securityFilter`). |

---

## Pipeline Sequence — Locked

1. Calendar event detected → Secretary runs → folders + manifest created
2. D-1/day-of: Recording Date Reminder cue projects client-side from `Recording_Date` (no task row spawned)
3. Herald runs → Guest Brief written → Guest Brief Enrich task (Audra)
4. Audra enriches + approves → Guest Brief Review task (JT) — auto-closes
5. Audra uploads finished transcript to `Staging/Episode/`; uploads proxy mp4 to GCS `dwyp-review-playback/episodes/{EUID}/proxy.mp4` via "Upload Proxy" button on the Upload_Produced_Episode task card (browser-side resumable upload)
6. Upload success auto-fires `completeUploadEpisode` → `Video_Status → review` + Review Episode task spawned (JT)
7. Reel card elements light from Episode.Status (in_production+); reel surface accessible directly — no task gate (AD #93)
8. JT sorts assets; comments on reels → Revise_Reels tasks for Audra
9. D-7 through D-1: Runway Reminder cue projects client-side from `Release_Date` (no task row)
10. JT approves episode proxy → `approveEpisodeForRelease()` → `Video_Status: approved` + `Upload_Final_Episode` task spawned for Audra
10b. Audra places final mastered episode file in `Staging/Episode/` → taps Complete on card task → `completeFinalEpisodeUpload()` → `Final_Episode_ID` written, `Status: ready_to_release`, Filing + Release tasks spawned
10c. Audra confirms Release task (checkbox + Spotify link) → Release task complete
11. Audra manually sets `Status: live` at release (Tuesday)
12. Nightly housekeeping (Sunday after release): `archiveLiveEpisodes()` detects `Release_Date + 5 days ≤ today` → `runFilingFairy()` → Staging folder moved wholesale to Finished, `Status: archived`
13. Corpus deposit: manual — Audra drops assets to `CORPUS_DRIVE_FOLDER_ID` and triggers sync in GCP Console
14. Daily Pulse Loop D: transcript detected → Track A (`buildEpisodeIndexV2`, Claude) → Track B (`runEditorialPass`) → Track C (`materializeQuoteGraphicAssets`) → Artist Fairy handoff
15. D-1/D-0: Release Day Tomorrow / Release Day cues project client-side from `Release_Date` (no task rows)

---

## Tasks — Design (Locked, May 2026)

`renderDashboard()` entry point. Two workspaces selectable via Tasks sub-items in the left rail.

### Episodes workspace (Tasks → Episodes)

**Ordering principle:** Release date is the spine. The operator arrives with zero loaded state; only a stable, monotonic index can re-ground the view instantly. Workflow `Status` is read off the card after the index places the episode — not used as the grouping axis.

**Left column — month bands.** Episodes with a `Release_Date` group by release month. Conditional render: only months with episodes paint. Sort within band: `Release_Date` asc.

**Right column — two workspace regions:**
- **Upcoming Recordings** — `Status = upcoming` (no release date yet; recording not yet happened).
- **TBD Episodes** — `Status ≥ in_production`, `Release_Date` blank.

**Episode card (band — two-column layout):** Left: guest name / release date / blocking task (most state-advancing open task title, or "Approve episode" when `Video_Status = review`). Right: state indicator top (Episode.Status → label + Lucide icon: `in_production` → activity, `review` → eye, `ready_to_release` → check, `live` → radio); three action icon dots bottom (headphones / film / calendar). Card-body tap = expand/collapse inline task list. `Episode.Status` drives release-month vs. right-column placement.

**Upcoming Recordings side cards:** feather (quill) inert stub icon replaces mic. **TBD Episodes side cards:** calendar stub unchanged. Neither side card expands.

**Per-asset icon routing:** Lit/secondary headphones → Studio Guest → Episode sub-tab (`stSelectGuestView`). Lit film → Studio Guest → Reels sub-tab. Calendar always inert (Schedule not built). Muted icons have no handler. Icon color model (muted/red/gold truth-fix) is a deferred spoke — current build uses the existing `stGetEpHeadphones`/`stGetEpFilm` signals as-is.

**Date-cue projection (SPOKE_Task_Projection).** Recording Reminder, Runway, Release Day cues are computed on read, never stored. `stGetEpCues(ep)` projects against `Recording_Date` and `Release_Date` using window math (`0 ≤ diff ≤ N`): Recording D-1/day-of; Runway D-7 through D-1; Release Day Tomorrow D-1; Release Day D-0. Two independent axes — a `Release_Date` reschedule never moves the Recording Reminder. No `Release_Date` → cues silent. Demoted rows swept once by `clearReminderRows()`.

**Arrange mode (SPOKE_Episode_Arrange — desktop only, JT + Audra).** Toggle on the Episodes view; no new surface. Mode bar above columns: Episodes title | unsaved-changes dot | Discard | Save | Arrange button. Draft-only — drag writes nothing until Save. Tuesday cadence: `nextTuesday + (position × 7d)`. Episode number = computed slot rank (not stored; cannot diverge from date). Insert-and-shift drag; drop between cards shifts everything below one Tuesday later. Save = full rewrite of `Release_Date` for all scheduled episodes + `bumpVersion('episodes')`. Discard = silent, snaps to last-saved order (no confirm — the button press IS the deliberate action). Leave-guard warns on dirty navigation exit.

**Arrange right-column sub-regions.** Upcoming Recordings (`Status = upcoming`) — structurally excluded from drag at `stArrangeEnter`; rendered as inert cards (no `draggable`, no drag handlers). Unscheduled (`Status ≥ in_production`, `Release_Date` blank) — drag-enabled. Live/archived appear in month bands, drag-locked, never in `st.arrangeOrder`.

**Drag-back.** Dropping an episode onto the Unscheduled zone clears `Release_Date` only; episode state untouched. An episode can be `ready_to_release` with no air date — valid, card displays it. No confirm, no warning.

**Card edit mode (orthogonal to arrange draft).** Guest name correction (`updateEpisodeName`). Un-complete a mis-marked task (`uncompleteTask` — flips Status → open, clears Completed_At). Demoted cue workflow steps excluded from the completed task list (`stGetEpCues` projections have no rows to un-complete). Card edit does not enter the arrange draft.

**Arrange invariant.** No code path in Arrange removes a finished-episode file. `saveArrangeOrder` touches only `Release_Date`. See Finished-final invariant in Staging Environment section.

### Buckets workspace (Tasks → Buckets)

User-organized loose task mode. Podcast · People · Personal grouping is retired (AppSheet-era residue; `renderDashboardLoose` removed from `renderDashboard()`). Buckets is the live model.

**Three regions, one teardown-registered panel (`stBucketsView`):**
- **Left — bucket bands.** Each user-defined bucket = one band; conditional render (empty buckets absent). Standard task cards: Title / Due / Notes / Complete.
- **Top-right — Quick Tasks.** Flat per-user list; tagged `Workflow_Step = "quick"`. Enter-to-add input + checkbox. Checked → `Status = "complete"` (done-and-hidden, not deleted — Preservation Mandate).
- **Bottom-right — Capture form.** Title → Assignee (defaults to self; other users available for cross-assignment) → Bucket dropdown → Due → Notes → Create Task. Voice-keyboard compatible on mobile.

**Bucket source of truth:** User Registry tab, `Buckets` column (comma-delimited list) and `Default_Bucket` column (single value). Both are Audra hand-edits; system never supplies bucket values. No fallback defaults.

**Cross-assignment:** Assignee ≠ creator → task routes to assignee's `Default_Bucket`; creator's chosen bucket is ignored. Blocks with toast if assignee has no default set. `bumpVersion("tasks")` fires on all task-write paths.

**Episode tasks — absent function, next focused build.** Episodes are viewable in the workspace but have no task surface yet. Sketch: card-expand reveals the episode's task list; each task has a route. Own fresh thread; needs a short design beat first.

### Contacts Tab (live)
See Contacts Tab section below.

### Episode Detail
All production-forward tasks visible to both. Errors excluded from JT view. Quick Links: Studio / Drive Folder. (Image Workshop and NotebookLM links retired — Spoke 1 removes them.)

---

## Contacts Tab — Design (Locked, Spoke 2 Complete)

**List View:** Display_Name, Organization, Relationship_Type, completion dot. Search by name/org/tag/relationship. Sorted by Last_Activity desc.

**Detail View:** All fields, graceful empties. Social links as tappable icons. Tags + Research Note editable with debounced autosave. EH toggle writes `Influence_Tier = "EH"`. Headshot if URL populated.

**Write paths:** Tags (debounced), Personal_Note (debounced), Influence_Tier (EH toggle only).

**Not in scope:** New contact creation (Secretary owns), core field edits, Herald from Contacts, CRM automation.

**Open Questions:**
| Question | Notes |
|---|---|
| Completion signal criteria | Bio + at least one social + Headshot? Not formally defined. |
| Relationship_Type editable? | Needed if guests become sponsors. Part of C-1. |

---

## Studio — Design (Locked May 2026)

**Architectural details:** `DWYP_Platform_Reference.md` § AI Layer Architecture (Episode Index, retrieval strategy), § Schema (Posting_Schedule), and `DWYP_Operating_Model.md` § 8 (Companion Model). Platform State carries overview only.

### What It Is
JT's NotebookLM replacement. Corpus-powered creative surface. Bottom nav tab (replaces NotebookLM link). Not a pipeline step. Image Workshop fully retired — Design canvas replaces it.

### Navigation Structure

```
[GUEST NAME]
    Images
    Reels
    Episode
    Schedule

[GUEST NAME]
    ...

WRITE
    Brainstorm

TASKS
    Buckets
    Episodes    ← app entry / home
```

| Surface | Status | Notes |
|---|---|---|
| **Guest → Images** | 🔶 Active | Canvas: `st.fabricCanvas` ring-fenced. H&Q chip drop, background library/generate, Export PNG + `.txt`. **Caption and Title Card live on the center canvas — not rail panels.** Text scaling (A+/A− + normalize-on-scale). Color picker (brand + complementary rows). Export PNG fix (`multiplier:2` removed). CP3+4 deferred. Right panel: pinned icon rail — H&Q / Backgrounds / AI Chat panes (AI Chat inert pending wiring spoke). Session memory + first-touch default H&Q. Derivative mode: H&Q hidden, rail opens on Backgrounds. |
| **Guest → Reels** | 🔶 Active, cosmetic pass pending | Drive `/preview` player, `Caption_Host` wired, Generate caption, Export, Request Revision. |
| **Guest → Episode** | ✅ Live | Native `<video>` proxy player, timestamped comments, Request Revisions, Approve. View split: `#stEpVideoWrap` (top, regenerated per load) + `#stEpShowNotesWrap` (bottom, show-notes card editor; vertical drag handle to resize). Right panel: Revision Comments · AI Chat tabs. |
| **Show Notes (in Episode)** | ✅ Live | Cards rendered in `#stEpShowNotesWrap` from Track B Google Doc (`manifest.show_notes`). Standard prose cards (`contenteditable`); Hooks container (add/delete items); Guest Quotes container (quote text + attribution fields). Whole-surface Save: floating Undo Changes / Save bar scoped to notes pane; dirty indicator; leave-guard matching Arrange. Serializer writes parser-valid format (hooks: `N. text` numbered-list per template; quotes: `QUOTE N: [text]\nATTRIBUTION: [name]`); Track B/C GAS parsers unchanged. Postfix fixes (2026-06-08): hook serializer format corrected (was emitting `HOOK N:`, not `N. text`); client-side hooks parser, card collapse, layout floor, section filter patched. Deferred: `show_notes_locked` flag; Send hook/quote to Images gesture; JT Copy doc fairy. |
| **Guest → Schedule** | 🔶 Active, Export All untested | Pool = `Status='schedule'` AL rows for episode; placed assets badged + sunk (`Availability='placed'`). Workspace toggles The Week ↔ Swipe Package. The Week template-driven from `Posting_Schedule`. Drag pool → slot; drag slot card → pool/another slot. Swipe = free-pick bag, dedup enforced. Send to Schedule live on Images (canvas upsert + thumbnail render to `Schedule_Renders/`) and Reels (status-flip). `edit ↗` round-trips to Images (canvas restored) or Reels. Export All code complete (untested — needs full week of placed assets). **Open:** aesthetic polish deferred; `edit ↗` back-button parked for Hub. Companion = Pass 3 stub. |
| **Write → Brainstorm** | ⏳ Not built | Global, non-episode-scoped. AI Chat: Research (Gemini + Vert) / Polish (Claude) toggle. Doc-generation pattern: AI output → new Drive doc in JT's Drive; JT switches center pane to it. Desktop continuation of Write Lite (Write Lite saves Drive doc; Brainstorm reads those docs in Docs picker). Replaces standalone Ideas tab. |
| **Tasks → Episodes** | ✅ Live | App entry/home. Episode-organized task view. Arrange mode: in-memory draft reorder with Tuesday cadence, insert-and-shift drag, leave-guard. Date cues (Recording Reminder / Runway / Release Day) projected client-side — no task rows. |
| **Tasks → Buckets** | ⏳ TBD spec | Bucket organization pattern (Erin's concept). Internal structure needs Hub session before implementation. |
| **Write → Outreach** | ⏳ Future | Guest comms. Scribe template dependency — not ready to design. |

### Current Structure Notes (Rail Remodel Pass 1 / 1.5 / 1.6)

**Left rail — guest accordions.** Guest names are accordion headers; one guest expanded at a time (expanding one collapses the rest). Expanded guest shows sub-items **Images · Reels · Episode · Schedule** (no Show Notes — see above). Active guest is expanded on render. Write (Brainstorm) and Tasks (Buckets, Episodes) are peer root items with the same accordion treatment. Selection styling: red→gold gradient bar on the expanded guest header (`#d12026` solid to ~65%, then → `#faae17` tail); active sub-item is solid gold text; white left-aligned text held over the red zone for contrast.

**Caption and Title Card** live in the center workspace — not rail panels. Element IDs preserved so reel-select population still works.

**Episode view — two containers.** `#stEpView` is split into `#stEpVideoWrap` (top; regenerated on each episode load) and `#stEpShowNotesWrap` (bottom; show-notes card editor). A vertical drag handle between them lets JT favor the video or the notes. The lower wrap renders editable cards from the Track B Google Doc; standard prose + special Hooks/Quotes container cards; floating Undo Changes / Save bar scoped to the notes pane. Episode right panel shows **Revision Comments · AI Chat** tabs.

**Right rail — icon rail (Images); text-tabs (Reels/Schedule); push-drawer (Episode).** Images: pinned icon strip (Lucide: quote / image / message-circle) rightmost in `stAssetsCol`; panel opens inboard (pushes workspace). State: `railState.images.openPane` drives both panel and icon `.active` — no desync. Session memory + first-touch default (H&Q). Reels and Schedule: old text-tab bars (`stReelRailTabBar`, `schRailTabBar`) still active — icon rail HTML strips and `railPaneMeta` entries are in place but `enterIconRailMode` is not yet called for those surfaces; migration pending. Episode: `stEpDrawerIcon` column-collapse mechanism still active (not yet migrated to pinned icon strip); X button (`ep-drawer-close`) still present on companion drawer. AI Chat is an inert pane on all surfaces pending the AI wiring spoke.

### AI Companion (Phase 4)
Per-asset chat with Claude in the AI Chat rail icon. Conversation history per asset in Asset_Library. Companion scope = asset-in-focus only (Design–Schedule split means no same-date sibling injection). Chip suggestions never auto-write JT's draft. Full spec in `DWYP_Operating_Model.md` § 8 (Companion Model). (Prior reference to `docs/DWYP_Publish_Feature_Patterns.md` removed — file does not exist.) Build is Phase 4 in the playbook.

### Episode Navigation
- **Nav tab → Studio:** auto-selects via `stSelectGuest()` on data-load.
- **Episode card → Studio:** episode UID passed as context payload — lands directly in that episode.
- **Mid-session episode switch:** safe — all canvas state persists to Asset Library; all docs save to My Docs.

### Retired
- Publish tab — retired May 2026. All `pb*` code removed.
- Design as standalone root surface — retired Hub May 2026. Canvas access now via guest → Images and guest → Reels within guest nav.
- Left-center chat panel (`#stChatCol`) — **retired** (Rail Remodel Pass 1). Dissolved entirely. Chat removed; will be rebuilt later as the AI Chat rail icon (AI wiring spoke). The segmented Images/Reels/Episode toggle (`#stSegRow`) that lived inside it is also gone — view-switching is now driven by left-rail sub-items calling `stSetDesignTab`.
- `#stSegRow` (Images/Reels/Episode segmented toggle) — retired with `#stChatCol`; view-switching now driven by left-rail guest sub-items.
- Mode list (seven modes: Show Notes, Episode Copy, Interview Prep, Social Media, Newsletter, Outreach, Brainstorm) — retired. Surfaces now organized by guest nav sub-items + Write/Tasks root items.
- Ideas tab — retired. Brainstorm (Write → Brainstorm) replaces it.
- Starred — retired. My Docs + Asset Library persistence covers it.
- "Librarian Vert" and "Social Vert" personas — retired. Claude introduces itself as Claude.
- Image Workshop — fully retired. No bones carried forward.

---

## UI Design Workflow

**Rule: values, not adjectives.** Audra composes the target look in Figma (visual builder + imported modern UI kit). The handoff artifact is a screenshot plus exact values from Figma Dev Mode — color (hex), spacing (px), font, radius. Code implements the values directly; descriptive language ("make it feel warmer") is not a valid handoff.

**Scope:** App chrome only — all HTML/CSS surfaces (panels, buttons, layout, type). Excludes Studio canvas composition (Fabric.js quote-graphic layout: `stDropText`, `Textbox` positions), which is JS-driven and tuned via JS params — a separate mechanism.

**Optional:** Full HTML/CSS markup exportable from Figma via Anima plugin when a layout needs structure, not just styling values.

---

## Background Generator — Locked (Fully Patched)

- **Model:** `gemini-2.5-flash-image`
- **Stateless:** Canvas background IS the context. No history.
- **Auto-save:** `IMAGE_BACKGROUND_LIBRARY_ID` as `bg_[slug]_YYMMDD-HHMM.ext`
- **Used in:** Studio Design tab right rail

---

## Image Workshop — Retired

Image Workshop is fully retired. Replaced by the Design canvas in Studio. No bones carried forward. Social Vert retired with it. Code preserved per spring-clean decision — dead code removed in Spoke 1.

---

## Manifest Schema — Current

| Field | Writer | Notes |
|---|---|---|
| `episode_uid` | Secretary | |
| `contact_id` | Secretary | |
| `guest_name` | Secretary | |
| `recording_date` | Secretary | |
| `raw_folder_id` | Secretary | |
| `staging_folder_id` | Secretary | |
| `status` | Various | |
| `phase` | Various | |
| `created_at` | Secretary | |
| `herald_form_data` | Secretary | |
| `identity_pending` | Herald | Set true on Enrichment Pending path |
| `raw_hooks` | housekeeping.gs | Vestigial — Social Vert retired. Remove in Spoke 1. |
| `raw_quotes` | housekeeping.gs | Vestigial — Social Vert retired. Remove in Spoke 1. |
| `image_prompts` | housekeeping.gs | Vestigial |
| `show_notes` | Vert Fairy (`runEditorialPass`) | Drive file ID of Show Notes doc. Track B. |
| `episode_index` | — | Drive doc ID of Episode Index doc (v1). `createEpisodeIndexDoc()` deleted. Field vestigial — no writer, no reader in active code. Remove in housekeeping spoke. |
| `episode_index_v2` | Vert Fairy (`buildEpisodeIndexV2`) | Drive file ID of Episode Index v2 doc. Track A. |
| `quote_graphic_assets_built` | Bridge Fairy (`materializeQuoteGraphicAssets`) | Boolean. Set true after Track C writes Asset_Library rows. Track C. |
| `quote_graphic_asset_count` | Bridge Fairy (`materializeQuoteGraphicAssets`) | Integer. Total Asset_Library rows written (hooks + quotes). Track C. |
| `mend_ledger` | Mending Fairy (`runMendingFairy`) | Object map: `issueKey → { attempts: Number, lastAttempt: ISO, status: 'open' \| 'healed' \| 'task_spawned' }`. Issue key formats: `'I4:{pngDriveFileId}'`, `'I1:{fieldName}'`, `'O1:{contactId}'`. Written by `_mendUpdate`; committed as single `patchManifest` call at run-end. `task_spawned` prevents nightly re-spawn; `healed` marks successful self-repair. |
| `revision_cycle` | `requestEpisodeRevisions` (finalize) + `completeEpisodeRevision` (re-upload) | Integer ≥ 1. Current video version = the round new comments are tagged with. Lazy default 1 when absent. Re-upload bumps it (+1); finalize re-writes the same value alongside the flag. Read via `_readRevisionCycleState`. Revision Conversation v1. |
| `revision_finalized` | `requestEpisodeRevisions` (true) + `completeEpisodeRevision` (false) | Boolean. JT has committed the current cycle's comment set. `true` hard-blocks new top-level comments + withdraw (`submitEpisodeCommentRow` / `setEpisodeCommentStatus` read it); re-upload resets to `false`. Lazy default `false`. Revision Conversation v1. |

---

## Build Sequence

### ✅ Complete

| Item | Description | Notes |
|---|---|---|
| 1–84 | Master Sheet build through Reels Surface inline player | See git history for detail |
| 85 | Design Foundation Session — five foundation docs | Surface, Performance, Publish AI, Build Playbook, PreFlight |
| 86 | Phase 1.1 — Versions Tab | 11 domain rows in production sheet |
| 87 | Phase 1.2 — `bumpVersion()` + version endpoints | LockService + audit_trail recursion guard |
| 88 | Phase 1.3 — Endpoint retrofit | 40 write paths across 4 files |
| 89 | Phase 1.4 — Frontend version-aware loader | `getDomainsBatch()`, three-bucket domain model |
| 90 | Phase 1.5 — Tasks screen migration | Version-aware cold start; tab return = 0–1 batch calls |
| 91 | v3 Center Canvas — cosmetic pass | Left rail, reel card, image card, urgency fix |
| Track C | Bridge — `materializeQuoteGraphicAssets` | Vert → Claude → Bridge pipeline closed; verified on David Bedrick |
| 92 | v3 Wiring Phases 1–2 | Dual-JSON canvas, Export button, viewport fix, per-asset storage |
| Pipeline Rewire | Track A/B/C Pipeline Rewire | `buildEpisodeIndexV2` rewritten (Claude, extract-not-interpret); Loop D rewired (two-condition A→B); `runVertFairy` + all show-notes + Vertex helpers deleted; `dev_tools.js` cleaned; `runVertFairyForEpisode` repointed |
| Episode Upload Affordance | Browser-side resumable upload + auto-handoff to Review | Confirmed 2026-05-26 on 3-sec and 60-min+ videos. Playback, comments, and auto-handoff all live. |
| Rail Remodel Pass 1 / 1.5 / 1.6 | Left-rail guest accordion + sub-item nav + Episode view split | Guest accordion (one open, red→gold gradient), sub-items Images·Reels·Episode·Schedule, stSetDesignTab wiring, Episode view split (#stEpVideoWrap + drag + #stEpShowNotesWrap inert), right-rail AI Chat inert stubs, Schedule stub. |
| Document Provenance | PROPOSAL_ / SPOKE_ prefix scheme added to CLAUDE.md | Canon is unprefixed. Build_Playbook rename/exception open. |
| Nav Panel Teardown Fix | Studio: panel leak + double-active nav corrected | `stHideAllPanels()` hides all 11 content/assets panels; called first in `stSetDesignTab`, `stAccSelectTaskView`, `stShowScheduleStub`, `stAccSelectMode`. `stSyncSubItemActive` clears Tasks items on Guest activate. `stAccSelectTaskView` clears Guest sub-items on Tasks activate. `stEpView` container mount confirmed correctly scoped inside `stContentCol` (not hoisted). `stSwitchTab` is a known exception site — sets panels via `isImg` ternary but always delegates to a teardown-owning function (design → `stSetDesignTab`; write path enters through `stAccSelectMode`); no independent leak risk. |
| SPOKE_Tasks_1 | Episode State — five-state Status model | `upcoming`→`in_production`→`ready_to_release`→`archived`. `archived` is the sole terminal gate (replaces `complete`). Creation init: `upcoming`. Loop D flip: `in_production` on transcript detect. `triggerReadyForRelease()` flip: `ready_to_release`. All 6 active dailyPulse loops: `complete`→`archived`. `getEpisodes()` + `getActiveEpisodes()` + secretary calendar + herald prior filter: all updated. Housekeeping: processes `in_production` + `ready_to_release` only. `EPISODES_COLS`: `Frameio_Project_ID` + `Guest_Package_URL` columns retired. `Images_Status` column left in place (inert — no write path; Filing redesign will resolve). Dead writer `filing_fairy.js:301` (`Status:"complete"`) parked for Filing redesign. **✅ Enum complete:** Episodes sheet Status enum = `upcoming` / `in_production` / `review` / `ready_to_release` / `live` / `archived`. |
| SPOKE_Tasks_2 | Episodes workspace — month calendar + two workspace regions | Left column: release-month bands (conditional render, `Release_Date` as sort key). Right column: Upcoming Recordings (`Status = upcoming`) + TBD Episodes (`Status ≥ in_production`, no release date). Episode card: action label + state line + per-asset icons (placeholder — icon model deferred). `stRenderEpisodesView()` + `stRenderEpBandCard()` + side-card renderers. `stEpisodesView` panel registered in `stHideAllPanels()`. |
| SPOKE_Tasks_3 | Buckets workspace — three-region task mode | `stBucketsView` panel; bucket bands (left), Quick Tasks (top-right), Capture form (bottom-right). Buckets sourced from User Registry `Buckets` + `Default_Bucket` columns (Audra hand-edits; no system defaults). Cross-user assignment routes to assignee's `Default_Bucket`. `Workflow_Step = "quick"` tags stickies. `Tasks.Bucket` col 18 added to `TASKS_COLS`, `getTasks()`, `createTask()`, `spawnTask()`. `renderDashboardLoose` retired from `renderDashboard()` (Podcast/People/Personal gone). **Audra hand-edits required:** User Registry — add `Buckets` + `Default_Bucket` columns and populate. Tasks tab — add `Bucket` header in col R (18). |
| SPOKE_Episode_Tasks_1 | Episodes workspace — episode task surface + release flow | Band card: two-column layout (left: name/dateline/blocking task; right: state+icon/action dots), card-body tap = expand/collapse inline task list, lit icon routing to Studio sub-tabs. Upcoming side card: quill (feather) replaces mic. State icon map: in_production→activity, review→eye, ready_to_release→check, live→radio. Task kinds on card: GCS upload (Produce/Upload_Produced/Revise_Episode via `startEpisodeUpload`), Revise_Reels (Drive link + Complete → `completeReelRevision`), Upload_Final_Episode (Complete → `completeFinalEpisodeUpload`), release (checkbox + Spotify link). `approveEpisodeForRelease` replaces direct `writeVideoStatus` call on JT approval. `completeFinalEpisodeUpload`: scans Episode/ folder, writes `Final_Episode_ID`, flips Status→ready_to_release, spawns Filing + Release tasks. `EPISODES_COLS.Images_Status` → `Final_Episode_ID` (col 12). `SPOTIFY_EPISODE_BASE` governance key populated. `live` added as recognized Episode.Status. RFR cleanup complete: `triggerReadyForRelease`, `checkReadyForRelease`, RFR modal, `.btn-ready-for-release` CSS deleted. Testing in progress (2026-05-31). |
| SPOKE_Orchestrator_Pulse | State-driven pulse — replace loop stack with lifecycle orchestrator | `dailyPulse()` rewritten: single entry point, 6-stage lifecycle, no per-pulse throttle. Loops 1/2/3/B/C/D retired; Loops 3 + B deleted outright. Content chain (A→B→C) chained-within-pulse with per-stage try/catch + alert task. Reel chain independent (syncReelAssets → runReelEditorialPass). Stage 3 backup final-video detect (dual-trigger with UI Complete button; `Final_Episode_ID empty` gate makes them mutually exclusive). `runArtistFairy` deleted (Track C owns). `dev_tools.js` rewritten in lifecycle order. Full 6-state model: `upcoming → in_production → review → ready_to_release → live → archived`. ✅ Enum includes `review`. |
| SPOKE_Pulse_Throttle | Per-run heavy-pass budget + per-stage enable flags | **Throttle:** `PULSE_HEAVY_PASS_BUDGET` governance key (default 2 if absent). `_pulse_contentChain` accepts `heavyBudgetRemaining` param and returns count of heavy passes fired; `dailyPulse` accumulates with `heavyUsed`. Track A (`buildEpisodeIndexV2`) + Track B (`runEditorialPass`) are counted; if budget exhausted before a pass is needed, the pass is deferred (audit-logged) and the episode is picked up next run — idempotent by existing per-episode conditions. Track C (`materializeQuoteGraphicAssets`) is not counted and always runs when its condition is met. Reel chain is not counted. When Track C is auto-triggered it will join the budget — flagged for that spoke. Stage dispatch in `dailyPulse` confirms that `_pulse_contentChain` is only called for `upcoming` (transcript-detect path) and `in_production` — never for `review` or beyond. **Enable flags (opt-in, blank/absent/FALSE = no-op):** `PULSE_CONTENT_ENABLED`, `PULSE_REELS_ENABLED`, `PULSE_RECORDING_REMINDERS_ENABLED`, `PULSE_RELEASE_REMINDERS_ENABLED`. Each is the first check in its corresponding stage helper; checked via case-insensitive `=== "TRUE"`. No master `PULSE_ENABLED` key — the time trigger is the master switch. **Governance keys populated (2026-06-01):** All five keys added to Governance_Config. `PULSE_HEAVY_PASS_BUDGET = 2`. All four enable flags set to `FALSE` — stages are dark while trigger is verified. New daily pulse trigger created. Bring stages online by flipping each flag to `TRUE` individually. |
| Verify_Index_Wiring | Index stage wiring verification (report-only) | `buildEpisodeIndexV2` confirmed wired and reachable via `in_production` path. Kill switch (`PULSE_CONTENT_ENABLED`) confirmed `FALSE` — chain dark. Throttle (`PULSE_HEAVY_PASS_BUDGET = 2`) is per-run across all episodes; Track A can be deferred (not blocked) when earlier episodes consume budget. Idempotency gate confirmed two-layer (manifest field + Drive file-presence self-repair). Template precondition open: revised `# AI Search Index` section must be pasted into Master Template before chain goes live. No code changed. |
| Studio Version-Aware Cache | Session-scoped read-path cache for all four Studio surfaces (2026-06-07) | `st._cache` store + 4 helpers. Five loaders gated by domain version: Episode (stream URL UID-only; revisions `episodes`), Reels (`asset_library`), Images H&Q (`manifests`), Images BG (`image_library`), Schedule (`asset_library` + `episodes`). 10 write-path `_cacheInvalidate` calls. `stSyncFlush()` + Sync button in avatar menu. CP0 corrected domain for Images H&Q from `asset_library` → `manifests`. Pre-existing `st.hqContent` episode-key bug fixed. `st._lastEpLoaded` parked fix superseded by cache-hit guard. |
| SPOKE_Task_Projection | Date-reminders demoted to projected cues (2026-06-07) | `_pulse_recordingReminders` + `_pulse_releaseReminders` gutted to no-ops. `clearReminderRows()` added to `fairy_circle.js` — one-time idempotent sweep (Recording_Reminder, Release_Reminder, Runway, Release_Day; skips `complete` rows). `test_clearReminderRows()` added to `dev_tools.js`. Client `stGetEpCues(ep)` projects on read: window math (0 ≤ diff ≤ N), two axes (Recording_Date D-1/day-of; Release_Date D-7 Runway, D-1 Release Day Tomorrow, D-0 Release Day). State-advancing tasks remain spawned rows (Preservation Mandate). |
| SPOKE_Episode_Arrange | Arrange mode on Tasks → Episodes, desktop, JT+Audra (2026-06-07) | In-memory draft; Tuesday cadence (`nextTuesdayBase + position×7d`); episode number = computed slot rank. Insert-and-shift drag; drag-back clears `Release_Date` only (state untouched). `saveArrangeOrder()` full rewrite + `bumpVersion('episodes')`. Upcoming episodes structurally excluded from drag at `stArrangeEnter`; live/archived locked. Discard silent; leave-guard on dirty nav exit. Card edit (orthogonal): guest name (`updateEpisodeName`) + un-complete mis-marked task (`uncompleteTask`). `getEpisodeCompletedTasks()` lazy-loaded on card expand. Save writes dates; no cascade (projection live). **Hanging:** arrange drop-zone `data-idx` overlap between contiguous month bands — visual-only highlight glitch on hover; insert position correct. Deferred. |
| SPOKE_Show_Notes_Card_Editor | Show Notes card editor in Episode surface (2026-06-08) | GAS: `getShowNotesForEdit(episodeUid)` — reads Track B Google Doc, parses preamble + sections (standard / hooks / quotes), returns structured JSON. `saveShowNotes(episodeUid, sections, preamble)` — full-body rewrite of doc in parser-valid format; first preamble line as H1; hooks as `N. text` numbered list; `QUOTE N: [text]\nATTRIBUTION: [name]`; Track B/C GAS parsers unchanged. Client: `epNotes` state object; `epNotesLoad` fires on every episode switch; `epNotesRender` builds cards (standard `contenteditable` prose, Hooks add/delete list, Quotes add/delete list with quote text + attribution fields); `input` listeners keep in-memory state in sync; `epNotesDirtySet` shows/hides floating Undo Changes / Save bar + dirty dot (scoped to `#stEpShowNotesWrap` flex column, never window-pinned); `epNotesLeaveCheck` leave-guard wired into all 4 navigation guards (after `stArrangeLeaveCheck`). Rider: Arrange toolbar `Discard` → `Undo Changes`. Companion workspace state updated from `#stEpShowNotesBox.innerText` → `epNotesBuildText()`. Deferred: `show_notes_locked` flag; Send hook/quote to Images gesture; JT Copy doc fairy; AI Chat/Hook/Quote/Caption submission rail. **Postfix fixes (2026-06-08):** hook serializer was emitting `HOOK N: text` (parser format) instead of `N. text` (Track B template format) — corrected; client hooks parser updated to match; card collapse, layout floor, and section-filter bugs patched. |
| SPOKE_Icon_Rail_Half_Height_Fix | Images rail half-height anomaly — one-line fix (2026-06-08) | Dropped stale `else` branch in `stSetDesignTab` that showed `stEpBrowser` for all non-Episode tabs (Images, Reels). Root cause: `stImgAssets` and `stEpBrowser` both have `flex:1` inside `stAssetsCol` (flex-column) — showing both split the column 50/50; bottom half was stale Episode companion chat content. Fix: `if (isEp) { epDrawerEnterMode(); }` — `else` removed; `stEpBrowser` is only shown by `epDrawerEnterMode()` in Episode mode. |
| SPOKE_Freeform_Description_Editable | Episode Show Notes — Description + Bullets folded into one editable region (2026-06-09) | `getShowNotesForEdit` (dwyp_app.js ~3593): parse-time fold — Insight Bullets content appended to Episode Description section (blank-line separated); `INSIGHT BULLETS:` header consumed and dropped from `rawSections`. `epNotesRender` (dwyp_ui.html) simplified: merged display-only card path and `skipSi` pre-pass removed; Description routes through standard `contenteditable` prose branch. `saveShowNotes` unchanged — writes single section under `EPISODE DESCRIPTION:` anchor. **`EPISODE DESCRIPTION:` is the sole surviving anchor** (parser dependency — all-caps, colon, own line, always written). `INSIGHT BULLETS:` is no longer parsed, rendered, or emitted. Verified in-app and confirmed in raw Track B Google Doc: one anchor, zero `INSIGHT BULLETS:` lines. Hooks/Quotes editing regression-checked unaffected. |
| SPOKE_IconRail_Migration + SPOKE_IconRail_Correction + SPOKE_IconRail_State_Behavior | Icon rail built for Images surface; full state behavior across all three wired surfaces (2026-06-09) | **Pattern (all rail-mode surfaces):** `[ center workspace ][ panel — open only ][ icon strip — pinned right, always visible ]`. Rail never moves; panel slides open inboard (pushes workspace, not overlay). No X button — active icon is close affordance. **CSS:** `.st-icon-strip` (36px, `height:100%`, `justify-content:center`, `border-left`); `.st-icon-btn` gray/gold-hover/gold-filled-active; red never on rail. **Icons (Lucide 16px):** H&Q = `quote`, Backgrounds = `image`, AI Chat = `message-circle` (identical on all surfaces), Reels Browser = `film`, Pool = `layout-grid`. **Images surface fully wired:** `stRailIconsImages` strip (rightmost child of `stAssetsCol`); `stSetRailTab` thin wrapper on `setRailPane('images', pane)`; derivative-mode redirect (H&Q → Backgrounds) preserved. **State model (`railState[surface]`):** `{ openPane: <paneKey>|null, openWidth, touched }` — single source of truth for both panel display and icon `.active` class; cannot desync. `exitIconRailMode` preserves state (session memory; forgotten on reload). `enterIconRailMode` restores on return (open or closed as left). **Default-open first touch:** Images → H&Q, Reels → Browser, Schedule → Pool, **Episode → closed** (companion optional). Remembered state beats default; remembered-closed honored. **Interaction:** closed+click=open; open+same=close; open+different=swap (no animation). **Functions:** `enterIconRailMode(surface)`, `exitIconRailMode(surface)`, `setRailPane(surface, pane)`, `_railShowPane`, `_railHideAllPanes`, `_railSetActiveIcon`; `railPaneMeta` lookup table for all three surfaces. `stHideAllPanels` + `stSwitchTab` else-branch call `exitIconRailMode`. **HANGING — Reels, Schedule, Episode not yet migrated.** Old text-tab bars (`stReelRailTabBar`, `schRailTabBar`) still active for Reels/Schedule. `stSetReelRailTab`/`schSetRailTab` still use old mechanisms. Episode companion still uses `stEpDrawerIcon` column-collapse + X button (`ep-drawer-close`). HTML strips and `railPaneMeta` entries exist for all four surfaces; `enterIconRailMode` called only for Images in `stSetDesignTab`. |
| SPOKE_Mending_Fairy_v1 | Mending Fairy — loop scaffold + v1 ops (I4/I1/O1) | `runMendingFairy` wired into nightly housekeeping. I4: auto-write missing `.txt` caption sidecars for exported PNGs (T1, backoff). I1: manifest-to-Episodes reconcile for `guest_name`/`contact_id` (fill-when-absent backoff; conflict → immediate task). O1: Herald bio enrichment on empty/sentinel Bio_Summary (T2 fill-only, backoff; no signals → immediate task). `mend_ledger` in manifest. `_mendDecide`/`_mendUpdate` backoff helpers. `test_runMendingFairy` in dev_tools. **Unvalidated — validate on a real episode before trusting nightly run output.** |
| SPOKE_Schedule_1–3 | Schedule surface — pool, placement, send verbs, Export All | **Spoke 1 (2026-06-02):** Surface scaffolding. Left-rail guest sub-item. `getScheduleData`, `placeAssetSchedule`, `removeAssetFromSchedule`. Pool = `Status='schedule'` AL rows. Drag-to-slot week placement + swipe placement. Optimistic UI. **Spoke 2 (2026-06-02):** Send to Schedule verbs on Images and Reels surfaces. Images: `sendImageToSchedule` — AL row upsert at `Status:'schedule'`, PNG thumbnail rendered via `canvas.toDataURL()` and written to `Staging/[ep]/Schedule_Renders/`, `Drive_File_ID` stored on row. Reels: `sendReelToSchedule` — status-flip of existing AL row to `'schedule'`. **Spoke 3 (2026-06-02 — code complete, untested):** `exportAllSchedule` — assembles hand-off package in `Staging/[GuestName]/`. Day folders (non-empty only) + SWIPE folder. Images: PNG copied from Schedule_Renders + Caption_Host `.txt`. Reels: `.txt` with title + caption (Caption_Host week / Caption_Guest SWIPE) + Drive link. Clear-and-rebuild re-run strategy. `schExportAll()` frontend handler opens folder on success. **Needs a full week of placed assets to test.** |

### ⏳ In Progress
- **v3 Wiring spoke (item 92)** — Phase 1 ✅ confirmed. Phase 2 ✅ shipped. Phase 3 (reel card expand) next.

### 🔜 Soon

- **Rail Remodel** — next left-rail pass.
- **Schedule companion — Pass 4 of 4** — AI Chat for Schedule surface. Prerequisite (Pass 3) complete. Build from scratch; no existing footprint on Schedule surface.

### Queued — Next

**Phase 1 — Performance Foundation**
✅ 1.1–1.5 Complete (see Build Sequence table above).
1.6 Blurhash thumbnails — **parked** (deprioritised; not blocking anything).
1.7 Pre-compute audit — identify >200ms operations.

**Phase 2 — Design System (Hub-led, Audra)**
✅ 2.0 Action-Completeness Audit — Closed. Five sessions, saturation marker S5. Output: `DWYP_App_Structure.md` v1.3 + `DWYP_User_Flows.md` v1.0. Phase 2.1 / 2.3 / 2.4 / 3.3 hub sessions unblocked.
2.1 Component library design.
2.2 Status indicator component (first-class component for save/saved/failed).
2.3 Mobile IA design.
2.4 Desktop chrome conventions.

**Reels Surface push** — Verbs shipped (2026-05-24): export move, generate caption, edit with vids, request revision, §4 atomic close. Cosmetic/UI polish pass pending.

**Spoke 8 — Daily Pulse audio** *(after Phase 1)*
- Reel upload detected → audio extraction → transcription → episode index reel descriptions.
- Quick Caption path via Studio (not standalone).
- `fairy_circle.js`, `dwyp_app.js`.

**Other queued (not Studio):**
- Runway Reminder spoke — Daily Pulse D-7, idempotent.
- F-7 timezone — wire `JT_TIMEZONE` / `AUDRA_TIMEZONE` into recording reminder logic.
- F-9 — Tasks tab nav entry.
- getTasks() header-driven fix.
- Review_Images close path.
- C-3 — Re-run Herald button (Audra-only, Contact Detail).
- C-4 — Influence Tier three-way toggle (EH / HI / LF).
- C-2 — Revision Task inline checkboxes.
- C-1 — Contacts Add/Edit (awaiting JT feedback).
- F-2 — Reels Viewport Fit (iPhone SE) (pending device confirmation).

**Parked — Hub Backlog (Item 92 Phase 2 session):**
- Stub → real card swap on first paint: render skeleton card frames (no text, no bg) until `_pbPrefetchAssets` resolves, then single swap to real content. Skeleton-first preferred over block-until-ready — also closes orphaned-Card-1 race (click before prefetch returns → stub ID → hydration bails) as a side effect.
- AL row as single source of truth across surfaces: every surface displaying asset content (stack card thumbnail text/bg, caption draft chip, details panel quote text) should read from AL row, not derived or cached copies. Requires audit-first spoke (map all surfaces + current vs. should-be sources), then patch spoke. Open design decisions for hub: overwrite `Quote_Text` on edit or new `Display_Text` column? Write to AL on save or derive from Canvas_State on every read? Does caption draft regenerate against edited text or stay locked to original?

**Parked — 2026-06-09 session:**
- Social posting + analytics leg — design session pending. Tooling decision locked (prior Hub session): Outstand unified social API over direct platform calls and over Make (Make never enters the stack; hybrid escape hatch = direct native call per platform if metric depth proves insufficient). Orientation brief lives in Google Drive: "Outstand — Hub Orientation Brief" (verified June 2026; carries VERIFY-LIVE checklist — TikTok app-audit coverage, IG Reels metric population, LinkedIn analytics scope, server-side `scheduledAt` across all five platforms, `platform_specific` metric depth — all must be tested on a live account before design locks). Build order: dumb timed handoff first (function), strategy-driven timing second (enhancement). The LLM scoring pass may be the same wedge as the parked Schedule companion strategy-source — check before treating as separate.
- Template excavation ("souls") — standalone idea capture, full doc lives in Google Drive: "Excavation: Finding the Souls on the Far Side of Complexity." Three-layer model (soul/script/plumbing); thesis: worker identities are latent in the Master Template, excavate via co-occurrence → repetition → read, evidence first. No build follows until a Hub session picks it up.
- GCS latent capabilities (idea seeds, nothing committed): (a) object-change event triggers (Cloud Functions/Eventarc) — push-based upgrade to the pulse-polling "watch for file → react" pattern if the platform leans harder on GCS; (b) lifecycle rules + Nearline/Coldline/Archive tiers (~$0.001/GB-mo) for finished masters / old episodes; (c) guest signed-write upload links — one-time minted URL, guest drops headshot/media with no account (natural fit for the CRM surface file-drop leg); (d) public-prefix embeds (`public/…` open, `private/…` signed, same bucket) for intended-public assets like investor reels — no adaptive bitrate, egress billed per GB, never for unreleased content.
- Computed episode number surfacing at D-1 — QoL, confirmed wanted 2026-06-09. Build the unbuilt half of Arrange decision #5: compute rank-by-`Release_Date` among non-archived episodes, surface on episode card / Release-tomorrow cue for manual Spotify entry. In-spoke decision: retire hand-managed `Episode_Sequence` display sites (~5 legacy render sites) or keep the stored column as the frozen record for live/archived rows. AD #28 holds — computed display, never a column write.
- Stale comment `dwyp_app.js:978` references deleted `placeAssetInSlot` — one-line fix, batch into any spoke touching that file.
- Shadow Index — parked new-agent spec (not built). Gemini-owned cross-corpus speculative index, complement to the Claude-exact single-episode Index: nightly (or on-new-transcript) pass writes cross-episode hypotheses (guest intersects, theme echoes, conflicts) to a spreadsheet; human filters — high wrong-tolerance by design. Division of labor: Claude = exact/trusted/build-time, Gemini = associative/cheap/nightly. **Load-bearing quarantine: spreadsheet lives OUTSIDE `CORPUS_DRIVE_FOLDER_ID`** (new Governance key) — Shadow reads transcripts (± Claude-exact episode Indexes), never its own prior output; structural boundary against self-eating, enforced by location + prompt. Open when built: read source (transcripts only vs. + Indexes), schema columns, trigger, human review/promote flow (can a promoted row feed anything without breaking quarantine?), relation to Vertex-query-shaping. Depends on: Episode Index writing reliably + corpus volume.
- Anchor uniqueness verification — build-time check in `buildEpisodeIndexV2` (post-Claude, pre-Drive-write): for each `SEGMENT:` anchor, string-search transcript, expect exactly 1 match; flag 0 (paraphrase — verbatim discipline broke) or 2+ (collision) to Audit_Trail with epUid + anchor + count; optional low-priority task; never auto-rewrite (regeneration decision, not silent patch). Build only if collisions recur in practice, or fold into companion-wiring spoke (first consumer depending on anchors resolving). Pairs with freshness check (transcript last-updated vs. `BUILT_FROM` stamp) as one "map still points true" validation pass. Closes the "anchor uniqueness at scale" Open item under Track A.
- Mending Fairy slot-integrity pass — Final_Remove Tier 3 auto-reconciliation (deferred at SPOKE_Final_Remove_Proxy_Replace). Natural v2 mend op: `reconcileFinalEpisodeSlot(epUid)` already exists as the callable; wrap in `_mendDecide`/`_mendUpdate` backoff. T1 (additive/reversible — reconcile only, never trashes).
- State-gate dematerialization — Task Projection held phase 2 (AD #126): convert state-advancing task rows to card affordances. Gated on projection proven live over time (live since 2026-06-07). Hub design session before any build.
- `#app` shell `max-width:480px` — AppSheet-era mobile phone-width inheritance. Studio overlay already breaks out to full desktop; only the shell carries the old assumption. Phase 3.1 shell re-layout. Not blocking anything.
- Icon rail migration — Reels, Schedule, Episode surfaces not yet wired to `enterIconRailMode`. Old text-tab bars (`stReelRailTabBar`, `schRailTabBar`) still active; Episode companion still uses `stEpDrawerIcon` + X button. Infrastructure in place; migrate as a separate spoke per surface.
- Cross-session rail persistence — explicitly out of scope by decision; within-session memory only.

**Parked — this session (2026-05-30/31):**
- Filing-redesign cluster (from SPOKE_Tasks_1): `Images_Status` column inert (no write path); dead read sites; dead `Status:"complete"` writer at `filing_fairy.js:301`; date-based archive trigger undefined. Resolve together in Filing redesign spoke.
- Icon rebuild: film icon is muted-always (lit-always is mildly wrong, not just unpolished). Headphones gold/red inverted in current build. Full muted/red/gold icon pass required — batches with the icon-model build. Do not fix piecemeal.
- Gemini brain-dump parse (unstructured paragraph → four task fields). Capture works via form + voice-keyboard; parse is convenience on a working function. Help-Desk-as-task-inlet parked with it.
- artist_fairy.js janitorial: dead helpers (`buildPlaceholderMap`, `resolveGuestHeadshotBlob`, `resolveHostHeadshotBlob`, `populateSlideDeck`, `processSlide`, `processGroup`, `replaceTextPlaceholders`) + stale file header comment.
- `upsertEpisodes` DEFAULTS stale entry: `Images_Status: "pending"` (harmless, header-driven, silently skipped).

### Upcoming (no order)

- **CRM surface** — next major surface (2026-06-09 hub decision). JT needs a place to organize guest info. Two legs: (1) drop-files-into-app — JT drops a batch of files, they land in the guest's Contact Library folder; (2) post-transcript contact enrichment — Herald does the pre-recording first pass; once the transcript exists, a second pass extracts richer contact info from episode content. This is the resolved meaning of the Arrange-era "enrichment of content" note (episode content enriches contact records). Needs a Hub design session to scope.
- **Identity addition in Master Template** — guest/host identity block in v3 template.
- **Prettify the app** — visual polish pass; Figma-first with exact values.

### Later
- Filing Fairy expansion — subfolder moves on filing; uncomment corpus deposit when us-south1 API available
- Gemini auto-transcription spoke
- Clerk Fairy rebuild
- Mending Fairy expansion — `correctGuestName()`, `archiveEpisode()`, re-enrichment trigger; I2+ ops (show-notes repair, catalog expansion) pending provenance spoke
- Guest Brief formatter + Guest Doc formatter
- JT social tasks + Audra release tasks
- Zernio integration
- Herald suppression — delay Guest Brief Review task until closer to recording date
- Server-side task security filtering — v2
- Contact folder recreation — graceful recovery when folder missing
- Task List visual polish
- JT Clip Review Tool
- AI reel hook/title analysis — Gemini multimodal video review. Major lift. Architecture TBD.
- F-8 — Un-approve / un-sort asset toggle

### Ideas — Back Burner
*(Not sized, not sequenced — come back to these in a hub session)*

- **JT episode promo Reel** — short promo Reel per episode (not a clip). Optional Tuesday drop cadence. Optional: Micropod drops 2x/week. Generated in the Write tab.
- **Episode detail page** — each episode gets its own page with longer notes.
- **Podcast URL** — dedicated podcast URL for discoverability.


---

## Open Issues

| # | Area | Issue | Action |
|---|---|---|---|
| 15 | `dwyp_ui.html` | **CLOSED (2026-05-25).** Drive chrome now shown intentionally. Crop hack retired. | — |
| 16 | `dwyp_app.gs` / `dwyp_ui.html` | **Companion image prompts temporarily removed (2026-05-22).** `[[PROMPT:]]` instruction + definition stripped from `STUDIO_MODE_INSTRUCTIONS.images`; regex and `stTapChip` handler removed from UI. | Re-add when image prompt → background generation flow is ready to be wired properly. |
| 2 | `dwyp_app.gs` | **RESOLVED (2026-05-24).** `requestEpisodeRevisions` spawns/appends `Revise_Episode` task for Audra with folder deep-link. Compose loop writes individual Episode_Log rows via `submitEpisodeCommentRow`. | — |
| 3 | RAG Engine | `importRagFiles` API unavailable in us-south1. | Corpus sync commented out. Manual GCP Console import is operational path. Revisit when us-south1 expands. |
| 4 | `herald_fairy.gs` / `secretary_fairy.gs` | Contact record exists but Contact Library folder missing → flags duplicate. | Fix before next new guest run. |
| 6 | `dwyp_ui.html` | B-5: Bottom border missing on quote card template. | Awaiting reference image from JT. |
| 7 | `dwyp_ui.html` | B-3: Fonts not rendering correctly in Add Text. Should be Libre Baskerville / Nunito (Sofia Pro sub). | IW polish spoke (F-5). |
| 8 | `dwyp_ui.html` | Write tab has no episode picker — `stSelectEpisode()` only fires from Design tab's dropdown. `stRagContext` stays empty in Write mode; Claude has no episode context. | Part of Write tab UI redesign (three-panel). Not a wiring bug. |
| 9 | `dwyp_ui.html` / `dwyp_app.gs` | **CLOSED (2026-05-26).** `stRenderAccordionTasks()` rewritten to show all tasks via `renderTaskCard()`, removing the loose-tasks-only filter. All tasks (episode-linked and loose) now appear in the Studio Tasks accordion. | — |
| 10 | `dwyp_ui.html` | Trim button opens `vids.google.com` root — needs a deep-link to the specific Drive/GCS file. | Deferred until GCS embed + Sentinel confirmed. Scope in wiring hub. |
| 11 | `dwyp_ui.html` | Processing overlay fires immediately on Trim click — should only show while async work is in progress. | Deferred until real Trim async path is wired. |
| 17 | `dwyp_ui.html` | Manually pausing the episode video then sending a comment produces no timestamp. Only focus-triggered pauses (clicking the comment field while the video is playing) stamp a time. | Deferred — log for a future spoke. |
| 12 | `artist_fairy.js` | Host Quotes downstream cleanup. Audit `buildPlaceholderMap()` for HOST QUOTES extraction; remove `{{HOST_QUOTE_*}}` placeholders from Square/Horizontal/Vertical slide deck templates in Drive. Cosmetic — not load-bearing. `{{HOST_QUOTE_*}}` tokens already resolve to empty strings via graceful-degradation. | Open spoke. |
| 13 | Caption Voice Supplement | Doc states "em-dash as breath beats" — directly contradicts v2.4 Voice Prohibitions (em-dashes forbidden everywhere except quote attribution). | Fix at source before next editorial pass. |
| 14 | `fairy_circle.js` | Pile of Puppies footer hardcoded in template. Migrate to `DESCRIPTION_FOOTER` key in Governance_Config; append at assembly time parallel to `_appendCaptionSignoff_`. | Spoke-sized. Queue after v2.4 patch applied. |
| 18 | `dwyp_ui.html` | **Arrange drop-zone data-idx overlap.** When month bands are contiguous without live episodes between them, trailing drop zone of one band and leading drop zone of the next band share the same `data-idx` value. `querySelector('[data-idx="N"]')` returns the first match, so the wrong drop zone gets highlighted on hover. Insert position is functionally correct (`insertIdx` is passed directly through `ondrop`, not read from DOM). Visual-only glitch. | Deferred. Fix: unique drop-zone IDs or sequential counter across all bands. |

---

## Pending Decisions

| Item | Status |
|---|---|
| Corpus sync schedule | Drive connector daily via GCP Console. Manual trigger after each filing. |
| `parsePipelineBlock()` / housekeeping | Vestigial — Social Vert retired. Confirm removal before housekeeping spoke. |
| JT's device — iPhone SE? | Determines F-2. |
| Contacts completion signal | Bio + one social + Headshot? Not formally defined. |
| Contacts Relationship_Type editable? | Part of C-1. Awaiting JT feedback. |
| D-1 container architecture | User-created categories as data structure from day one. |
| Artist Fairy post-redesign role | No longer produces quote graphics. May handle Reel thumbnails or other assets. Confirm before that spoke opens. |
| Voice Prohibitions ↔ Brand_Identity_V2 reconciliation | v2.4 absorbed Brand_Identity_V2 forbidden phrases into Voice Prohibitions, but a full cross-doc reconciliation remains. Decision-heavy — separate hub session. |
| **Asset_Library `chat_history` column (OQ-G)** | Sheet is 18 cols (vestigial cols 19/20 deleted by Audra 2026-06-09; code already aligned). Add `chat_history` as col 19 before Phase 4 (Publish Companion). |
| **Conversation history turn cap (OQ-E)** | N=? Decide before Phase 4 spoke. |
| **Sibling context cap UX (OQ-D)** | Reduced/moot under Design–Schedule split; companion = asset-in-focus only. Revisit only if Schedule surface reintroduces stacks. |
| **Reels revision input design** | Text box → task spawn with JT's words (Option A/Notes). Small spoke. Ping Audra when ready to spec. |
| **Schedule rail composition** | What rail icons appear on guest → Schedule. Gated on Build Playbook 3.3 (Schedule Panel UX Hub session). |
| **Audra Ops as User Bucket** | Bucket spec doesn't exist yet. Define Buckets internal structure first (Tasks → Buckets Hub session). |
| **Help Desk placement** | Universal right-rail icon vs. off-rail. Role-filtered availability is a separate question. Never formally resolved. |
| **AI assignment per surface** | Model choice for Guest → Episode, Guest → Reels, Guest → Images, Guest → Schedule — TBD in rail composition table. Parked for follow-up Hub. |
| **JT-variant asset schema** | Show Notes Hook/Quote/Caption submissions arrive pre-bundled with caption. Caption-traveling-with-asset for JT-variants: reuse `Caption_Host` or add sibling field. Decide before Show Notes spoke. |
| **Canon pass — Platform_Reference task-dispatch AD** | Three-axis taxonomy in task-dispatch AD is obsolete; needs forward-looking correction. No code impact — doc-only pass. |
| **Canon pass — Reels Surface doc rename** | `DWYP_Spoke_Reels_Surface.md` does not follow Document Provenance scheme (should be `SPOKE_Reels_Surface.md`). Rename on next canon pass or before that spoke resumes. |

---

## Reminders — Action Required

| Item | Status |
|---|---|
| **[SPOKE_Tasks_1]** Run `clasp login` + `clasp push` to deploy five-state Status changes to GAS | ⏳ Blocked — clasp re-auth required |
| **[SPOKE_Tasks_1]** Update Episodes sheet Status data-validation: `upcoming`, `in_production`, `review`, `ready_to_release`, `live`, `archived` (remove `active`, `complete`) | ⏳ Audra — in-sheet |
| **[SPOKE_Episode_Tasks_1]** Episodes sheet col L: confirm header is `Final_Episode_ID` | ✅ Done (2026-05-31) |
| **[SPOKE_Episode_Tasks_1]** `SPOTIFY_EPISODE_BASE` in Governance_Config | ✅ Done (2026-05-31) |
| **[SPOKE_Episode_Tasks_1]** Cleanup spoke: remove orphaned `triggerReadyForRelease`, `checkReadyForRelease`, RFR modal, `.btn-ready-for-release` CSS | ⏳ Future cleanup spoke |
| **[SPOKE_Tasks_1]** Relabel existing `active` rows: transcript+index_v2 set → `in_production`; review tasks closed → `ready_to_release`; no transcript yet → `upcoming` | ⏳ Audra — in-sheet |
| Register nightly trigger for `triggerNightlyHousekeeping()` | ⏳ Not yet done |
| After each episode files: trigger corpus sync in GCP Console | ⏳ Ongoing — manual step |
| Set `STUDIO_LLM_MODE = claude` in Governance_Config | ⏳ Before Studio backend spoke |
| Add `ASSET_LIBRARY_TAB_NAME = Asset_Library` to Governance_Config | ⏳ Verify before Spoke 2 push |
| Add `STUDIO_TOKEN_WARNING_THRESHOLD = 50000` to Governance_Config | ⏳ Before Studio backend spoke |
| Add `PUBLISH_CHAT_HISTORY_TURN_CAP` to Governance_Config | ⏳ Before Phase 4 (Publish Companion) |
| Delete `test_batchEnrichReels` time-based trigger (every 30 min) | ⏳ Enrichment complete — trigger no longer needed |
| Retire `PUBLISH_LLM_MODE`, `IMAGE_WORKSHOP_GEM`, `IW_EXPORT_FALLBACK_FOLDER_ID`, `NOTEBOOKLM_LINK` | ⏳ Pipeline cleanup spoke |

---

## Governance Keys — Current State

**Full key list is authoritative in Governance_Config sheet.** This section tracks status only.

**Populated and confirmed:**
`GEMINI_API_KEY`, `MODEL_NAME`, `CLAUDE_API_KEY` ✅, `MASTER_SHEET_ID`, `SPOTIFY_EPISODE_BASE` ✅ (base URL for Release task Payload_Link — populated 2026-05-31), `MASTER_TEMPLATE_ID`, `STAGING_DEPLOYMENT_URL` ✅, `STAGING_SHEET_ID` ✅, `REVIEW_GCS_BUCKET` (`dwyp-review-playback`) ✅, `GCS_SIGNER_SA` (`309883149140-compute@developer.gserviceaccount.com`) ✅, `GCS_EXPIRY_SECONDS` (`28800`) ✅ — note: signing requires explicit project-level signBlob grant (AD #122), `RAW_PRODUCTION`, `STAGING_DRAFTS`, `FINISHED_EPISODES`, `DWYP_CALENDAR_ID`, `CALENDAR_TRIGGER_PREFIX`, `ASSIGNEE_HOST`, `ASSIGNEE_PRODUCER`, `HOST_NAME`, `HOST_EMAIL`, `CONTACT_LIBRARY_FOLDER_ID`, `PODCAST_NAME`, `HERALD_RESEARCH_PROMPT_KEY`, `HERALD_BIO_PROMPT_KEY`, `HERALD_BRIEF_PROMPT_KEY`, `CONTENT_SENSITIVITY_ID`, `BRAND_VOICE_ID`, `NOTEBOOK_STAGING`, `ARCHIVE_FOLDER_ID`, `RELEASE_REMINDER_HOURS`, `SCRIPT_ID`, `INTAKE_NAME_KEY`, `INTAKE_EMAIL_KEY`, `INTAKE_REFERRAL_KEY`, `ARTIST_THUMBNAIL_DECK_ID`, `ARTIST_SQUARE_DECK_ID`, `ARTIST_VERTICAL_DECK_ID`, `IMAGE_BACKGROUND_LIBRARY_ID`, `STUDIO_CORPUS_ID`, `CORPUS_DRIVE_FOLDER_ID`, `VERTEX_RAG_REGION` (`us-south1`), `REELS_ARCHIVE_FOLDER_ID`, `POSTING_SCHEDULE_TAB_NAME`, `SOCIAL_ASSETS_TAB_NAME`, `STUDIO_ROOT_FOLDER_ID`, `STUDIO_CANVAS_MANIFEST_FOLDER_ID`, `STUDIO_DOCS_FOLDER_ID`, `STUDIO_SESSIONS_FOLDER_ID`, `EPISODE_SEARCH_INDEX_KEY`, `JT_TIMEZONE`, `AUDRA_TIMEZONE`, `PUBLISH_LLM_MODE` (gemini — retire after Spoke 1), `PRECOMP_BACKGROUND_LIBRARY_ID` (Drive folder `1Tyw7ArpdmYiKZNL4FOQNIpA4fTXkwkh6`; currently same as IMAGE_BACKGROUND_LIBRARY_ID — curated set to be built and swapped; filename convention: `bg_NNN_darktext` / `bg_NNN_lighttext`), `STUDIO_IMAGE_MODEL` = `gemini-2.5-flash-image` (locked — hub decision May 2026), `CAPTION_VOICE_SUPPLEMENT_ID` ✅ (Track B voice authority patch), `DELIVERABLES_VOICE_SPEC_ID` ✅ (Track B voice authority patch).

**Needs value set:**
`STUDIO_LLM_MODE` → `claude` (before Studio backend spoke).
`GCS_UPLOAD_EXPIRY_SECONDS` → `3600` (defaults to 3600 in code if absent; set explicitly for clarity).

**Add before Studio backend spoke:**
`ASSET_LIBRARY_TAB_NAME` = `Asset_Library`, `STUDIO_TOKEN_WARNING_THRESHOLD` = `50000`.

**Add before Phase 4 (Publish Companion):**
`PUBLISH_CHAT_HISTORY_TURN_CAP` = (TBD), `PUBLISH_SIBLING_CONTEXT_CAP` = `4`.

**Present but blank — populate when ready:**
`SCHEDULING_LINK`, `RIVERSIDE_LINK`, `INTAKE_FORM_ID`, `INVITE_EMAIL_TEMPLATE_KEY`, `DAILY_DIGEST_TIME`, `GUEST_BRIEF_TEMPLATE_ID`, `EPISODE_CARD_TEMPLATE_ID`, all seven Scribe template keys (migrate to Writer Email quick-start templates — Reframe #8).

**Retire after Spoke 1:**
`PUBLISH_LLM_MODE`, `IMAGE_WORKSHOP_GEM`, `IW_EXPORT_FALLBACK_FOLDER_ID`, `NOTEBOOKLM_LINK`.

**Already retired:**
`FRAMEIO_API_TOKEN`, `FRAMEIO_TEAM_ID`, `MAKE_FRAMEIO_WEBHOOK_URL`, `FRAMEIO_WORKSPACE_ID`, `VISUAL_DESIGNER_GEM_ID`, `GUEST_BRIEF_LIBRARY_ID`, `MAKE_PNG_CONVERSION_WEBHOOK_URL`, `PRODUCTION_NOTES_PROMPT_KEY`, `SAFETY_IMAGE_PROMPT_KEY`, `PREP_GEM_ID`, `COWORK_TRIGGER_FOLDER_ID`, `COWORK_OUTPUT_FOLDER_ID`, `MARCOM_COWORK_TIMEOUT_HOURS`, `STUDIO_CORPUS_FOLDER_ID` (renamed to `CORPUS_DRIVE_FOLDER_ID`), `SOCIAL_VERT_BUCKET`.

---

## GAS Deployment Checklist

| Item | Status |
|---|---|
| `MASTER_SHEET_ID` Script Property | ✅ Set |
| `STAGING_DEPLOYMENT_URL` and `STAGING_SHEET_ID` in Governance_Config | ✅ Set |
| `isStaging()` and `getMasterSheetId()` helpers in `fairy_circle.js` | ✅ Locked architectural pattern |
| All sheet access routes through `getMasterSheetId()` | ✅ Audit complete |
| `onFormSubmit` → `processFormSubmission` | ✅ Wired |
| `dailyPulse` time-based trigger | ✅ Set — 4am daily |
| `checkCalendarForInterviews` time-based trigger | ✅ Set — hourly |
| Web app deployed (dwyp_app.gs) | ✅ Deployed. executeAs: USER_DEPLOYING. |
| `triggerNightlyHousekeeping` time-based trigger | ⏳ Not yet registered |
| `clerk_fairy` doPost() | ⏳ Rebuild queued |
| Items 43–58 pushed and deployed | ✅ Done |
| Items 59–84 + item 91 pushed and deployed | ✅ Done |

---

## Engineering Notes

### Video Playback — Current Strategy

**Episode proxy:** Native `<video id="stEpVideo">` fed by `getEpisodeStreamUrl(episodeUid)` — a V4-signed GCS GET URL (bucket `dwyp-review-playback`, path `episodes/{EUID}/proxy.mp4`, 8h expiry). Signing: `iamcredentials.googleapis.com/v1/projects/-/serviceAccounts/{sa}:signBlob` via owner's `ScriptApp.getOAuthToken()` (`cloud-platform` scope in manifest). Both playback and upload URLs flow through shared `_signV4()` helper. URL re-minted on every open; never stored. Enables JS control (focus-pause-freeze-timestamp, scrub, resume). File must exist at exact GCS path before player opens. **Troubleshooting signBlob 403:** `iam.automaticIamGrantsForDefaultServiceAccounts` org policy is active — Owner on the project does NOT implicitly grant `iam.serviceAccounts.signBlob`; the grant must be set explicitly at project level. Use GCP Console → IAM & Admin → Policy Troubleshooter to verify. SA keys are forbidden (AD #122) — keyless signBlob only.

**Episode proxy upload:** Browser-side resumable upload direct to GCS. `getEpisodeUploadUrl(episodeUid)` mints a V4-signed POST URL (1h expiry). Client POSTs to signed URL with `X-Goog-Resumable: start` → reads `Location` session URI → sends 5 MB chunks via PUT with `Content-Range`; 308 = continue, 200/201 = done.

**CORS:** Confirmed origin is `https://n-z5do…-script.googleusercontent.com` (Apps Script sandbox host). If uploads CORS-fail in the future, read the exact origin from the DevTools console preflight error and update the bucket's CORS allow-list. See AD #122.

**Browser gotchas** (server examples don't warn about these):
1. **Init can return 201, not just 200.** Handle both. Our code does.
2. **308 Resume Incomplete kills XHR.** Browsers fire `onerror` (status 0) on a 308 with no `Location` header — they can't complete the redirect. Use `fetch(redirect:'manual')` instead; a 308 resolves as `response.type === 'opaqueredirect'`. Our code does this.
3. **Do not set `Content-Length`.** It's a forbidden request header in browsers. The browser computes it from the body automatically. Setting it throws a console error and may cause the request to fail. Our code does not set it.

**Reels:** Drive `/preview` iframe. `getReelStreamUrl(fileId)` sets sharing to anyone-with-link and returns `/preview` URL. URL cached on `reel._streamUrl` — first tap pays GAS round-trip, subsequent taps instant. Drive chrome shown intentionally (two-person internal app). Crop hack retired 2026-05-25 — produced a 2px left-edge artifact unreachable from CSS. See memory: `project_video_hosting.md` — Cloudflare Stream or Mux when ready to upgrade.

**Paths ruled out for reels:**
- `uc?id=` + native `<video>` — Drive returns a virus-scan HTML interstitial for files over ~25MB; scrubbing broken (no byte-range). Dead on real reels.
- GCS migration for reels — deferred; GAS blob ceiling + microservice requirement. Bucket `dwyp-reel-playback` is an orphan (zero code references) — safe to delete in GCP Console at discretion.

---

## Canvas State Architecture — Locked Principles

### Render-on-send
Canvas_State JSON is the authored artifact. PNG is rendered at dispatch time — never stored as a pre-render that must be kept in sync. The `addToWeekAsImage` schedule-time PNG creation is a vestigial preview/backup path. `exportAssetToDrive` is the formal manual-backup channel. JT can edit canvas state until the moment of send.

### Dual-JSON for canvas state
Canvas state has two destinations with different constraints:
- **In-memory (`pb.cardCanvases[assetId]`):** full JSON with embedded base64 background. Enables synchronous restore on reopen. No async race window.
- **Server (`Canvas_State` cell on AL row):** stripped JSON — `obj.src` emptied for data URIs, filter matrices nulled. Fits under GAS URL-encoded payload limit and Sheets 50k char cell cap. Reload-time hydration re-applies background from precomp pool by index.

`pbSaveAndExit` save-core produces both via different post-processing from one `canvas.toJSON()` call. Never collapse them into one variable — they serve different consumers with different constraints.

---

## Known Issues / Troubleshooting

- **RAG corpus sync blocked in GAS:** `importRagFiles` API unavailable in us-south1. Manual GCP Console import is operational path. Code preserved in housekeeping.gs with dated comment.
- **Herald re-enrichment:** Run Herald button on Fairy Remote Control is the re-enrichment trigger.
- **F-7 timezone:** `JT_TIMEZONE` and `AUDRA_TIMEZONE` governance keys added. Verify recording reminder timing before first live recording.

---

## Episode Roster

| Seq | Guest | Release Date | Tier | Status |
|---|---|---|---|---|
| 1 | Carrie Sipe | — | EH | Released 2026-05-19 (EP-260428-1928) |
| 2 | Dr. Meenakshi Aggarwal | June 2 | EH | Active episode (EP-260504-0736) |

> Full release schedule lives in the Episodes tab of the Master Sheet — that is the authoritative source.

---

## Rules for This Codebase

- All GAS code written by Claude only. Never edited directly in Apps Script or by Gemini.
- Claude Code (VS Code + Claude Code extension) is the preferred workflow.
- Prose behavior controlled through Master Template, not code.
- All config values in Governance_Config. No hardcoded strings.
- Confirm before writing or patching any script. Confirm save/push before moving on.
- Multi-file patches fine when tightly coupled.
- Intentional deletion policy (replaced Preservation Mandate): nothing removed without explicit decision. Renames and dead code removal require explicit approval. Active function behavior never changed without a confirmed design decision.
- All sheet access goes through `getMasterSheetId()` — never read `MASTER_SHEET_ID` directly except inside `getGovernance()` (the bootstrap exception).
- New features must apply Surface Principle (mobile = ops, desktop = creation) and Performance Principle (show first, sync second; version-stamp invalidation). Surface decisions that conflict with the principles are flagged, not silently resolved.
- Once `bumpVersion()` ships in Phase 1.2, every write path must call it for the affected domain. Until then, surface back when writing data-mutation code so the versioning gets retrofitted cleanly.
- Mode awareness governs session behavior — Hub mode (no code, design and capture), Spoke mode (one focused unit, surface back at push checkpoints), Verification mode (report findings only, do not fix).
- `CLAUDE.md` in repo root instructs Claude Code to read State + changelog + foundation docs (per tier) on every session.

---

*Version history stripped. See git log for session-by-session detail.*


